---
phase: 03-linear-proxy
plan: 03
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - scripts/linear/__fixtures__/gql-clean.ts
  - scripts/linear/__fixtures__/gql-with-email.ts
  - functions/api/linear/[[path]].test.ts
  - functions/api/linear/[[path]].ts
autonomous: true
requirements: [REQ-PROXY-1, REQ-PROXY-2, REQ-PROXY-3, REQ-PROXY-4]
must_haves:
  truths:
    - "GET /api/linear/<unknown> returns 404 (only registered ops served)"
    - "GET /api/linear/snapshot on a clean upstream returns 200 with schema-valid RoadmapJson"
    - "An upstream response containing an email causes assertNoLeak to throw → 502 with no PII in the body"
    - "The LINEAR_API_KEY value never appears in any response body across success and all error paths"
    - "Missing env.LINEAR_API_KEY returns 500 with a generic body; Linear non-ok or GraphQL errors return 502"
    - "Successful responses set Cache-Control: private, max-age=60"
  artifacts:
    - path: "functions/api/linear/[[path]].ts"
      provides: "Proxy handler: OPERATIONS registry, binding fetch, mapWorkspace→buildSnapshot→assertNoLeak, error table, cache, per-isolate rate-limit"
      exports: ["onRequestGet"]
      min_lines: 40
    - path: "functions/api/linear/[[path]].test.ts"
      provides: "Unit tests for REQ-PROXY-1..4 with mocked fetch + constructed context"
      contains: "onRequestGet"
    - path: "scripts/linear/__fixtures__/gql-clean.ts"
      provides: "GqlResponse-shaped clean fixture (data.initiatives.nodes / data.projects.nodes)"
    - path: "scripts/linear/__fixtures__/gql-with-email.ts"
      provides: "GqlResponse-shaped fixture with a planted email for the leak regression"
  key_links:
    - from: "functions/api/linear/[[path]].ts"
      to: "scripts/linear/transform.ts"
      via: "buildSnapshot import (runs assertNoLeak)"
      pattern: "buildSnapshot"
    - from: "functions/api/linear/[[path]].ts"
      to: "scripts/linear/client.ts"
      via: "mapWorkspace import"
      pattern: "mapWorkspace"
    - from: "functions/api/linear/[[path]].ts"
      to: "env.LINEAR_API_KEY"
      via: "Authorization header (binding, never logged)"
      pattern: "env.LINEAR_API_KEY"
---

<objective>
Build the Linear proxy Pages Function test-first: the named-operation registry, the binding-authenticated fetch, the transform-and-leak-gate pipeline, the error table, the cache header, and a minimal per-isolate rate limit.

Purpose: This is the heart of the phase and it crosses a real trust boundary (a new authenticated endpoint holding the Linear token). CLAUDE.md + the spec REQUIRE test-first for auth/API/token handling, so this plan is strict RED→GREEN: write the four spec tests (REQ-PROXY-1..4) failing first, then implement the handler to green.
Output: GqlResponse-shaped fixtures, the handler test, and the full handler (replacing the 03-01 probe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-RESEARCH.md
@docs/superpowers/specs/2026-06-28-linear-proxy-access-design.md
@.planning/phases/03/03-VALIDATION.md
@.planning/phases/02/SECURITY.md

<interfaces>
<!-- Available after 03-01 + 03-02. Confirm import paths against 03-01-SUMMARY (cross-dir vs src/lib/linear). -->

From scripts/linear/query.ts: `export const WORKSPACE_QUERY: string`
From scripts/linear/client.ts: `export function mapWorkspace(json: GqlResponse): RawWorkspace`, `export interface GqlResponse`
From scripts/linear/transform.ts: `export function buildSnapshot(raw: RawWorkspace, opts?): RoadmapJson` (calls assertNoLeak then RoadmapJsonSchema.parse internally)
From src/lib/roadmap/schema.ts: `RoadmapJsonSchema`, `type RoadmapJson` (for the test's safeParse assertion only — the handler does not re-validate)

GqlResponse shape (from client.ts) for fixtures:
- `data.initiatives.nodes: Array<{ id, name, color: string|null, state: string }>`
- `data.projects.nodes: Array<{ id, name, description: string|null, initiative: { id } | null, state: { name, type }, priority: number, startedAt: string|null, targetDate: string|null, projectMilestones: { nodes: Array<{ id, name, targetDate }> }, issues: { nodes: Array<{ state: { type } | null }> } }>`
- optional `errors?: Array<{ message }>`
  NOTE: existing __fixtures__/raw-clean.ts is RawWorkspace-shaped (flat `initiativeId`, no `data` envelope). The new fixtures MUST be the GQL envelope shape above (nested `initiative: { id }`, wrapped in `data`). Reuse raw-malicious.ts's email `secret@example.com` for the leak fixture.

Handler contract (from research Pattern 1 + Code Examples — implement, do not copy verbatim):
- `export const onRequestGet: PagesFunction<Env>` with `interface Env { LINEAR_API_KEY: string }`
- `const op = Array.isArray(params.path) ? params.path[0] : params.path` (catch-all [[path]] gives an ARRAY)
- `const OPERATIONS = { snapshot: { query: WORKSPACE_QUERY, transform: buildSnapshot } } as const`
- Linear endpoint: `https://api.linear.app/graphql`, POST, headers `Content-Type: application/json` + `Authorization: env.LINEAR_API_KEY`, body `JSON.stringify({ query: entry.query })`
</interfaces>
</context>

<feature>
  <name>Linear proxy Function — named-op snapshot</name>
  <files>functions/api/linear/[[path]].ts, functions/api/linear/[[path]].test.ts, scripts/linear/__fixtures__/gql-clean.ts, scripts/linear/__fixtures__/gql-with-email.ts</files>
  <behavior>
    - Test (REQ-PROXY-1): `onRequestGet(ctx(["nope"]))` resolves to a Response with status 404 and body "unknown operation". No fetch is called.
    - Test (REQ-PROXY-2): with `fetch` stubbed to return 200 `{ data: gqlClean }`, `onRequestGet(ctx(["snapshot"]))` → status 200; `RoadmapJsonSchema.safeParse(await res.json()).success === true`; response header `Cache-Control` === "private, max-age=60".
    - Test (REQ-PROXY-3): with `fetch` stubbed to return 200 `{ data: gqlWithEmail }`, `onRequestGet(ctx(["snapshot"]))` → status 502; `(await res.text())` does NOT contain "@" and does not contain "secret@example.com".
    - Test (REQ-PROXY-4): across the success path and every error path (404 unknown op, 500 missing key, 502 upstream/leak), assert the response body never contains the env key value ("lin_api_TESTKEY000") nor matches `/lin_api_/`.
    - Test (500): `onRequestGet({ params: { path: ["snapshot"] }, env: {} })` → status 500, generic body, fetch never called.
    - Test (502 upstream): `fetch` stubbed to return `{ status: 500 }` (non-ok) → 502; and separately a 200 with `{ errors: [{message}] }` → 502.
  </behavior>
  <implementation>
    Implement onRequestGet exactly to the handler contract in the interfaces block: registry lookup (404 on miss, before any fetch), env.LINEAR_API_KEY presence check (500 generic body, before any fetch), single POST to the Linear endpoint with the Authorization binding header, 502 on `!upstream.ok` or `json.errors?.length`, then `entry.transform(mapWorkspace(json))` inside a try/catch where ANY throw (assertNoLeak/parse) maps to a generic 502 — the catch body MUST NOT include the upstream body or any auth header (Pitfall 3). Success returns `new Response(JSON.stringify(result), { status: 200, headers: { "Content-Type": "application/json", "Cache-Control": "private, max-age=60" } })`. Add the minimal module-scope per-isolate fixed-window rate limit (LIMIT=30, WINDOW_MS=60_000) checked before the upstream fetch → 429 "rate limited" when exceeded; keep it exactly that small (no KV/DO). NEVER `console.log` env.LINEAR_API_KEY and never return the upstream body. Confirm import paths match 03-01-SUMMARY (relative cross-dir or src/lib/linear).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): GqlResponse fixtures + failing handler tests for REQ-PROXY-1..4</name>
  <read_first>
    - scripts/linear/__fixtures__/raw-clean.ts and raw-malicious.ts (shape + the `secret@example.com` email to reuse)
    - scripts/linear/client.ts (GqlResponse shape — nested initiative + data envelope)
    - src/lib/roadmap/schema.ts (RoadmapJsonSchema for the safeParse assertion)
    - .planning/phases/03/03-RESEARCH.md "Vitest unit test of the handler" + "Common Pitfalls"
    - .planning/phases/03/03-01-SUMMARY.md (import path strategy)
  </read_first>
  <action>
    Create scripts/linear/__fixtures__/gql-clean.ts exporting `gqlClean` in the GqlResponse `data` envelope shape (data.initiatives.nodes + data.projects.nodes, with nested `initiative: { id }`), derived from raw-clean.ts's data so the resulting RoadmapJson is schema-valid. Create scripts/linear/__fixtures__/gql-with-email.ts exporting `gqlWithEmail` — same shape but with `secret@example.com` planted in a project description (mirroring raw-malicious.ts). Create functions/api/linear/[[path]].test.ts with a `ctx(path: string[], env?)` helper (default env `{ LINEAR_API_KEY: "lin_api_TESTKEY000" }`) that casts `{ params: { path }, env }` to the handler's first param, `afterEach(() => vi.unstubAllGlobals())`, and the six tests described in <behavior> (404, 200+schema+Cache-Control, email→502 no PII, token-never-present across all paths, 500 missing key, 502 upstream-non-ok and GraphQL-errors). Use `vi.stubGlobal("fetch", vi.fn(...))`. These tests MUST fail now (the 03-01 probe handler is trivial). Run the suite to confirm RED, then commit `test(03-03): add failing handler tests for REQ-PROXY-1..4`.
  </action>
  <verify>
    <automated>pnpm test functions/api/linear 2>&1 | grep -qi "fail\|error" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/linear/__fixtures__/gql-clean.ts exports `gqlClean` with a top-level `data.initiatives.nodes` and `data.projects.nodes`
    - scripts/linear/__fixtures__/gql-with-email.ts exports `gqlWithEmail` containing `secret@example.com`
    - functions/api/linear/[[path]].test.ts imports `onRequestGet` from "./[[path]].ts" and asserts statuses 404/200/502/500
    - vitest runs (does NOT skip) the new file — test count for functions/ is ≥ 6 (proves the 03-02 glob works)
    - `pnpm test` shows these tests FAILING (RED state captured before implementation)
  </acceptance_criteria>
  <done>Fixtures and failing tests exist and run (not skipped); RED state committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): implement the proxy handler to pass all tests</name>
  <read_first>
    - functions/api/linear/[[path]].ts (the 03-01 probe — to be fully replaced)
    - functions/api/linear/[[path]].test.ts (the failing tests from Task 1)
    - .planning/phases/03/03-RESEARCH.md "Reading the binding…" + "Minimal per-isolate fixed-window rate limit" + Pattern 1
    - scripts/linear/transform.ts (buildSnapshot/assertNoLeak — reused, not reimplemented)
  </read_first>
  <action>
    Replace functions/api/linear/[[path]].ts with the full handler per the <implementation> block: `interface Env { LINEAR_API_KEY: string }`, the OPERATIONS registry with the single `snapshot` entry, catch-all array `op` extraction, ordered checks (404 unknown op → 429 rate-limit → 500 missing key → fetch → 502 on !ok/errors → transform in try/catch → 200 with Cache-Control), and the module-scope fixed-window rate limit (LIMIT=30, WINDOW_MS=60_000). Import WORKSPACE_QUERY, mapWorkspace+GqlResponse, and buildSnapshot from the paths recorded in 03-01-SUMMARY. Do not log or echo the key; 502/error bodies are generic strings only. Run the suite to GREEN, then commit `feat(03-03): implement Linear proxy Function (named-op snapshot)`.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - functions/api/linear/[[path]].ts contains `export const onRequestGet`, `interface Env`, `OPERATIONS`, and `Cache-Control` value `private, max-age=60`
    - `grep -c "console.log" functions/api/linear/[[path]].ts` returns 0
    - `grep -c "linearRes.text()\|upstream.text()\|await.*\.text()" functions/api/linear/[[path]].ts` returns 0 (no upstream body passthrough)
    - `pnpm test` exits 0 — all six tests green (REQ-PROXY-1..4)
    - `pnpm typecheck` exits 0 (no `any` in the handler — REQ-TYPE confirmed)
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>The handler passes all REQ-PROXY tests, typechecks with no any, and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/linear/* | Untrusted request selects an operation name |
| Function → Linear API | Server token crosses here in an Authorization header |
| Linear response → client | Upstream bytes must be projected + leak-gated before return |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering / Elevation | operation selection | mitigate | OPERATIONS registry; client cannot supply raw GraphQL; unknown op → 404 (REQ-PROXY-1, ASVS V4). |
| T-03-06 | Information Disclosure | PII through live path | mitigate | Response runs buildSnapshot (allow-list projection) → assertNoLeak (EMAIL_RE) → throw → 502, no PII (REQ-PROXY-3). |
| T-03-07 | Information Disclosure | token in response/error body | mitigate | Token only in Authorization header; generic 404/429/500/502 bodies; no upstream-body passthrough; REQ-PROXY-4 test asserts key absent on all paths. |
| T-03-08 | Information Disclosure | missing-binding stack trace | mitigate | Explicit 500 generic body when env.LINEAR_API_KEY absent; no key/value in message. |
| T-03-09 | Denial of Service | Linear abuse/cost | mitigate | Cache-Control private max-age=60 + per-isolate fixed-window rate-limit → 429; dashboard rule documented in 03-05. |
| T-03-SC | Tampering | npm/pnpm installs | accept | No installs in this plan. |
</threat_model>

<verification>
- `pnpm test` green: REQ-PROXY-1..4 plus 500/502 paths.
- `pnpm typecheck` + `pnpm lint` green: no `any`, no lint errors on functions/.
- Token-never-present test passes across success and all error paths (REQ-PROXY-4).
</verification>

<success_criteria>
- A single named-op proxy returns schema-valid RoadmapJson on success, 404 for unknown ops, 502 on upstream/leak, 500 on missing key, 429 when rate-limited — with the token never in any body and the leak gate enforced on every live byte.
</success_criteria>

<output>
Create `.planning/phases/03/03-03-SUMMARY.md` when done. Two TDD commits expected (RED test, GREEN impl).
</output>
