---
phase: 03-linear-proxy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - tsconfig.functions.json
  - tsconfig.json
  - wrangler.toml
  - .gitignore
autonomous: false
requirements: [REQ-TYPE]
must_haves:
  truths:
    - "@cloudflare/workers-types is installed as a devDependency and verified legitimate before install"
    - "vitest discovers and runs tests under functions/**"
    - "pnpm typecheck covers functions/** with @cloudflare/workers-types and no any"
    - "preview:functions runs wrangler via npx --yes wrangler@4 (no wrangler project dependency required)"
    - ".dev.vars is gitignored so the local live-test token file can never be committed"
  artifacts:
    - path: "tsconfig.functions.json"
      provides: "TS project for functions/** with @cloudflare/workers-types"
      contains: "@cloudflare/workers-types"
    - path: "vitest.config.ts"
      provides: "include glob that adds functions/**/*.test.ts"
      contains: "functions/**/*.test.ts"
    - path: "tsconfig.json"
      provides: "root project reference to tsconfig.functions.json"
      contains: "tsconfig.functions.json"
    - path: ".gitignore"
      provides: "ignore rule for .dev.vars (local Pages secret file)"
      contains: ".dev.vars"
  key_links:
    - from: "tsconfig.json"
      to: "tsconfig.functions.json"
      via: "project reference"
      pattern: "tsconfig.functions.json"
    - from: "vitest.config.ts"
      to: "functions/**/*.test.ts"
      via: "include glob"
      pattern: "functions/\\*\\*/\\*.test.ts"
---

<objective>
Lay the config foundation so the handler in 03-03 can be tested and typechecked: install the Worker types, widen the vitest include glob, add a functions TS project, wire a clean-machine live-preview script, and gitignore the local secret file.

Purpose: Without the vitest glob change the handler tests are SILENTLY SKIPPED (load-bearing, per VALIDATION Wave 0). Without `tsconfig.functions.json` the handler is not typechecked and `any` could slip in (REQ-TYPE). The `preview:functions` script must work on a clean machine without adding a `wrangler` dependency. And `.dev.vars` (used for local live testing with the real token in 03-04/03-05) MUST be gitignored before any live-test workflow runs — token sensitivity makes this non-negotiable. These are pure config edits that gate 03-03.
Output: `@cloudflare/workers-types` devDep, updated `vitest.config.ts`, `tsconfig.functions.json`, root `tsconfig.json` reference, an npx-based `preview:functions` script, a `.dev.vars` gitignore rule, and any non-secret wrangler config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-RESEARCH.md
@.planning/phases/03/03-VALIDATION.md

<interfaces>
<!-- Extracted from the existing codebase. -->

From vitest.config.ts (current):
- `test: { environment: "node", include: ["scripts/**/*.test.ts"] }` — glob must GAIN `"functions/**/*.test.ts"`.

From tsconfig.json (current):
- `{ "files": [], "references": [ { "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }, { "path": "./tsconfig.scripts.json" } ] }` — add a fourth reference.

From tsconfig.scripts.json (the analog to mirror):
- `compilerOptions.types: ["node"]`, `moduleResolution: "bundler"`, `allowImportingTsExtensions: true`, `isolatedModules: true`, strict + noUnusedLocals/Parameters, `paths: { "@/*": ["./src/*"] }`, `include: ["scripts/**/*"]`. For the functions variant: swap `types: ["node"]` → `types: ["@cloudflare/workers-types"]`, set `include: ["functions/**/*"]`, keep everything else identical (the handler imports cross-dir into scripts/linear, so the @/* path + bundler resolution must match).

From package.json (current):
- scripts: `typecheck: "tsc -b --noEmit"`, `test: "vitest run"`. Add `preview:functions`.

From .gitignore (current — confirmed):
- `.wrangler/` is ALREADY ignored; `.dev.vars` is NOT ignored (confirmed via `git check-ignore .dev.vars` → not ignored). Adding the `.dev.vars` rule is therefore mandatory, not optional.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this is the package legitimacy gate REQUIRED before installing @cloudflare/workers-types.</what-built>
  <how-to-verify>
    03-RESEARCH.md "Package Legitimacy Audit" records that slopcheck was UNAVAILABLE in the research environment; the package was verified manually (official @cloudflare scope, 7.0M weekly downloads, no postinstall, maintainers all @cloudflare.com). The scoped name `@cloudflare/*` cannot be typosquatted. Per the planner package-legitimacy gate, automated slopcheck did not run, so this install pauses for human confirmation.
    1. Open https://www.npmjs.com/package/@cloudflare/workers-types
    2. Confirm the package is published by the Cloudflare org, has millions of weekly downloads, and has no install scripts.
    3. Approve to proceed with `pnpm add -D @cloudflare/workers-types`, or reject.
  </how-to-verify>
  <resume-signal>Type "approved" to install, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @cloudflare/workers-types as a devDependency</name>
  <read_first>
    - package.json (devDependencies block)
    - .planning/phases/03/03-RESEARCH.md "Standard Stack" + "Package Legitimacy Audit"
  </read_first>
  <action>
    Run `pnpm add -D @cloudflare/workers-types`. Use the npm `latest` dist-tag (research recorded `^4.20260628.1`, but this is a daily-versioned package — re-verify the resolved version is current at install time and pin the caret range pnpm writes). Confirm `scripts.postinstall` is absent in the installed package. Do NOT add `wrangler` as a project dependency (it is invoked via `npx wrangler@4` per research — Environment Availability).
  </action>
  <verify>
    <automated>grep -q "@cloudflare/workers-types" package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains `@cloudflare/workers-types`
    - pnpm-lock.yaml updated
    - `node -e "require('@cloudflare/workers-types/package.json')"` does not error (package resolves)
  </acceptance_criteria>
  <done>The official Worker types package is installed; no postinstall script ran.</done>
</task>

<task type="auto">
  <name>Task 2: Add functions tsconfig, root reference, vitest glob, npx preview script, .dev.vars gitignore, and wrangler config</name>
  <read_first>
    - tsconfig.scripts.json (the project to mirror)
    - tsconfig.json (root references list)
    - vitest.config.ts (include glob)
    - wrangler.toml
    - package.json (scripts block)
    - .gitignore (confirm .wrangler/ present, .dev.vars absent)
    - .planning/phases/03/03-RESEARCH.md "Open Questions #2" + "Recommended Project Structure"
  </read_first>
  <action>
    Create tsconfig.functions.json by mirroring tsconfig.scripts.json: identical compilerOptions EXCEPT `types: ["@cloudflare/workers-types"]` (not `["node"]`) and a distinct `tsBuildInfoFile`; set `include: ["functions/**/*"]`; keep `moduleResolution: "bundler"`, `allowImportingTsExtensions: true`, strict, `noUnusedLocals`/`noUnusedParameters`, and the `paths: { "@/*": ["./src/*"] }` block (the handler resolves `RoadmapJsonSchema` transitively via scripts/linear/transform.ts which imports from src). Add `{ "path": "./tsconfig.functions.json" }` to the `references` array in root tsconfig.json. In vitest.config.ts add `"functions/**/*.test.ts"` to the `include` array (keep `"scripts/**/*.test.ts"`) — this is LOAD-BEARING; without it the handler tests in 03-03 are silently skipped.

    Add a `preview:functions` script to package.json with the EXACT value `npx --yes wrangler@4 pages dev dist` (NOT `wrangler pages dev dist`) — the `npx --yes wrangler@4` form runs on a clean machine without adding a `wrangler` dependency to the repo (cross-AI review MEDIUM).

    Add `.dev.vars` to .gitignore on its own line (place it near the existing `.env` / `.wrangler/` rules). This is MANDATORY: `.dev.vars` is the local Pages secret file holding the real LINEAR_API_KEY during live smoke testing, and it is currently NOT ignored. Do not remove or reorder existing .gitignore entries (Surgical Changes).

    wrangler.toml: add only non-secret config Functions need — research indicates likely NONE beyond the existing `name`/`compatibility_date`/`pages_build_output_dir`; if so, leave wrangler.toml unchanged and note that in the SUMMARY. NEVER add the secret to wrangler.toml.
  </action>
  <verify>
    <automated>pnpm typecheck && git check-ignore .dev.vars</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.functions.json exists and contains `"types": ["@cloudflare/workers-types"]` and `"include": ["functions/**/*"]`
    - tsconfig.json references array contains `./tsconfig.functions.json`
    - vitest.config.ts include array contains both `"scripts/**/*.test.ts"` and `"functions/**/*.test.ts"`
    - package.json scripts contains `preview:functions` with value `npx --yes wrangler@4 pages dev dist` (grep confirms `npx --yes wrangler@4`)
    - `.gitignore` contains a `.dev.vars` line and `git check-ignore .dev.vars` exits 0 (the file is now ignored)
    - `grep -c "lin_api_\|LINEAR_API_KEY" wrangler.toml` returns 0 (no secret committed)
    - `pnpm typecheck` exits 0 (the probe handler from 03-01 typechecks under the new project)
  </acceptance_criteria>
  <done>functions/ is typechecked with Worker types, vitest discovers functions tests, the live-preview script runs on a clean machine via npx, .dev.vars is gitignored, and no secret is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency supply chain | New npm package enters the build |
| Repo ↔ secret store | wrangler.toml and .dev.vars must never carry the token into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `@cloudflare/workers-types` install | mitigate | Blocking human-verify legitimacy checkpoint precedes install (slopcheck unavailable → manual npmjs.com verification of official @cloudflare scope, downloads, no postinstall). |
| T-03-03 | Information Disclosure | wrangler.toml | mitigate | Grep gate asserts no `lin_api_`/`LINEAR_API_KEY` in wrangler.toml; secret stays in Pages binding only. |
| T-03-04 | Tampering | silently-skipped handler tests | mitigate | vitest include glob adds `functions/**/*.test.ts`; 03-03 verifies the test count is non-zero. |
| T-03-17 | Information Disclosure | local `.dev.vars` token committed | mitigate | `.dev.vars` added to .gitignore (was NOT ignored); `git check-ignore .dev.vars` gate confirms it before any live-test workflow uses the real key. |
</threat_model>

<verification>
- `pnpm typecheck` green with the new functions project.
- vitest `include` covers functions tests (proven non-zero in 03-03).
- No token in wrangler.toml (grep gate); `.dev.vars` ignored (`git check-ignore` gate).
- `preview:functions` invokes `npx --yes wrangler@4` (clean-machine safe).
</verification>

<success_criteria>
- Worker types installed (legitimacy-gated), functions typechecked with no `any`, functions tests discoverable, clean-machine live-preview script present, `.dev.vars` gitignored, no secret committed.
</success_criteria>

<output>
Create `.planning/phases/03/03-02-SUMMARY.md` when done. Record the resolved @cloudflare/workers-types version and whether wrangler.toml needed any change.
</output>
