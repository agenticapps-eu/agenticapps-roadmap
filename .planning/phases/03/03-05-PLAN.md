---
phase: 03-linear-proxy
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - docs/access-setup.md
  - .planning/phases/03/03-ACCESS-PROOF.md
autonomous: false
requirements: [REQ-PROXY-1, REQ-PROXY-4]
must_haves:
  truths:
    - "A runbook documents setting the LINEAR_API_KEY Pages secret binding (never committed)"
    - "A runbook documents the Cloudflare Access email allow-list over BOTH the Pages project AND /api/*"
    - "The optional dashboard rate-limit rule for /api/* is documented as optional"
    - "Captured evidence proves an unauthenticated request to /api/linear/snapshot is BLOCKED by Access and an allowed identity SUCCEEDS — this is a blocking completion gate, not a doc instruction"
  artifacts:
    - path: "docs/access-setup.md"
      provides: "Console-only setup runbook: binding, Access policy, optional rate-limit rule"
      contains: "/api/*"
      min_lines: 25
    - path: ".planning/phases/03/03-ACCESS-PROOF.md"
      provides: "Captured proof of the Access enforcement check (unauth blocked + allowed succeeds) — the phase-completion artifact"
      contains: "/api/linear/snapshot"
  key_links:
    - from: "docs/access-setup.md"
      to: "functions/api/linear/[[path]].ts"
      via: "documents the LINEAR_API_KEY binding the handler reads via context.env"
      pattern: "LINEAR_API_KEY"
    - from: ".planning/phases/03/03-ACCESS-PROOF.md"
      to: "Cloudflare Access policy over /api/*"
      via: "records the captured unauth-blocked + allowed-succeeds evidence"
      pattern: "Access"
---

<objective>
Write the console-only runbook for the out-of-band setup the live path depends on, AND turn "unauthenticated requests are blocked by Access" from a documented intention into a captured, blocking completion artifact.

Purpose: "Done when #2" (unauthenticated requests blocked by Access) is configured in the Cloudflare console by the user, not in code. Both cross-AI reviewers agreed this must be a HARD completion gate with captured proof, not verification-by-document. This plan makes the console steps reproducible (explicitly covering `/api/*`) AND requires recorded evidence that the policy is actually live: an unauthenticated `/api/linear/snapshot` is blocked and an allowed identity succeeds.
Output: docs/access-setup.md (the runbook) and .planning/phases/03/03-ACCESS-PROOF.md (the captured evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-RESEARCH.md
@docs/superpowers/specs/2026-06-28-linear-proxy-access-design.md

<interfaces>
<!-- From the spec §D and research Security Domain. -->
- The handler reads the secret as `context.env.LINEAR_API_KEY` (a Pages secret binding) — NOT process.env. Same secret NAME as the existing CI/GitHub Actions secret; do not rename.
- Access must gate the Pages project AND `/api/*` (Spoofing mitigation — unauth access to /api/*).
- Optional: a Cloudflare dashboard rate-limit rule on /api/* (defense-in-depth; the spec marks it optional; the Function already has a per-isolate limiter).
- .dev.vars (local secret file) is gitignored by 03-02 — reference that, do not re-add it here.
- Access enforcement only applies on the DEPLOYED Pages app (the *.pages.dev or custom domain), NOT under local `wrangler pages dev`. The proof check therefore runs against the deployed URL.
- Live smoke commands (from VALIDATION Manual-Only): `pnpm build && npx --yes wrangler@4 pages dev dist`; 200 from /api/linear/snapshot, 404 from /api/linear/nope (local handler behavior, separate from the Access edge check).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/access-setup.md runbook</name>
  <read_first>
    - docs/superpowers/specs/2026-06-28-linear-proxy-access-design.md §D "Cloudflare Access + docs"
    - .planning/phases/03/03-RESEARCH.md "Security Domain" + "Runtime State Inventory" (Live service config row)
    - .planning/phases/03/03-VALIDATION.md "Manual-Only Verifications"
    - docs/architecture.md (match the existing docs tone/structure)
  </read_first>
  <action>
    Create docs/access-setup.md as a console runbook with three sections: (1) Set the LINEAR_API_KEY Pages secret binding — where in the Cloudflare dashboard (Pages project → Settings → Environment variables/bindings, encrypted), that it is the SAME name the handler reads via context.env.LINEAR_API_KEY and the same name as the existing CI secret, and an explicit warning that it must NEVER be committed or placed in wrangler.toml/roadmap.json/the bundle (reference the `lin_api_` grep gate). For local live testing, document the gitignored `.dev.vars` file (note that .wrangler/ and .dev.vars are already gitignored by 03-02 — do not re-add). (2) Create the Cloudflare Access policy — an email allow-list (named allow-list per the spec) that explicitly covers BOTH the Pages project domain AND the `/api/*` path, with a note that omitting /api/* leaves the proxy open (the Spoofing threat). (3) Optional dashboard rate-limit rule on /api/* — marked optional, defense-in-depth on top of the Function's per-isolate limiter. End with a "Verify" section: the wrangler pages dev local smoke (200 from /api/linear/snapshot, 404 from /api/linear/nope) AND the DEPLOYED Access check (unauthenticated request to the deployed /api/linear/snapshot → Access challenge/403; an allowed identity → 200) — and point the reader to record that deployed check in .planning/phases/03/03-ACCESS-PROOF.md (Task 2). Do not include any real secret value or domain-specific tokens. Match the heading style of docs/architecture.md.
  </action>
  <verify>
    <automated>test -f docs/access-setup.md && grep -q "/api/\*" docs/access-setup.md && grep -q "LINEAR_API_KEY" docs/access-setup.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/access-setup.md exists
    - Contains a section on the LINEAR_API_KEY Pages secret binding with a never-commit warning
    - Contains the Access allow-list covering BOTH the project AND `/api/*`
    - Contains the optional rate-limit rule marked optional
    - Contains a Verify section with the local 200/404 smoke AND the deployed unauth-vs-allowed Access check, pointing to 03-ACCESS-PROOF.md
    - `grep -c "lin_api_[A-Za-z0-9]" docs/access-setup.md` returns 0 (no real token value)
  </acceptance_criteria>
  <done>The runbook documents binding + Access (project + /api/*) + optional rate limit + verification, with no committed secret.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Access enforcement proof gate. The runbook (Task 1) describes how to configure Cloudflare Access over the Pages project AND /api/*. This checkpoint turns "Done when #2" into CAPTURED EVIDENCE: it requires you to perform the console setup (out-of-band — Cloudflare Access policies are configured in the dashboard, there is no CLI for the org's Access app), then prove the policy is live against the DEPLOYED app and record the result. Claude has created/updated docs/access-setup.md with the exact steps; this gate captures the proof.</what-built>
  <how-to-verify>
    Perform these against the DEPLOYED Pages app (Access does NOT apply under local `wrangler pages dev`):
    1. In the Cloudflare dashboard, configure the Access email allow-list covering BOTH the Pages project domain AND the `/api/*` path, per docs/access-setup.md §2.
    2. UNAUTHENTICATED check: from a context with NO Access session (e.g. `curl -sS -o /dev/null -w "%{http_code}\n" https://<deployed-domain>/api/linear/snapshot`, or a private browser window with no login). EXPECT an Access challenge / redirect / 403 — NOT a 200 and NOT any Linear data. Capture the status code and note that no body data was returned.
    3. ALLOWED-IDENTITY check: authenticate as an allow-listed email, then request the same `/api/linear/snapshot`. EXPECT 200 with schema-valid RoadmapJson (and confirm the LINEAR_API_KEY value appears in NO response/header/log).
    4. Record BOTH outcomes in .planning/phases/03/03-ACCESS-PROOF.md: the date, the deployed domain (redact if sensitive), the unauthenticated status code (blocked), the allowed-identity status code (200), and an explicit statement that the unauthenticated request returned no Linear data and no token. This file is the phase-completion artifact for "Done when #2".
    Phase 03 is NOT complete until 03-ACCESS-PROOF.md records a BLOCKED unauthenticated result and a SUCCESSFUL allowed-identity result.
  </how-to-verify>
  <resume-signal>Type "approved" once 03-ACCESS-PROOF.md records the blocked-unauth + allowed-success evidence, or describe what is not yet blocking.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge → /api/* | Cloudflare Access gates reachability before the Function runs |
| operator → secret store | Binding set in the console, never in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing | unauth access to /api/* | mitigate | Runbook mandates the Access allow-list explicitly cover /api/*, not just the project root; the blocking proof gate captures evidence that an unauthenticated /api/linear/snapshot is actually blocked on the deployed app (no longer doc-only). |
| T-03-14 | Information Disclosure | secret committed to git | mitigate | Runbook documents binding/.dev.vars only and warns against wrangler.toml/bundle; reinforces the `lin_api_` grep gate. |
| T-03-15 | Denial of Service | Linear abuse | accept | Optional dashboard rate-limit documented; per-isolate limiter already shipped in 03-03; Access keeps the audience tiny. |
| T-03-19 | Spoofing | Access misconfig shipped silently (policy not actually applied) | mitigate | Blocking human-verify gate requires CAPTURED proof (03-ACCESS-PROOF.md) of unauth-blocked + allowed-success before the phase can be marked complete. |
| T-03-SC | Tampering | npm/pnpm installs | accept | No installs in this plan (docs only). |
</threat_model>

<verification>
- docs/access-setup.md exists with the binding, Access-over-/api/*, optional rate-limit, and verify sections; no real secret value (grep gate).
- .planning/phases/03/03-ACCESS-PROOF.md records a blocked unauthenticated /api/linear/snapshot AND a successful allowed identity (blocking completion artifact for Done-when #2).
</verification>

<success_criteria>
- The out-of-band setup for "Done when #2" is reproducible from the runbook, AND the proxy path is proven gated by Access with captured evidence (unauth blocked + allowed succeeds), not merely documented.
</success_criteria>

<output>
Create `.planning/phases/03/03-05-SUMMARY.md` when done. The SUMMARY must reference 03-ACCESS-PROOF.md as the captured completion artifact.
</output>
