---
phase: 03-linear-proxy
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/lib/roadmap/loader.ts
  - src/pages/OverviewPage.tsx
  - src/pages/TimelinePage.tsx
  - src/components/AppHeader.tsx
autonomous: false
requirements: [REQ-LOADER]
must_haves:
  truths:
    - "With ?source=live the loader fetches /api/linear/snapshot and validates it with RoadmapJsonSchema"
    - "ANY live failure falls back to /roadmap.json and surfaces a 'live unavailable' notice — it never throws"
    - "A genuine snapshot failure still throws a Response so RoadmapError renders (error boundary intact)"
    - "The header toggle flips ?source between snapshot and live and triggers loader revalidation"
    - "Snapshot remains the default data path (no ?source → snapshot, zero Linear calls)"
  artifacts:
    - path: "src/lib/roadmap/loader.ts"
      provides: "Source-selecting loader returning { data, live, liveUnavailable }"
      contains: "source"
    - path: "src/components/AppHeader.tsx"
      provides: "Snapshot/Live toggle via useSearchParams + live-unavailable notice"
      contains: "useSearchParams"
    - path: "src/pages/OverviewPage.tsx"
      provides: "Reads data off the new loader wrapper"
      contains: "data"
    - path: "src/pages/TimelinePage.tsx"
      provides: "Reads data off the new loader wrapper"
      contains: "data"
  key_links:
    - from: "src/lib/roadmap/loader.ts"
      to: "/api/linear/snapshot"
      via: "fetch on ?source=live"
      pattern: "api/linear/snapshot"
    - from: "src/components/AppHeader.tsx"
      to: "loader"
      via: "useSearchParams flips ?source (RR7 auto-revalidates)"
      pattern: "useSearchParams"
    - from: "src/pages/OverviewPage.tsx"
      to: "loader"
      via: "useRouteLoaderData reads .data off the wrapper"
      pattern: "\\.data"
---

<objective>
Add the client live-data path and toggle: a source-selecting loader with snapshot fallback, the two consumer-page updates the new return shape requires, and a header toggle that flips `?source` and shows a "live unavailable" notice.

Purpose: This completes "Done when #1" from the user's side — live mode fetches through /api/linear with snapshot as the default and a graceful fallback on any failure. Because the loader's return shape changes from a bare RoadmapJson to `{ data, live, liveUnavailable }`, the two consumer pages are in-scope BREAKING edits (Pitfall 4).
Output: updated loader, OverviewPage, TimelinePage, and AppHeader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03/03-RESEARCH.md
@docs/superpowers/specs/2026-06-28-linear-proxy-access-design.md

<interfaces>
<!-- Extracted from the codebase. -->

From src/lib/roadmap/loader.ts (current):
- `export async function roadmapLoader(): Promise<RoadmapJson>` — fetches "/roadmap.json", throws `new Response(...)` on !ok (status res.status) and on malformed (status 500), returns `parsed.data`. RR7 passes `{ request, params }`; the current signature ignores them.

From src/router.tsx:
- The loader is wired to the route with `id: "root"`. Pages read it via `useRouteLoaderData("root")`.

From src/pages/OverviewPage.tsx (line 4-5):
- `const data = useRouteLoaderData("root") as RoadmapJson;` then reads `data.initiatives`, `data.projects`.

From src/pages/TimelinePage.tsx (line 4-5):
- `const data = useRouteLoaderData("root") as RoadmapJson;` then reads `data.projects` (filter/sort by targetDate).

From src/components/AppHeader.tsx (lines 35-44):
- An EXISTING disabled "Connect" button in the right-side slot (`<div className="ml-auto">`). This is the toggle mount point — enable/replace it, do not add new layout.

From src/lib/roadmap/schema.ts:
- `RoadmapJsonSchema` (same schema validates the live response), `type RoadmapJson`.

RR7 behavior (research Pattern 5, verified): a navigation that changes search params revalidates loaders by default — no useRevalidator/revalidate() needed. `LoaderFunctionArgs` has `request: Request`; read params via `new URL(request.url).searchParams`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-selecting loader with fallback + consumer-page updates</name>
  <read_first>
    - src/lib/roadmap/loader.ts (current implementation — full)
    - src/pages/OverviewPage.tsx and src/pages/TimelinePage.tsx (the two consumers)
    - src/router.tsx (confirm route id "root" and that errorElement is RoadmapError)
    - .planning/phases/03/03-RESEARCH.md "Pattern 4" + "Pitfall 4"
  </read_first>
  <action>
    Change roadmapLoader to accept `{ request }: LoaderFunctionArgs` and read `const wantLive = new URL(request.url).searchParams.get("source") === "live"`. When wantLive: try `fetch("/api/linear/snapshot")`; on `!res.ok` or a failed `RoadmapJsonSchema.safeParse`, fall through (do NOT throw); on success return `{ data: parsed.data, live: true, liveUnavailable: false }`. The snapshot path (existing logic) stays: `fetch("/roadmap.json")`, `throw new Response("Failed to load roadmap snapshot", { status: res.status })` on !ok, `throw new Response("Roadmap snapshot is malformed", { status: 500 })` on malformed — these THROWS stay so a genuine outage hits RoadmapError. Return `{ data: parsed.data, live: false, liveUnavailable: wantLive }` from the snapshot path (liveUnavailable true only when live was attempted and fell back). Export a `RoadmapLoaderData` type for the wrapper. Update BOTH consumers in the SAME change: in OverviewPage.tsx and TimelinePage.tsx replace `useRouteLoaderData("root") as RoadmapJson` with reading the wrapper (`as RoadmapLoaderData`) and destructure `data` off it; drop the unsafe bare-RoadmapJson cast. Do not change the rendering logic below the data read.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/roadmap/loader.ts reads `searchParams.get("source")` and fetches "/api/linear/snapshot" on live
    - The live branch NEVER throws (failures fall through to the snapshot path)
    - The snapshot path still `throw new Response(...)` on !ok and on malformed (error boundary intact)
    - loader returns an object with `data`, `live`, `liveUnavailable`
    - OverviewPage.tsx and TimelinePage.tsx read `data` off the wrapper (no `as RoadmapJson` bare cast remains)
    - `pnpm typecheck` exits 0; `pnpm test` exits 0
  </acceptance_criteria>
  <done>Loader selects source with safe fallback; both pages consume the new wrapper; types and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Header Snapshot/Live toggle + live-unavailable notice</name>
  <read_first>
    - src/components/AppHeader.tsx (the disabled Connect button slot, lines 35-44)
    - src/lib/roadmap/loader.ts (RoadmapLoaderData wrapper from Task 1, for the notice)
    - .planning/phases/03/03-RESEARCH.md "Pattern 5: Header toggle via search params"
  </read_first>
  <action>
    In AppHeader.tsx replace the disabled Connect button with an enabled toggle: `const [params, setParams] = useSearchParams(); const live = params.get("source") === "live";` and an onClick that calls `setParams(prev => { prev.set("source", live ? "snapshot" : "live"); return prev; })`. Label it "Live" / "Snapshot" reflecting current state (and a clear aria-label). Read the loader's `liveUnavailable` via `useRouteLoaderData("root")` (typed as RoadmapLoaderData) and, when true, render a small inline "live unavailable — showing snapshot" notice in the header (muted text). Do NOT call useRevalidator or revalidate() — RR7 revalidates on the search-param change automatically. Optionally reflect `useNavigation().state === "loading"` as a pending style on the toggle (polish, not required). Keep all existing header layout and nav untouched (Surgical Changes).
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/AppHeader.tsx contains `useSearchParams` and an onClick that sets `source`
    - The button is no longer `disabled`
    - AppHeader renders a "live unavailable" notice gated on `liveUnavailable`
    - `grep -c "useRevalidator\|revalidate()" src/components/AppHeader.tsx` returns 0
    - `pnpm build` exits 0; `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>A working Snapshot/Live toggle flips ?source and shows the fallback notice; the app builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full live path end-to-end: loader source selection, snapshot fallback, and the header toggle. Pages Functions only run under wrangler pages dev (not vite dev), so this is the live smoke verification.</what-built>
  <how-to-verify>
    1. Run `pnpm build && npx --yes wrangler@4 pages dev dist --port 8788` (set LINEAR_API_KEY in the local env or a `.dev.vars` file for this smoke — never commit it).
    2. Open http://127.0.0.1:8788/ — confirm it renders the snapshot (default, no ?source).
    3. Click the toggle to Live (URL gains ?source=live) — confirm the board still renders; check the network tab shows a 200 from /api/linear/snapshot and that the LINEAR_API_KEY value appears in NO response or log line.
    4. Direct check: `curl -i http://127.0.0.1:8788/api/linear/snapshot` returns 200; `curl -i http://127.0.0.1:8788/api/linear/nope` returns 404.
    5. Stop the Functions process and reload with ?source=live — confirm the "live unavailable — showing snapshot" notice appears and the snapshot still renders (fallback works).
    6. Also confirm `pnpm dev` (plain vite) renders the snapshot cleanly with ?source=live (fallback is the local default).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/linear/snapshot | Live fetch crosses to the proxy (gated by Access at the edge) |
| live response → UI | Already leak-gated server-side; client re-validates shape only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | malformed/hostile live response | mitigate | Client re-validates with the SAME RoadmapJsonSchema; any mismatch → fall back to snapshot, never render unvalidated data (REQ-LOADER, ASVS V5). |
| T-03-11 | Denial of Service | live failure crashing the app | mitigate | Live branch never throws; falls back to snapshot + notice. Genuine snapshot outage still hits RoadmapError. |
| T-03-12 | Information Disclosure | token reaching the client | mitigate | Loader fetches the proxy, never Linear directly; no token in the bundle (re-confirmed by the phase-gate `lin_api_` grep). |
| T-03-SC | Tampering | npm/pnpm installs | accept | No installs in this plan. |
</threat_model>

<verification>
- `pnpm test && pnpm typecheck && pnpm lint && pnpm build` green.
- Live smoke (checkpoint): 200 from /api/linear/snapshot, 404 from /api/linear/nope, fallback notice on Functions-down, no token in any response/log.
</verification>

<success_criteria>
- Snapshot is the default; live mode works through /api/linear; any live failure falls back to the snapshot with a visible notice; the token never reaches the client.
</success_criteria>

<output>
Create `.planning/phases/03/03-04-SUMMARY.md` when done.
</output>
