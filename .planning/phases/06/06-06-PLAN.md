---
phase: 06
plan: 06
type: execute
wave: 4
depends_on: [06-01, 06-03, 06-04, 06-05]
files_modified: [scripts/sync-gsd-linear/apply.ts, scripts/sync-gsd-linear/apply.test.ts]
autonomous: true
requirements: [SYNC-04]
must_haves:
  truths:
    - "--dry-run performs zero mutation calls (no create/update fetch is ever issued)"
    - "Apply upserts project, milestones, and issues resolve-before-create for one project, writing ids back to linear-map.json"
    - "A second apply against the now-populated map/workspace is a no-op (idempotent)"
    - "The planAhead patch keeps public/roadmap.json schema-valid and token-free via the existing gate"
  artifacts:
    - path: "scripts/sync-gsd-linear/apply.ts"
      provides: "upsert orchestration + linear-map.json write-back + gated planAhead patch"
      contains: "export async function applyProject"
  key_links:
    - from: "scripts/sync-gsd-linear/apply.ts"
      to: "scripts/linear/transform.ts"
      via: "reuses assertNoLeak + RoadmapJsonSchema.parse on the patched roadmap.json"
      pattern: "assertNoLeak"
---

<objective>
Build the SYNC-04 write engine: apply.ts upserts one project's Linear records
(resolve-before-create for every entity), writes resolved ids back to linear-map.json, and
emits the planAhead flag into public/roadmap.json through the existing leak/schema gate.
Dry-run must issue zero mutations; a re-run must be a no-op. This plan is autonomous logic
+ mocked tests; the CLI wiring and live human-verify are 06-07.

Output: apply.ts (+test: dry-run, idempotent, planAhead — all against mocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
From scripts/linear/transform.ts: assertNoLeak(serialized: string): void (throws on token/email). From src/lib/roadmap/schema.ts: RoadmapJsonSchema (parse), Project has planAhead?: boolean nullish. From scripts/sync-gsd-linear/mutations.ts (06-03): PROJECT_CREATE, PROJECT_MILESTONE_CREATE, ISSUE_CREATE, PROJECT_LABEL_CREATE, ISSUE_LABEL_CREATE, INITIATIVE_TO_PROJECT_CREATE + payloads. From resolve.ts (06-05): buildResolvedWorkspace, resolveMilestone, resolveIssue. From config.ts: LinearMap, NormalizedModel, ResolvedWorkspace. From diff.ts (06-04): buildDiff. client.ts pattern: read process.env["LINEAR_API_KEY"] once with a fail-fast message; write with writeFileSync(path, JSON.stringify(data, null, 2)).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apply.ts — upsert orchestration + linear-map.json write-back</name>
  <files>scripts/sync-gsd-linear/apply.ts, scripts/sync-gsd-linear/apply.test.ts</files>
  <read_first>
    - scripts/linear/client.ts (Node-only process.env boundary + fail-fast message)
    - scripts/sync-snapshot.ts (thin writeFileSync + one-line console summary)
    - scripts/sync-gsd-linear/mutations.ts + resolve.ts (create mutations + resolvers)
    - .planning/phases/06/06-RESEARCH.md (Pattern 2 resolve-then-create; Pitfall 4 team required; idempotent apply pseudocode)
    - scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts (drives dry-run + idempotency tests)
  </read_first>
  <behavior>
    - applyProject(deps, model, resolved, map, {dryRun}) with dryRun=true issues zero create/update mutation fetches and returns the planned diff.
    - With dryRun=false it resolves-then-creates project (ensuring team + both labels + optional initiative join), then each milestone (with proposedDate), then each issue (bucketed into its phase milestone, description = the plan's task checklist), writing every new id into the LinearMap pools.
    - A second applyProject against the now-populated map/mutation-mock creates nothing (idempotent) — buildDiff over the re-resolved state is empty.
  </behavior>
  <action>
    Create apply.ts. Export async applyProject(deps: {fetchFn: typeof fetch; endpoint: string; auth: string}, model: NormalizedModel, map: LinearMap, opts: {dryRun: boolean}): Promise<DiffSummary>. Flow: buildResolvedWorkspace -> buildDiff -> if opts.dryRun return the diff without any mutation fetch. Else, in resolve-before-create order per RESEARCH Pattern 2: resolveTeam (fail-fast if missing), ensure ProjectLabel + IssueLabel (create via PROJECT_LABEL_CREATE/ISSUE_LABEL_CREATE only if resolve missed), ensure Project (PROJECT_CREATE with teamIds + projectLabel), attach initiative via INITIATIVE_TO_PROJECT_CREATE when entry.initiative set, ensure each milestone (PROJECT_MILESTONE_CREATE with targetDate), ensure each issue (ISSUE_CREATE with teamId+projectId+projectMilestoneId+issueLabel, description built from the plan's task lines). After each create, write the new id into the corresponding LinearMap pool. Export writeLinearMap(path, map) using writeFileSync(JSON.stringify(map,null,2)). Read LINEAR_API_KEY only at the Node boundary in the caller (06-07) — applyProject receives auth via deps. Write apply.test.ts (test-first) with the mutation-mock: "dry-run" asserts the mock's create handlers were never called; "idempotent" runs applyProject twice and asserts the second run creates nothing and buildDiff is empty; assert ids land in the map after the first run.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "dry-run" && CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "idempotent"</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "dry-run" exits 0 and asserts zero mutation fetches
    - CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "idempotent" exits 0 and asserts second run creates nothing
    - grep -c 'export async function applyProject' scripts/sync-gsd-linear/apply.ts returns 1
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>applyProject upserts one project resolve-before-create, writes ids back, and re-runs as a no-op; dry-run issues zero mutations; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply.ts — gated planAhead patch into roadmap.json</name>
  <files>scripts/sync-gsd-linear/apply.ts, scripts/sync-gsd-linear/apply.test.ts</files>
  <read_first>
    - scripts/linear/transform.ts (assertNoLeak + RoadmapJsonSchema.parse gate — import, do NOT reimplement)
    - src/lib/roadmap/schema.ts (planAhead?: boolean already on ProjectSchema line 20)
    - .planning/phases/06/06-CONTEXT.md (D-06-09 planAhead emission: gated to apply / --write-snapshot, never plain dry-run, token-free)
    - .planning/phases/06/06-PATTERNS.md section "Leak/schema gate" (must reuse verbatim)
  </read_first>
  <behavior>
    - patchPlanAhead(roadmapPath, repoProjectName, planAhead) merges the boolean into the matching project in roadmap.json, runs assertNoLeak on the serialized result, then RoadmapJsonSchema.parse, then writeFileSync.
    - The patch is invoked only on a real apply or an explicit --write-snapshot, never during a plain dry-run.
    - A serialized object containing a token/email throws before writing.
  </behavior>
  <action>
    Extend apply.ts with patchPlanAhead(roadmapPath: string, projectName: string, planAhead: boolean): void: readFileSync roadmap.json, JSON.parse, set planAhead on the project whose name matches projectName (leave others untouched), assertNoLeak(JSON.stringify(patched)) (imported from scripts/linear/transform.ts), RoadmapJsonSchema.parse(patched), then writeFileSync(roadmapPath, JSON.stringify(patched, null, 2)). Wire applyProject to call patchPlanAhead only when !opts.dryRun OR opts.writeSnapshot === true (add writeSnapshot?: boolean to opts). Extend apply.test.ts "planAhead" asserting: patched roadmap.json still passes RoadmapJsonSchema; a token-bearing object throws via assertNoLeak; plain dry-run does not call patchPlanAhead.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "planAhead"</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/apply.test.ts -t "planAhead" exits 0
    - grep -c 'assertNoLeak\|RoadmapJsonSchema' scripts/sync-gsd-linear/apply.ts returns >= 2 (both reused, not reimplemented)
    - Test proves a token/email in the serialized object throws before write
    - Test proves plain dry-run does not patch roadmap.json
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>planAhead patch reuses the audited gate, is gated to apply/--write-snapshot, and keeps roadmap.json schema-valid + token-free; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| untrusted slugs/headings -> write mutations | Values passed as GraphQL variables in create mutations |
| CLI -> public/roadmap.json (app data contract) | The only write into the app's token-free data path |
| LINEAR_API_KEY -> outbound header / written files | Must never reach linear-map.json or roadmap.json |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | create mutations | mitigate | All titles/descriptions/slugs passed via GraphQL variables (mutations.ts documents), never interpolated |
| T-06-02 | Information Disclosure | roadmap.json / linear-map.json writes | mitigate | patchPlanAhead runs assertNoLeak (token + email + live-key check) before writeFileSync; auth never logged |
| T-06-05 | Tampering | duplicate Linear records | mitigate | applyProject resolves-before-create for every entity; idempotency proven against the mutation-mock |
| T-06-SC | Tampering | package installs | accept | No packages this phase |
</threat_model>

<success_criteria>
- SYNC-04 core satisfied: dry-run zero-mutation, idempotent apply, ids persisted, gated token-free planAhead patch.
- assertNoLeak + RoadmapJsonSchema reused verbatim (not reimplemented).
- apply.test.ts green for dry-run, idempotent, planAhead; typecheck clean.
</success_criteria>

<output>
Create `.planning/phases/06/06-06-SUMMARY.md` when done.
</output>
