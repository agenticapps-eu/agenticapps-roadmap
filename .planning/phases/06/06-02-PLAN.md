---
phase: 06
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [scripts/sync-gsd-linear/walker.ts, scripts/sync-gsd-linear/walker.test.ts, scripts/sync-gsd-linear/parser.ts, scripts/sync-gsd-linear/parser.test.ts]
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "The walker enumerates every phases/* directory from disk, including duplicate leading-number and decimal-numbered dirs"
    - "The parser produces a normalized { repo, phases[], plans[] } model whether or not PLAN.md has frontmatter or a descriptive H1"
    - "Phase identity is the full directory slug, never the bare phase number"
    - "Each plan carries a stable identity key (repo/phaseSlug/relativePlanPath) distinct from its display title, so multiple generic-heading plans in one phase never collapse to the same identity"
    - "Each plan carries taskLines (its task/checklist lines) for the apply engine's issue description"
  artifacts:
    - path: "scripts/sync-gsd-linear/walker.ts"
      provides: "phases/* directory enumeration keyed on full dir slug"
      contains: "export function walkPlanning"
    - path: "scripts/sync-gsd-linear/parser.ts"
      provides: "phase dirs + PLAN.md files -> NormalizedModel (with key + taskLines per plan)"
      contains: "export function parseRepo"
  key_links:
    - from: "scripts/sync-gsd-linear/parser.ts"
      to: "scripts/sync-gsd-linear/config.ts"
      via: "NormalizedModelSchema.parse at the return boundary"
      pattern: "NormalizedModelSchema\\.parse"
---

<objective>
Build SYNC-01: the filesystem walker and the PLAN.md parser that together turn a sibling
repo's .planning/ tree into the normalized model config.ts declares. Correctly handle the
three real-repo quirks RESEARCH found: duplicate leading-number dirs, decimal phases, and
frontmatter-optional / generic-H1 plans. Per the cross-AI review, each plan now carries a
stable identity key (never the display title) and its taskLines (for the issue body).
ROADMAP.md/STATE.md are read only to enrich completion status, never to discover phases.

Output: walker.ts, parser.ts, and their tests (test-first against the planning-trees fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
From scripts/sync-gsd-linear/config.ts (06-01): NormalizedModel, NormalizedPhase {slug, number, completed, plans[], proposedDate?}, NormalizedPlan {file, title, key, taskLines}, NormalizedModelSchema. `key` is the stable identity path repo/phaseSlug/relativePlanPath used downstream for title-hashing; `title` is display-only; `taskLines` is the plan's task/checklist lines. Fixtures live at scripts/sync-gsd-linear/__fixtures__/planning-trees/.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: walker.ts — enumerate phases/* keyed on full dir slug</name>
  <files>scripts/sync-gsd-linear/walker.ts, scripts/sync-gsd-linear/walker.test.ts</files>
  <read_first>
    - scripts/sync-gsd-linear/__fixtures__/planning-trees/ (from 06-01 — the fixtures under test)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 1 duplicate-NN + decimal; Anti-Patterns "Trusting ROADMAP.md")
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" walker.ts (use node:fs readdirSync/readFileSync directly)
    - .planning/phases/06/06-RESEARCH.md Environment Availability (missing repo path -> skip with warning, do not crash)
  </read_first>
  <behavior>
    - walkPlanning(planningDir) lists every subdirectory of phases/ as a RawPhaseDir with its full slug + absolute path + the PLAN.md file paths inside it; two dirs sharing leading number "01" both appear as distinct entries.
    - Decimal-numbered dirs (03.5, 04.10, 04.2) are all enumerated; ordering is deferred to dates.ts (walker does not sort numerically).
    - A missing/absent phases/ dir or repoPath returns an empty result with a console.warn, never throws.
  </behavior>
  <action>
    Create walker.ts exporting walkPlanning(planningDir: string): RawPhaseDir[] and an exported RawPhaseDir interface {slug: string; dir: string; planFiles: string[]; roadmapPath: string | null; statePath: string | null}. Use node:fs readdirSync({withFileTypes:true}) over phases/, keep only directories, capture each dir's full name as slug (never split off the number). For each phase dir collect files matching /PLAN\.md$/ (both NN-MM-PLAN.md and bare PLAN.md) into planFiles. Detect optional .planning/ROADMAP.md and STATE.md paths for the parser's completion enrichment. If phases/ does not exist, console.warn(`No phases/ under ${planningDir}, skipping`) and return []. Write walker.test.ts test-first against the planning-trees fixtures asserting duplicate-NN both present, decimal dirs present, bare-PLAN dir has one planFile, and missing-dir returns [].
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/walker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/walker.test.ts exits 0
    - Test asserts both 01-go-routing and 01-gsd-bug-fixes appear as distinct slugs
    - grep -c 'export function walkPlanning' scripts/sync-gsd-linear/walker.ts returns 1
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>walker.ts enumerates every phase dir by full slug, tolerates missing dirs, and is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parser.ts — phase dirs + PLAN.md -> NormalizedModel (with key + taskLines)</name>
  <files>scripts/sync-gsd-linear/parser.ts, scripts/sync-gsd-linear/parser.test.ts</files>
  <read_first>
    - scripts/linear/transform.ts (explicit allow-list .map() + pure-helper-per-concern e.g. bucketFor; final Schema.parse at boundary)
    - scripts/sync-gsd-linear/config.ts (NormalizedModelSchema, NormalizedPhase, NormalizedPlan {file, title, key, taskLines})
    - scripts/sync-gsd-linear/walker.ts (RawPhaseDir input from Task 1)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 3 layered completion heuristic; Anti-Pattern "generic-H1 denylist"; Pitfall 1 component-wise number token)
    - .planning/phases/06/06-REVIEWS.md Consensus item 1 (separate display title from identity; carry plan body)
  </read_first>
  <behavior>
    - parseRepo(rawDirs, meta) yields a NormalizedModel whose phases[] preserve the full slug as identity and store number as the leading dot-separated numeric token string ("03.5", "04.10").
    - Every plan carries a stable identity `key = ${meta.repo}/${phaseSlug}/${relativePlanPath}` (the plan file's path relative to .planning/), which is what downstream hashing/dedup uses — NEVER the display title. This guarantees two generic-heading NN-MM-PLAN.md files in one phase get distinct identities.
    - Display `title`: use the PLAN.md H1 unless it is frontmatter-less AND matches a generic-heading denylist (e.g. /^Phase \d+/) — in which case fall back to the PLAN.md FILENAME (e.g. "09-02-PLAN") for a NN-MM-PLAN.md, and only to the directory slug for a single bare PLAN.md phase.
    - Every plan carries `taskLines`: the task/checklist lines extracted from the PLAN.md body (e.g. lines beginning with `- [ ]`/`- [x]`/`- ` or a `## Tasks` block), used by the apply engine for the issue description.
    - completionStatusFor(phaseDir) applies the layered heuristic: ROADMAP checkbox [x]/✅ -> completed; else VERIFICATION.md present -> completed; else every NN-MM-PLAN.md has a sibling NN-MM-SUMMARY.md -> completed; else in-progress. Bias to in-progress when unsure.
    - The returned model passes NormalizedModelSchema.parse.
  </behavior>
  <action>
    Create parser.ts exporting parseRepo(rawDirs: RawPhaseDir[], meta: {repo: string; projectName: string; teamKey: string; initiative?: string}): NormalizedModel. Import types from config.ts. For each RawPhaseDir map field-by-field (no spread) to a NormalizedPhase: slug from dir slug; number = leading numeric token extracted by regex (keep as dot-separated string, do NOT parseFloat); plans[] from planFiles read via readFileSync, each producing {file, title, key, taskLines}. Build `key` as `${meta.repo}/${phaseSlug}/${relativePlanPath}` (path relative to the repo's .planning/). Extract `title` via a titleFor(planFile, content, slug) pure helper implementing the H1 -> (generic denylist ? filename : H1) with the bare-PLAN.md -> slug special case. Extract `taskLines` via a small taskLinesFor(content): string[] helper (checklist/task lines from the body; empty array when none). completed via a small completionStatusFor helper implementing Pitfall 3's layered heuristic. End with `return NormalizedModelSchema.parse(result)`. Write parser.test.ts test-first against planning-trees fixtures asserting: frontmatter H1 used for title; a generic-H1 NN-MM-PLAN.md falls back to its FILENAME (not the slug); TWO generic-H1 plans in one phase get DISTINCT keys (no collision); bare PLAN.md yields one plan whose title is the slug; taskLines are populated from a plan with checklist lines and empty for a bodyless plan; completion heuristic classifies the VERIFICATION.md-bearing phase as completed and a plan-only phase as in-progress; number token kept as string.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/parser.test.ts exits 0
    - grep -c 'NormalizedModelSchema\.parse' scripts/sync-gsd-linear/parser.ts returns 1
    - Test proves a generic-H1 NN-MM-PLAN.md falls back to its filename for the title (not the slug)
    - Test proves two generic-H1 plans in one phase have distinct identity keys
    - Test proves taskLines are populated for a plan with checklist lines
    - Test proves the VERIFICATION.md phase is completed and a plan-only phase is in-progress
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>parser.ts produces a schema-valid NormalizedModel with slug phase-identity, per-plan identity key + taskLines, filename/slug title fallback, and the layered completion heuristic; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sibling `.planning/` Markdown/YAML -> parser | Untrusted plan headings/frontmatter/body read from other repos |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | parser output | mitigate | NormalizedModelSchema.parse validates the model at the boundary before any downstream stage consumes it |
| T-06-03 | Denial of Service | walker on malformed tree | mitigate | Missing phases/ or repoPath -> console.warn + empty result, never throw (Environment Availability fallback) |
| T-06-05 | Spoofing/duplication | plan identity | mitigate | Identity key is the relative plan path, not the display title, so distinct plans never collapse to one hash (SYNC-02 dedup integrity) |
| T-06-SC | Tampering | package installs | accept | No packages this phase |
</threat_model>

<success_criteria>
- SYNC-01 satisfied: walker + parser produce a normalized model handling all three real-repo quirks.
- Phase identity is the full directory slug; plan identity is the relative plan path; taskLines carried for the issue body.
- Both modules green and typecheck-clean.
</success_criteria>

<output>
Create `.planning/phases/06/06-02-SUMMARY.md` when done.
</output>
</content>
