---
phase: 06
plan: 07
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-04, 06-05, 06-06]
files_modified: [scripts/sync-gsd-linear/prompt.ts, scripts/sync-gsd-linear/cli.ts, scripts/sync-gsd-linear/cli.test.ts, scripts/sync-gsd-linear.ts, package.json]
autonomous: false
requirements: [SYNC-04]
must_haves:
  truths:
    - "pnpm sync:gsd defaults to dry-run and prints a per-project diff for one resolved --project"
    - "A write happens only after an interactive y/N approval (or --yes), always scoped to one resolved project — never a bulk write across repos"
    - "--anchor and --cadence flow into date proposal; --write-snapshot gates the planAhead patch"
    - "A real dry-run against claude-workflow produces a human-accurate diff"
  artifacts:
    - path: "scripts/sync-gsd-linear/cli.ts"
      provides: "parseArgs orchestration walker->parser->resolve->diff->dates->prompt->apply with single-project gate"
      contains: "parseArgs"
    - path: "scripts/sync-gsd-linear.ts"
      provides: "thin tsx entrypoint"
    - path: "package.json"
      provides: "sync:gsd script"
      contains: "sync:gsd"
  key_links:
    - from: "package.json"
      to: "scripts/sync-gsd-linear.ts"
      via: "sync:gsd script -> tsx entrypoint"
      pattern: "sync:gsd"
---

<objective>
Wire the SYNC-04 CLI: prompt.ts (the y/N gate), cli.ts (parseArgs + pipeline orchestration
with the hard single-project bulk-write guard), the thin entrypoint, and the package.json
script. Then verify the end-to-end manual items from VALIDATION.md: a real dry-run against
claude-workflow (human judges the diff) and a real apply + re-run no-op.

Output: prompt.ts, cli.ts (+test), scripts/sync-gsd-linear.ts, package.json sync:gsd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md
@.planning/phases/06/06-VALIDATION.md

<interfaces>
From scripts/sync-gsd-linear/config.ts: loadSyncConfig, loadLinearMap, SyncConfigEntry. From walker.ts/parser.ts (06-02): walkPlanning, parseRepo. From dates.ts (06-04): proposeDates. From diff.ts (06-04): buildDiff, renderDiff. From resolve.ts (06-05): buildResolvedWorkspace. From apply.ts (06-06): applyProject, writeLinearMap. Node builtins: node:util parseArgs, node:readline/promises. client.ts pattern: process.env["LINEAR_API_KEY"] read once with fail-fast message.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt.ts — node:readline y/N gate</name>
  <files>scripts/sync-gsd-linear/prompt.ts</files>
  <read_first>
    - .planning/phases/06/06-RESEARCH.md (Standard Stack: node:readline/promises createInterface().question())
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" prompt.ts (~10-line helper)
  </read_first>
  <action>
    Create prompt.ts exporting async confirm(question: string): Promise<boolean> using node:readline/promises createInterface({input: process.stdin, output: process.stdout}), asking question, returning true only for a "y"/"yes" (case-insensitive) answer, closing the interface in a finally. ~10 lines, no extra abstraction.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'readline/promises' scripts/sync-gsd-linear/prompt.ts returns 1
    - grep -c 'export async function confirm' scripts/sync-gsd-linear/prompt.ts returns 1
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>confirm() returns a boolean from an interactive y/N prompt; default-No behavior; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: cli.ts + entrypoint + package.json sync:gsd</name>
  <files>scripts/sync-gsd-linear/cli.ts, scripts/sync-gsd-linear/cli.test.ts, scripts/sync-gsd-linear.ts, package.json</files>
  <read_first>
    - scripts/sync-snapshot.ts (thin entrypoint style) + package.json line "sync:snapshot" (script wiring)
    - scripts/linear/client.ts (Node-only LINEAR_API_KEY fail-fast boundary)
    - .planning/phases/06/06-CONTEXT.md (D-06-07 approval flow, --dry-run default, --project, --yes; D-06-06 --anchor/--cadence; D-06-09 --write-snapshot)
    - .planning/phases/06/06-RESEARCH.md Security Domain (unintended bulk write -> hard single --project gate)
  </read_first>
  <behavior>
    - parseArgs supports --dry-run (default true), --project <name>, --yes, --anchor <date>, --cadence <weeks>, --write-snapshot.
    - runCli resolves exactly ONE config entry from --project; if --project is absent or matches zero/multiple entries it errors (no bulk write across repos).
    - Pipeline: loadSyncConfig+loadLinearMap -> walkPlanning+parseRepo -> proposeDates(anchor,cadence) -> buildResolvedWorkspace -> buildDiff -> renderDiff to stdout. Dry-run stops here. Otherwise confirm() (unless --yes) gates applyProject for that one project, then writeLinearMap.
    - LINEAR_API_KEY read once at the Node boundary with a fail-fast message.
  </behavior>
  <action>
    Create cli.ts exporting async runCli(argv: string[]): Promise<number>. Use node:util parseArgs with the flags in behavior. Resolve the single SyncConfigEntry by --project; throw `--project <name> must match exactly one configured repo (bulk write is disallowed)` when it does not resolve to exactly one. Read LINEAR_API_KEY via process.env once (client.ts message style). Orchestrate the pipeline in the documented order; print renderDiff; on non-dry-run call confirm() unless --yes, and only on approval call applyProject({fetchFn:fetch, endpoint, auth}, model, map, {dryRun:false, writeSnapshot:args['write-snapshot']}) then writeLinearMap. Return 0 on success, non-zero on error. Create scripts/sync-gsd-linear.ts thin entrypoint: `process.exit(await runCli(process.argv.slice(2)))`. Add "sync:gsd": "tsx scripts/sync-gsd-linear.ts" to package.json scripts directly under sync:snapshot. Write cli.test.ts asserting: missing/ambiguous --project errors; --dry-run default performs no apply (spy applyProject not called / no mutation fetch); a resolved single project prints a diff.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/cli.test.ts && grep -q 'sync:gsd' package.json && CI=true npx vitest run scripts/sync-gsd-linear</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/cli.test.ts exits 0
    - Test proves an absent or multi-match --project errors without applying (bulk-write guard)
    - Test proves --dry-run (default) never calls applyProject in write mode
    - grep -c 'sync:gsd' package.json returns 1
    - CI=true npx vitest run scripts/sync-gsd-linear (full phase suite) exits 0
  </acceptance_criteria>
  <done>pnpm sync:gsd runs the full pipeline, dry-run by default, one resolved project per invocation, approval-gated writes; phase suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E verification (dry-run accuracy + apply/re-run no-op)</name>
  <files>public/roadmap.json, linear-map.json</files>
  <action>
    Claude has already automated the full CLI. This checkpoint is the human-only live-wire verification from VALIDATION.md (writing to production Linear is deliberate and approval-gated). The developer runs the dry-run and a real apply + re-run and confirms accuracy and idempotency per the steps below.
  </action>
  <what-built>The complete sync-gsd-linear CLI: pnpm sync:gsd -- --project claude-workflow prints a per-project diff (dry-run default); --yes or an interactive y/N gates a real per-project apply.</what-built>
  <how-to-verify>
    1. Ensure LINEAR_API_KEY is exported (required for the Linear read/resolve step).
    2. Run: pnpm sync:gsd -- --project claude-workflow  (dry-run default). Eyeball the printed diff against ../claude-workflow/.planning/ and the AGE workspace in Linear — confirm milestone/issue counts and proposed dates look accurate (VALIDATION.md manual item #1). Phases claude-workflow shows as already shipped should NOT get fresh dates.
    3. Approve a real apply for ONE project (pnpm sync:gsd -- --project claude-workflow, answer y, or add --yes). Confirm records appear in Linear with no duplicates.
    4. Re-run the same apply command. Confirm the second run reports an empty diff / no writes (VALIDATION.md manual item #2 — idempotent live wire).
    5. Confirm public/roadmap.json gained planAhead on the affected project and still contains no token/email.
    NOTE (RESEARCH Open Question 2): fx-signal-agent has pre-existing non-conforming milestones (M7/M8). Do NOT apply fx-signal-agent first without seeding linear-map.json by hand, or scope the first rollout to claude-workflow + cparx.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the dry-run diff is accurate and a real apply + re-run is a no-op.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe the diff/idempotency discrepancy observed</resume-signal>
  <done>Both VALIDATION.md manual-only items confirmed: dry-run diff judged accurate, and a live apply followed by a re-run is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI argv -> pipeline | --project must resolve to exactly one repo before any write |
| LINEAR_API_KEY -> outbound header | Read once at the Node boundary; never logged or written to a file |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | runCli project resolution | mitigate | Hard-enforce a single resolved --project per apply; error on absent/multi-match; never loop all repos under one prompt (CLAUDE.md hard constraint, RESEARCH Security Domain) |
| T-06-02 | Information Disclosure | LINEAR_API_KEY | mitigate | process.env read once; never console.logged; not written to any file |
| T-06-04 | Access Control | approval gate | mitigate | Writes require interactive y/N or explicit --yes; dry-run is the default (D-06-07) |
| T-06-SC | Tampering | package installs | accept | No packages this phase; node:util/readline builtins |
</threat_model>

<success_criteria>
- SYNC-04 fully satisfied: dry-run-default, single-project approval-gated apply, idempotent re-run, flags wired.
- The two VALIDATION.md manual-only items captured (dry-run accuracy + live re-run no-op).
- Full phase test suite green; no bulk-write path exists.
</success_criteria>

<output>
Create `.planning/phases/06/06-07-SUMMARY.md` when done. Capture the human-verify result
(dry-run accuracy + apply/re-run no-op) in the SUMMARY or a UAT artifact.
</output>
