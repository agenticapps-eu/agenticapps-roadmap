---
phase: 06
plan: 07
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-04, 06-05, 06-06]
files_modified: [scripts/sync-gsd-linear/prompt.ts, scripts/sync-gsd-linear/cli.ts, scripts/sync-gsd-linear/cli.test.ts, scripts/sync-gsd-linear.ts, package.json]
autonomous: false
requirements: [SYNC-04]
must_haves:
  truths:
    - "The CLI has an explicit invocation truth table: dry-run is the default and an apply requires --apply (or --yes); an apply without exactly one resolved --project is a hard error (bulk-write guard)"
    - "--project matches against the config entry's `name` field"
    - "A --project-less invocation is allowed ONLY in dry-run: it prints a read-only multi-repo preview across all configured repos and performs zero mutations"
    - "A write happens only after an interactive y/N approval (or --yes), always scoped to one resolved project — never a bulk write across repos"
    - "--anchor and --cadence flow into date proposal; --write-snapshot threads into applyProject on BOTH branches so the dry-run-with-snapshot path patches planAhead"
    - "A real dry-run against claude-workflow produces a human-accurate diff; the live apply + re-run no-op is the binding contract test for SYNC-04 idempotency"
  artifacts:
    - path: "scripts/sync-gsd-linear/cli.ts"
      provides: "parseArgs orchestration walker->parser->dates->resolve->diff->prompt->apply with a single-project bulk-write gate on the apply path"
      contains: "parseArgs"
    - path: "scripts/sync-gsd-linear.ts"
      provides: "thin tsx entrypoint"
    - path: "package.json"
      provides: "sync:gsd script"
      contains: "sync:gsd"
  key_links:
    - from: "package.json"
      to: "scripts/sync-gsd-linear.ts"
      via: "sync:gsd script -> tsx entrypoint"
      pattern: "sync:gsd"
---

<objective>
Wire the SYNC-04 CLI: prompt.ts (the y/N gate), cli.ts (parseArgs + pipeline orchestration
with an explicit invocation truth table and the hard single-project bulk-write guard on the
apply path), the thin entrypoint, and the package.json script. Per the cross-AI review, the
apply path is now explicit (`--apply`/`--yes`, dry-run default), `--project` matches the
config `name`, and `--write-snapshot` threads through on both branches. Then verify the
end-to-end manual items from VALIDATION.md: a real dry-run against claude-workflow (human
judges the diff) and a real apply + re-run no-op (the binding idempotency contract test).

Output: prompt.ts, cli.ts (+test), scripts/sync-gsd-linear.ts, package.json sync:gsd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md
@.planning/phases/06/06-VALIDATION.md

<interfaces>
From scripts/sync-gsd-linear/config.ts: loadSyncConfig, loadLinearMap, SyncConfigEntry (now has `name`). From walker.ts/parser.ts (06-02): walkPlanning, parseRepo. From dates.ts (06-04): proposeDates (non-mutating). From diff.ts (06-04): buildDiff, renderDiff. From resolve.ts (06-05): buildResolvedWorkspace. From apply.ts (06-06): applyProject, writeLinearMap. Node builtins: node:util parseArgs, node:readline/promises. client.ts pattern: process.env["LINEAR_API_KEY"] read once with fail-fast message.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prompt.ts — node:readline y/N gate</name>
  <files>scripts/sync-gsd-linear/prompt.ts</files>
  <read_first>
    - .planning/phases/06/06-RESEARCH.md (Standard Stack: node:readline/promises createInterface().question())
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" prompt.ts (~10-line helper)
  </read_first>
  <action>
    Create prompt.ts exporting async confirm(question: string): Promise<boolean> using node:readline/promises createInterface({input: process.stdin, output: process.stdout}), asking question, returning true only for a "y"/"yes" (case-insensitive) answer, closing the interface in a finally. ~10 lines, no extra abstraction.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'readline/promises' scripts/sync-gsd-linear/prompt.ts returns 1
    - grep -c 'export async function confirm' scripts/sync-gsd-linear/prompt.ts returns 1
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>confirm() returns a boolean from an interactive y/N prompt; default-No behavior; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: cli.ts + entrypoint + package.json sync:gsd</name>
  <files>scripts/sync-gsd-linear/cli.ts, scripts/sync-gsd-linear/cli.test.ts, scripts/sync-gsd-linear.ts, package.json</files>
  <read_first>
    - scripts/sync-snapshot.ts (thin entrypoint style) + package.json line "sync:snapshot" (script wiring)
    - scripts/linear/client.ts (Node-only LINEAR_API_KEY fail-fast boundary)
    - ./CLAUDE.md (Commands table — NOTE: this revision makes the apply form explicit as `--project <name> --apply`; the executor should update the CLAUDE.md `sync:gsd` command examples to match the truth table below and note it in the SUMMARY)
    - .planning/phases/06/06-CONTEXT.md (D-06-07 approval flow, --dry-run default, --project, --yes; D-06-06 --anchor/--cadence; D-06-09 --write-snapshot)
    - .planning/phases/06/06-RESEARCH.md Security Domain (unintended bulk WRITE -> hard single --project gate on the apply path only)
    - .planning/phases/06/06-REVIEWS.md Consensus items 3 (apply-mode truth table + --project name key) and 6 (--write-snapshot threading)
  </read_first>
  <behavior>
    - parseArgs supports --dry-run (bool), --apply (bool), --project &lt;name&gt;, --yes (bool), --anchor &lt;date&gt;, --cadence &lt;weeks&gt;, --write-snapshot (bool). Apply mode is entered iff (--apply OR --yes); otherwise the run is dry-run (dry-run is the default). --yes additionally bypasses the interactive prompt.
    - INVOCATION TRUTH TABLE (implement exactly):
      | Invocation | Behavior |
      |---|---|
      | no flags / `--dry-run` | all-repo read-only preview (zero mutations) |
      | `--project X` (dry-run default) | one-project read-only preview |
      | `--project X --apply` | print the full operation list, interactive y/N prompt, then write |
      | `--project X --apply --yes` (or `--project X --yes`) | print operations, then write non-interactively |
      | `--apply`/`--yes` without exactly one resolved `--project` | HARD ERROR before any write (bulk-write guard) |
      | `--project` matching zero or multiple entries | HARD ERROR regardless of mode |
    - --project resolves against SyncConfigEntry.name (exact match); zero/multiple matches error with a clear message.
    - --project-less dry-run is ALLOWED: iterate every configured repo and print a read-only multi-repo preview (one renderDiff block per repo, prefixed by a "--- <name> ---" header). Zero mutations occur, so this does NOT violate the no-bulk-write constraint (which governs writes only).
    - Single-project pipeline: loadSyncConfig+loadLinearMap -> walkPlanning+parseRepo -> proposeDates(anchor,cadence) (pass the dated model into buildDiff) -> buildResolvedWorkspace -> buildDiff -> renderDiff to stdout. In dry-run, still call applyProject with {dryRun:true, writeSnapshot} so a --write-snapshot dry-run reaches patchPlanAhead (zero mutations). In apply mode, confirm() (unless --yes) gates applyProject({dryRun:false, writeSnapshot}) then writeLinearMap.
    - LINEAR_API_KEY read once at the Node boundary with a fail-fast message.
  </behavior>
  <action>
    Create cli.ts exporting async runCli(argv: string[]): Promise<number>. Use node:util parseArgs with the flags above; compute `const applyMode = Boolean(values.apply || values.yes)` and `const dryRun = !applyMode`. Read LINEAR_API_KEY via process.env once (client.ts message style). Resolve --project against entry.name. Branch: (a) if --project resolves to exactly one entry: run the single-project pipeline. In dry-run print renderDiff then call applyProject({fetchFn:fetch,endpoint,auth}, model, map, {dryRun:true, writeSnapshot:values['write-snapshot'], mapPath, roadmapPath}) (zero mutations, may patch planAhead). In apply mode, print the operation list, call confirm() unless values.yes, and on approval call applyProject({...},{dryRun:false, writeSnapshot:values['write-snapshot'], mapPath, roadmapPath}) then writeLinearMap. (b) if --project is ABSENT/empty: permit ONLY when dry-run — loop all configured repos and print a read-only preview renderDiff per repo (header per repo) with zero mutations; if applyMode is true, throw `--project <name> must match exactly one configured repo before an apply (bulk write is disallowed)`. (c) a --project matching zero or multiple entries throws that same error regardless of mode. Create scripts/sync-gsd-linear.ts thin entrypoint: `process.exit(await runCli(process.argv.slice(2)))`. Add "sync:gsd": "tsx scripts/sync-gsd-linear.ts" to package.json scripts directly under sync:snapshot. Write cli.test.ts asserting: (i) a --project-less --dry-run prints a preview and performs zero mutations; (ii) a --project-less --apply/--yes throws the bulk-write error; (iii) a --project matching zero/multiple entries errors; (iv) dry-run default (single project) performs no write; (v) --project X --apply calls confirm() then applyProject in write mode; (vi) --project X --yes writes without confirm(); (vii) --project matches on entry.name.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/cli.test.ts && grep -q 'sync:gsd' package.json && CI=true npx vitest run scripts/sync-gsd-linear</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/cli.test.ts exits 0
    - Test proves a --project-less --dry-run prints a preview and performs zero mutations
    - Test proves a --project-less --apply/--yes throws the bulk-write error
    - Test proves a --project matching zero or multiple entries errors without applying
    - Test proves --project X --apply gates on confirm() and --project X --yes skips it
    - Test proves --project resolves against entry.name
    - grep -c 'sync:gsd' package.json returns 1
    - CI=true npx vitest run scripts/sync-gsd-linear (full phase suite) exits 0
  </acceptance_criteria>
  <done>pnpm sync:gsd implements the invocation truth table: dry-run default (incl. a zero-mutation multi-repo preview), apply requires --apply/--yes + exactly one --project matched on name, --write-snapshot threads through both branches; phase suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live E2E verification — binding contract test for SYNC-04 (dry-run accuracy + apply/re-run no-op)</name>
  <files>public/roadmap.json, linear-map.json</files>
  <action>
    Claude has already automated the full CLI. This checkpoint is the human-only live-wire verification from VALIDATION.md and is the BINDING contract test for SYNC-04 idempotency (success criterion #2): the mocked apply.test.ts proves the LOGIC, but only this live re-run proves the CONTRACT against real Linear (which happily creates same-named records). It is non-circumventable — workflow.auto_advance does not apply. Writing to production Linear is deliberate and approval-gated. The developer runs the dry-run and a real apply + re-run and confirms accuracy and idempotency per the steps below.
  </action>
  <what-built>The complete sync-gsd-linear CLI: pnpm sync:gsd -- --project claude-workflow prints a per-project diff (dry-run default); pnpm sync:gsd -- --dry-run (no --project) prints a read-only multi-repo preview; pnpm sync:gsd -- --project claude-workflow --apply (or --yes) gates a real per-project apply.</what-built>
  <how-to-verify>
    1. Ensure LINEAR_API_KEY is exported (required for the Linear read/resolve step).
    2. Run: pnpm sync:gsd -- --project claude-workflow  (dry-run default). Eyeball the printed operation list against ../claude-workflow/.planning/ and the AGE workspace in Linear — confirm the enumerated milestone/issue/label/initiative operations and proposed dates look accurate (VALIDATION.md manual item #1). Phases claude-workflow shows as already shipped should NOT get fresh dates (they appear under "informational only", not as operations).
    3. Run: pnpm sync:gsd -- --dry-run  (no --project). Confirm it prints a read-only preview across all configured repos and performs zero writes.
    4. Approve a real apply for ONE project: pnpm sync:gsd -- --project claude-workflow --apply (answer y), or add --yes. Confirm records appear in Linear with no duplicates.
    5. Re-run the same apply command. Confirm the second run reports an empty operation set / no writes (VALIDATION.md manual item #2 — the binding idempotent live-wire test).
    6. Confirm public/roadmap.json gained planAhead on the affected project and still contains no token/email.
    NOTE (RESEARCH Open Question 2): fx-signal-agent has pre-existing non-conforming milestones (M7/M8). Do NOT apply fx-signal-agent first without seeding linear-map.json by hand, or scope the first rollout to claude-workflow + cparx.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the dry-run diff is accurate and a real apply + re-run is a no-op (binding SYNC-04 idempotency contract).</human-check>
  </verify>
  <resume-signal>Type "approved" or describe the diff/idempotency discrepancy observed</resume-signal>
  <done>Both VALIDATION.md manual-only items confirmed: dry-run diff judged accurate, and a live apply followed by a re-run is a no-op (binding SYNC-04 #2 contract test passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI argv -> pipeline | --project must resolve to exactly one repo before any WRITE; a --project-less run is permitted read-only in dry-run |
| LINEAR_API_KEY -> outbound header | Read once at the Node boundary; never logged or written to a file |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation of Privilege | runCli project resolution | mitigate | Apply mode (--apply/--yes) hard-requires exactly one resolved --project (matched on entry.name); error on absent/multi-match before any write; --project-less runs are read-only dry-run previews (zero mutations) so no bulk write path exists (CLAUDE.md hard constraint, RESEARCH Security Domain) |
| T-06-02 | Information Disclosure | LINEAR_API_KEY | mitigate | process.env read once; never console.logged; not written to any file |
| T-06-04 | Access Control | approval gate | mitigate | Writes require --apply plus interactive y/N or explicit --yes; dry-run is the default (D-06-07) |
| T-06-SC | Tampering | package installs | accept | No packages this phase; node:util/readline builtins |
</threat_model>

<success_criteria>
- SYNC-04 fully satisfied: dry-run-default, explicit --apply/--yes single-project approval-gated apply (matched on name), idempotent re-run, flags wired, --write-snapshot threaded on both branches.
- --project-less dry-run gives a CLAUDE.md-compatible zero-mutation multi-repo preview; every write path is single-project.
- The two VALIDATION.md manual-only items captured (dry-run accuracy + live re-run no-op), the latter being the binding SYNC-04 #2 contract test.
- Full phase test suite green; no bulk-write path exists.
</success_criteria>

<output>
Create `.planning/phases/06/06-07-SUMMARY.md` when done. Capture the human-verify result
(dry-run accuracy + apply/re-run no-op) in the SUMMARY or a UAT artifact, and note the
CLAUDE.md Commands-table reconciliation (`--project <name> --apply`).
</output>
</content>
