---
phase: 06
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified: [scripts/sync-gsd-linear/hash.ts, scripts/sync-gsd-linear/hash.test.ts, scripts/sync-gsd-linear/mutations.ts]
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "The title-hash is stable across runs: same slug/heading -> same hash, different slug -> different hash"
    - "Every Linear read query and write mutation exists as a typed GraphQL document string with $variable placeholders"
    - "No untrusted .planning/ content is ever interpolated into a GraphQL query body"
  artifacts:
    - path: "scripts/sync-gsd-linear/hash.ts"
      provides: "sha256 title-hash helper"
      contains: "createHash"
    - path: "scripts/sync-gsd-linear/mutations.ts"
      provides: "all read queries + write mutations as const strings + typed req/res interfaces"
      contains: "export const PROJECT_CREATE"
  key_links:
    - from: "scripts/sync-gsd-linear/mutations.ts"
      to: "Linear GraphQL variables"
      via: "$variable: Type! placeholders, never string interpolation"
      pattern: "\\$input: "
---

<objective>
Two foundational, dependency-free modules for SYNC-02: hash.ts (the stable title-hash the
dedup fallback keys on) and mutations.ts (every read query + write mutation as a typed
GraphQL document, verified field names from RESEARCH.md Code Examples). mutations.ts is
interface-first for the resolver (06-04) and apply (06-06) plans.

Output: hash.ts (+test), mutations.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
Analog scripts/linear/query.ts: MAIN_QUERY/ISSUES_QUERY as `export const NAME = \`...\`` with $var placeholders + a top comment explaining why. RESEARCH.md "Code Examples" gives the exact verified mutation/query bodies to drop in.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hash.ts — stable sha256 title-hash</name>
  <files>scripts/sync-gsd-linear/hash.ts, scripts/sync-gsd-linear/hash.test.ts</files>
  <read_first>
    - scripts/linear/transform.ts (small pure-helper style, e.g. redactEmails)
    - .planning/phases/06/06-RESEARCH.md (Don't Hand-Roll: node:crypto createHash; Open Question 1 hash input = directory slug)
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" hash.ts
  </read_first>
  <behavior>
    - titleHash(input) returns a stable hex digest; same input -> identical output on repeat calls; different input -> different output.
    - Milestone identity hashes the full directory slug; issue identity hashes the plan heading (or filename when generic) — the helper is agnostic, callers pass the right string.
  </behavior>
  <action>
    Create hash.ts exporting `export function titleHash(input: string): string` using node:crypto createHash("sha256").update(input).digest("hex"). Keep it a single pure ~5-line function, no process/fs. Write hash.test.ts asserting stability (two calls equal), collision-freedom for the two duplicate-NN slugs (01-go-routing vs 01-gsd-bug-fixes hash differently), and determinism across a fixed known input.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/hash.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/hash.test.ts exits 0
    - grep -c 'createHash' scripts/sync-gsd-linear/hash.ts returns 1
    - Test proves 01-go-routing and 01-gsd-bug-fixes produce different hashes
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>titleHash is stable, deterministic, collision-safe for distinct slugs; test green.</done>
</task>

<task type="auto">
  <name>Task 2: mutations.ts — typed read queries + write mutations</name>
  <files>scripts/sync-gsd-linear/mutations.ts</files>
  <read_first>
    - scripts/linear/query.ts (file-header rationale comment + const-string-with-$var pattern to copy exactly)
    - .planning/phases/06/06-RESEARCH.md "Code Examples" (verified bodies: TEAMS_QUERY, PROJECT_CREATE, INITIATIVE_TO_PROJECT_CREATE, MILESTONE_CREATE, ISSUE_CREATE, PROJECT_LABELS_QUERY, ISSUE_LABELS_QUERY)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 2 two label pools; Pitfall 4 teamIds required)
    - .planning/phases/06/06-PATTERNS.md section mutations.ts
  </read_first>
  <action>
    Create mutations.ts mirroring query.ts. Top comment block explains why teamIds must be resolved before projectCreate (Pitfall 4) and why labels are resolved twice — ProjectLabel vs IssueLabel are different pools (Pitfall 2). Export const GraphQL documents (bodies from RESEARCH Code Examples, verbatim field names): read queries TEAMS_QUERY, PROJECT_LABELS_QUERY, ISSUE_LABELS_QUERY; write mutations PROJECT_CREATE, PROJECT_UPDATE, PROJECT_MILESTONE_CREATE, PROJECT_MILESTONE_UPDATE, ISSUE_CREATE, ISSUE_UPDATE, PROJECT_LABEL_CREATE, ISSUE_LABEL_CREATE, INITIATIVE_TO_PROJECT_CREATE. Every document uses $variable placeholders (e.g. $input: ProjectCreateInput!, $key: String!, $name: String!) — never interpolate slugs/headings. Add typed request/response interfaces per document (e.g. ProjectCreatePayload {success: boolean; project: {id: string; name: string} | null}) matching RESEARCH payload shapes; no any. No fetch/process here — pure doc + type module.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'export const' scripts/sync-gsd-linear/mutations.ts returns >= 12 (three queries + nine mutations)
    - grep -c 'export const PROJECT_CREATE\|export const ISSUE_CREATE\|export const PROJECT_MILESTONE_CREATE\|export const INITIATIVE_TO_PROJECT_CREATE' scripts/sync-gsd-linear/mutations.ts returns 4
    - grep -v '^\s*//' scripts/sync-gsd-linear/mutations.ts | grep -c '\${' returns 0 (no interpolation of values into query bodies)
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>mutations.ts holds all read queries + write mutations as $variable-only typed documents with a rationale header; no any; no interpolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| untrusted .planning/ values -> GraphQL documents | Slugs/headings become mutation variables, never query-body text |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | mutations.ts documents | mitigate | All values passed via GraphQL $variables; acceptance greps that no `${` interpolation exists in non-comment lines (V5 Input Validation) |
| T-06-SC | Tampering | package installs | accept | No packages this phase; node:crypto is a builtin |
</threat_model>

<success_criteria>
- SYNC-02 groundwork: stable title-hash + a complete, injection-safe, typed GraphQL document set.
- No any; no value interpolation into query bodies.
</success_criteria>

<output>
Create `.planning/phases/06/06-03-SUMMARY.md` when done.
</output>
