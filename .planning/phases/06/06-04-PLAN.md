---
phase: 06
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified: [scripts/sync-gsd-linear/dates.ts, scripts/sync-gsd-linear/dates.test.ts, scripts/sync-gsd-linear/diff.ts, scripts/sync-gsd-linear/diff.test.ts]
autonomous: true
requirements: [SYNC-03]
must_haves:
  truths:
    - "Dates are proposed sequentially by phase order from an anchor at a fixed cadence"
    - "Completed phases keep their existing dates; no future date is invented for them"
    - "Decimal phases order component-wise (04.2 before 04.10), not via parseFloat"
    - "The diff renders the documented human summary: + N milestones, + M issues, ~ D dates"
  artifacts:
    - path: "scripts/sync-gsd-linear/dates.ts"
      provides: "anchor + cadence -> per-phase proposed target dates in phase order"
      contains: "export function proposeDates"
    - path: "scripts/sync-gsd-linear/diff.ts"
      provides: "normalized model + resolved state -> DiffSummary"
      contains: "export function buildDiff"
  key_links:
    - from: "scripts/sync-gsd-linear/diff.ts"
      to: "scripts/sync-gsd-linear/config.ts"
      via: "consumes NormalizedModel + ResolvedWorkspace, returns DiffSummary"
      pattern: "DiffSummary"
---

<objective>
Build SYNC-03: the date proposer and the diff engine, both pure computation over the
normalized model + resolved Linear state (contract types from 06-01). Correct decimal
phase ordering (component-wise, not parseFloat) and the D-06-06 date algorithm (fixed
cadence from an anchor, completed phases untouched). The diff produces the documented
human summary shape from CONTEXT.md.

Output: dates.ts, diff.ts, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
From scripts/sync-gsd-linear/config.ts (06-01): NormalizedModel, NormalizedPhase {slug, number, completed, plans[], proposedDate?}, ResolvedWorkspace {teamId, project, projectLabelId, issueLabelId}, ResolvedProject {id, name, repoKey, milestones[]}, DiffSummary {milestonesToCreate, issuesToCreate, datesToChange, detail[]}. Linear targetDate is a TimelessDate ("YYYY-MM-DD").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dates.ts — anchor + cadence sequential date proposer</name>
  <files>scripts/sync-gsd-linear/dates.ts, scripts/sync-gsd-linear/dates.test.ts</files>
  <read_first>
    - .planning/phases/06/06-CONTEXT.md (D-06-06 date algorithm: fixed cadence from anchor, completed untouched, default anchor today, configurable cadence)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 1 component-wise numeric ordering; TimelessDate YYYY-MM-DD)
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" dates.ts (plain Date arithmetic; bucketFor-style helper)
    - scripts/sync-gsd-linear/config.ts (NormalizedPhase, NormalizedModel)
  </read_first>
  <behavior>
    - comparePhaseNumber(a, b) orders decimal phases component-wise: 04.2 < 04.10, 03.5 between 03 and 04; never uses parseFloat.
    - proposeDates(phases, {anchor, cadenceWeeks}) sorts in phase order, leaves completed phases' existing dates untouched, anchors the first not-completed phase at anchor (default today), and adds cadenceWeeks per subsequent not-completed phase.
    - Output dates are TimelessDate "YYYY-MM-DD" strings.
  </behavior>
  <action>
    Create dates.ts exporting comparePhaseNumber(a: string, b: string): number (split on ".", compare numeric segments left-to-right, shorter-is-less on tie) and proposeDates(phases: NormalizedPhase[], opts: {anchor?: string; cadenceWeeks?: number}): NormalizedPhase[] (returns phases with proposedDate set for not-completed ones; default anchor = today's YYYY-MM-DD, default cadenceWeeks = 2). Format dates with a small toTimelessDate(d: Date) helper (YYYY-MM-DD, UTC). No process/fs; plain Date math. Write dates.test.ts test-first asserting: 04.2 sorts before 04.10; completed phase keeps its date; first not-completed phase = anchor; second = anchor + cadenceWeeks; decimal phase inserted in correct position.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/dates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/dates.test.ts exits 0
    - Test proves comparePhaseNumber orders 04.2 before 04.10
    - Test proves a completed phase's proposedDate is left untouched
    - grep -c 'parseFloat' scripts/sync-gsd-linear/dates.ts returns 0
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>dates.ts proposes cadence-from-anchor dates in correct decimal order, leaving completed phases untouched; test green; no parseFloat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diff.ts — normalized model + resolved state -> DiffSummary</name>
  <files>scripts/sync-gsd-linear/diff.ts, scripts/sync-gsd-linear/diff.test.ts</files>
  <read_first>
    - scripts/linear/transform.ts (buildSnapshot pure-computation shape)
    - scripts/sync-gsd-linear/config.ts (NormalizedModel, ResolvedWorkspace, ResolvedProject, DiffSummary)
    - scripts/sync-gsd-linear/dates.ts (proposeDates output feeds the date-change count)
    - .planning/phases/06/06-CONTEXT.md ("Specific Ideas" diff mockup: + N milestones, + M issues, ~ D dates + per-record detail)
  </read_first>
  <behavior>
    - buildDiff(model, resolved) counts milestonesToCreate (phases with no resolved milestone), issuesToCreate (plans with no resolved issue), datesToChange (phases whose proposedDate differs from the resolved milestone's targetDate), and a detail[] of per-record human lines.
    - When resolved state already contains every milestone/issue and dates match, all counts are 0 (empty diff — the idempotency signal).
    - renderDiff(summary, repo) returns the CONTEXT.md summary string ("repo -> Linear\n  + N milestones, + M issues, ~ D dates" + detail lines).
  </behavior>
  <action>
    Create diff.ts exporting buildDiff(model: NormalizedModel, resolved: ResolvedWorkspace): DiffSummary and renderDiff(summary: DiffSummary, repo: string): string. Pure functions, no I/O. Compare model phases/plans against resolved.project.milestones (matched by titleHash of slug from hash.ts) and against resolved issues to compute the three counts + detail[]. renderDiff builds the human summary using a tiny ANSI color helper (green +, yellow ~) per RESEARCH Supporting stack — hand-rolled, no chalk. Write diff.test.ts test-first asserting: all-new model yields + counts equal to phase/plan totals; fully-resolved model yields an empty diff (0/0/0); a changed proposedDate increments datesToChange; renderDiff output contains "+ " and "~ ".
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/diff.test.ts exits 0
    - Test proves a fully-resolved model produces a 0/0/0 empty diff
    - grep -c 'export function buildDiff\|export function renderDiff' scripts/sync-gsd-linear/diff.ts returns 2
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>diff.ts computes accurate create/date counts + human summary and returns an empty diff when Linear already matches; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| normalized model (untrusted origin) -> diff/dates | Pure computation only; no I/O, no query construction here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | diff/date proposals shown pre-write | mitigate | Diff is display-only; no mutation happens in this plan; dry-run stops before apply (SYNC-04 gate lives in 06-06/06-07) |
| T-06-SC | Tampering | package installs | accept | No packages this phase; ANSI helper hand-rolled |
</threat_model>

<success_criteria>
- SYNC-03 satisfied: correct decimal-ordered cadence dates (completed untouched) + accurate diff summary.
- Empty diff on a fully-resolved model — the idempotency signal 06-06 relies on.
- Both modules green; no parseFloat; no any.
</success_criteria>

<output>
Create `.planning/phases/06/06-04-SUMMARY.md` when done.
</output>
