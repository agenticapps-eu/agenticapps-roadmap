---
phase: 06
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified: [scripts/sync-gsd-linear/dates.ts, scripts/sync-gsd-linear/dates.test.ts, scripts/sync-gsd-linear/diff.ts, scripts/sync-gsd-linear/diff.test.ts]
autonomous: true
requirements: [SYNC-03]
must_haves:
  truths:
    - "Dates are proposed sequentially by phase order from an anchor at a fixed cadence"
    - "Completed phases keep their existing dates; no future date is invented for them"
    - "Decimal phases order component-wise (04.2 before 04.10), not via parseFloat"
    - "The diff enumerates EVERY mutation the apply will perform (SyncOperation list): project create, project-label create, issue-label create, initiative join, milestone create, issue create — so nothing is written that wasn't shown"
    - "The rendered diff shows the documented human summary (+ N milestones, + M issues, + L labels, ~ D dates) AND labels drifted existing-milestone dates as INFORMATIONAL (v1 create-only apply does not write them)"
  artifacts:
    - path: "scripts/sync-gsd-linear/dates.ts"
      provides: "anchor + cadence -> per-phase proposed target dates in phase order"
      contains: "export function proposeDates"
    - path: "scripts/sync-gsd-linear/diff.ts"
      provides: "normalized model + resolved state -> DiffSummary (full SyncOperation write set)"
      contains: "export function buildDiff"
  key_links:
    - from: "scripts/sync-gsd-linear/diff.ts"
      to: "scripts/sync-gsd-linear/config.ts"
      via: "consumes NormalizedModel + ResolvedWorkspace, returns DiffSummary with operations[]"
      pattern: "DiffSummary"
    - from: "scripts/sync-gsd-linear/diff.ts"
      to: "scripts/sync-gsd-linear/hash.ts"
      via: "matches phases/plans to resolved milestones/issues by titleHash of identity (from 06-03)"
      pattern: "titleHash"
---

<objective>
Build SYNC-03: the date proposer and the diff engine, both pure computation over the
normalized model + resolved Linear state (contract types from 06-01). Correct decimal
phase ordering (component-wise, not parseFloat) and the D-06-06 date algorithm (fixed
cadence from an anchor, completed phases untouched). Per the cross-AI review, the diff now
produces the FULL enumerated write set (every SyncOperation the apply will perform) so the
printed diff equals the approved writes, and it labels drifted existing dates as
informational because v1 apply is create-only.

Output: dates.ts, diff.ts, and their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
From scripts/sync-gsd-linear/config.ts (06-01): NormalizedModel, NormalizedPhase {slug, number, completed, plans[], proposedDate?}, NormalizedPlan {file, title, key, taskLines}, ResolvedWorkspace {teamId, project, projectLabelId, issueLabelId}, ResolvedProject {id, name, repoKey, milestones[], issues[]}, ResolvedIssue {id, title, identityKey, projectId, milestoneId, labelIds}, SyncOperation (discriminated union: kind ∈ project-create|project-label-create|issue-label-create|initiative-join|milestone-create|issue-create; identityKey; detail), DiffSummary {operations[], datesInformational[], milestonesToCreate, issuesToCreate, labelsToCreate, datesToChange, detail[]}. Linear targetDate is a TimelessDate ("YYYY-MM-DD"). From scripts/sync-gsd-linear/hash.ts (06-03): titleHash(input: string): string — hash the phase slug for milestones and the plan key for issues.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dates.ts — anchor + cadence sequential date proposer</name>
  <files>scripts/sync-gsd-linear/dates.ts, scripts/sync-gsd-linear/dates.test.ts</files>
  <read_first>
    - .planning/phases/06/06-CONTEXT.md (D-06-06 date algorithm: fixed cadence from anchor, completed untouched, default anchor today, configurable cadence)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 1 component-wise numeric ordering; TimelessDate YYYY-MM-DD)
    - .planning/phases/06/06-PATTERNS.md section "No Analog Found" dates.ts (plain Date arithmetic; bucketFor-style helper)
    - scripts/sync-gsd-linear/config.ts (NormalizedPhase, NormalizedModel)
  </read_first>
  <behavior>
    - comparePhaseNumber(a, b) orders decimal phases component-wise: 04.2 < 04.10, 03.5 between 03 and 04; never uses parseFloat.
    - proposeDates(phases, {anchor, cadenceWeeks}) returns a NEW NormalizedPhase[] (non-mutating) sorted in phase order, leaving completed phases' existing dates untouched, anchoring the first not-completed phase at anchor (default today), and adding cadenceWeeks per subsequent not-completed phase.
    - Output dates are TimelessDate "YYYY-MM-DD" strings.
  </behavior>
  <action>
    Create dates.ts exporting comparePhaseNumber(a: string, b: string): number (split on ".", compare numeric segments left-to-right, shorter-is-less on tie) and proposeDates(phases: NormalizedPhase[], opts: {anchor?: string; cadenceWeeks?: number}): NormalizedPhase[] (returns a NEW array with proposedDate set for not-completed phases — do not mutate the input; default anchor = today's YYYY-MM-DD, default cadenceWeeks = 2). Format dates with a small toTimelessDate(d: Date) helper (YYYY-MM-DD, UTC). No process/fs; plain Date math. Write dates.test.ts test-first asserting: 04.2 sorts before 04.10; completed phase keeps its date; first not-completed phase = anchor; second = anchor + cadenceWeeks; decimal phase inserted in correct position; the input array is not mutated.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/dates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/dates.test.ts exits 0
    - Test proves comparePhaseNumber orders 04.2 before 04.10
    - Test proves a completed phase's proposedDate is left untouched
    - Test proves proposeDates does not mutate its input array
    - grep -c 'parseFloat' scripts/sync-gsd-linear/dates.ts returns 0
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>dates.ts proposes cadence-from-anchor dates in correct decimal order (non-mutating), leaving completed phases untouched; test green; no parseFloat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diff.ts — normalized model + resolved state -> DiffSummary (full write set)</name>
  <files>scripts/sync-gsd-linear/diff.ts, scripts/sync-gsd-linear/diff.test.ts</files>
  <read_first>
    - scripts/linear/transform.ts (buildSnapshot pure-computation shape)
    - scripts/sync-gsd-linear/config.ts (NormalizedModel, ResolvedWorkspace, ResolvedProject, ResolvedIssue, SyncOperation, DiffSummary)
    - scripts/sync-gsd-linear/hash.ts (titleHash — from 06-03; diff matches by hash of phase slug / plan key)
    - scripts/sync-gsd-linear/dates.ts (proposeDates output feeds the date-change count; pass the dated model into buildDiff)
    - .planning/phases/06/06-CONTEXT.md ("Specific Ideas" diff mockup: + N milestones, + M issues, ~ D dates + per-record detail)
    - .planning/phases/06/06-REVIEWS.md Consensus item 2 (diff must equal the approved write set; state create-vs-update)
  </read_first>
  <behavior>
    - buildDiff(model, resolved) returns a DiffSummary whose `operations[]` enumerate EVERY mutation the apply will perform, in apply order: project-create (when resolved.project is null); project-label-create (when resolved.projectLabelId is null); issue-label-create (when resolved.issueLabelId is null); initiative-join (only when model.initiative is set AND the project is being newly created — v1 does not modify an existing project's initiative membership); milestone-create per phase with no resolved milestone (matched by titleHash(phase.slug)); issue-create per plan with no resolved issue (matched by titleHash(plan.key)). Each operation carries its identityKey + a human detail line. Label attach and milestone/date assignment ride inside the create inputs (no separate hidden write).
    - Convenience counts (milestonesToCreate, issuesToCreate, labelsToCreate, datesToChange) are derived from operations for the human summary.
    - datesInformational[] lists phases whose proposedDate differs from an EXISTING resolved milestone's targetDate — these are drift on records v1 create-only apply does NOT update; they are shown but NOT emitted as operations (so the diff stays honest that no date write happens for existing milestones). New milestones carry their proposedDate inside the milestone-create op.
    - When resolved state already contains every project/label/milestone/issue, operations[] is empty (the idempotency signal) even if datesInformational is non-empty.
    - renderDiff(summary, repo) returns the human summary: a "repo -> Linear" header, the "+ N milestones, + M issues, + L labels" line, each operation as a detail line, and a clearly-labeled "~ D dates (informational only — existing milestones are not updated in v1)" section when datesInformational is non-empty.
  </behavior>
  <action>
    Create diff.ts exporting buildDiff(model: NormalizedModel, resolved: ResolvedWorkspace): DiffSummary and renderDiff(summary: DiffSummary, repo: string): string. Pure functions, no I/O. Build operations[] by walking resolved state vs the (already date-proposed) model: push project-create/project-label-create/issue-label-create/initiative-join/milestone-create/issue-create ops exactly per the behavior rules, matching milestones by titleHash(phase.slug) against resolved.project.milestones and issues by titleHash(plan.key) against resolved.project.issues (identityKey). Compute datesInformational from proposedDate-vs-existing-milestone-targetDate drift. Derive the counts from operations. renderDiff builds the human summary using a tiny ANSI color helper (green +, yellow ~) per RESEARCH Supporting stack — hand-rolled, no chalk — and MUST print the informational-dates section under a distinct "informational only" label. Write diff.test.ts test-first asserting: an all-new model yields operations covering project+labels+initiative(if configured)+every milestone+every issue; a fully-resolved model yields an EMPTY operations[] (idempotency) even when a date has drifted; a drifted existing-milestone date appears in datesInformational but NOT in operations; renderDiff output contains "+ " and the "informational only" date label.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/diff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/diff.test.ts exits 0
    - Test proves an all-new model enumerates project-create + label-create(s) + milestone-create(s) + issue-create(s) in operations[]
    - Test proves a fully-resolved model produces an empty operations[] even with a drifted date
    - Test proves a drifted existing-milestone date is in datesInformational, not operations
    - grep -c 'export function buildDiff\|export function renderDiff' scripts/sync-gsd-linear/diff.ts returns 2
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>diff.ts computes the full enumerated write set (operations[]) + informational date drift + human summary, and returns an empty operation set when Linear already matches; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| normalized model (untrusted origin) -> diff/dates | Pure computation only; no I/O, no query construction here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | diff/date proposals shown pre-write | mitigate | Diff enumerates every mutation as operations[]; apply executes exactly this set (06-06), so no write happens that wasn't displayed and approved; dry-run stops before apply |
| T-06-SC | Tampering | package installs | accept | No packages this phase; ANSI helper hand-rolled |
</threat_model>

<success_criteria>
- SYNC-03 satisfied: correct decimal-ordered cadence dates (completed untouched) + a diff that enumerates the full approved write set.
- Empty operations[] on a fully-resolved model — the idempotency signal 06-06 relies on; drifted existing dates surfaced as informational only.
- Both modules green; no parseFloat; no any.
</success_criteria>

<output>
Create `.planning/phases/06/06-04-SUMMARY.md` when done.
</output>
</content>
