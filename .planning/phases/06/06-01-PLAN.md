---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/sync-gsd-linear/config.ts, scripts/sync-gsd-linear/config.test.ts, scripts/sync-gsd-linear/__fixtures__/planning-trees/README.md, scripts/sync-gsd-linear/__fixtures__/linear-responses.ts, scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts, sync.config.json, linear-map.json]
autonomous: true
requirements: [SYNC-01, SYNC-04]
must_haves:
  truths:
    - "sync.config.json and linear-map.json parse through Zod schemas or throw a clear error"
    - "The normalized-model contract types are defined once and importable by every downstream stage"
    - "NormalizedPlan carries a stable identity key (repo/phaseSlug/relativePlanPath) used for title-hashing, plus taskLines for the issue description — display title is a separate field"
    - "Each SyncConfigEntry carries a name field that --project matches against"
    - "ResolvedProject carries issue identity (id, title, milestoneId, labelIds) so issue dedup / second-run-no-op is expressible against the contracts"
    - "ResolvedWorkspace carries initiativeId — the resolved Linear Initiative id (null when the entry names no initiative) — so apply's initiative-join has a real ID to pass, not a bare name"
    - "DiffSummary enumerates every mutation the apply may perform (SyncOperation union) so the printed diff equals the approved write set"
    - "Wave-0 fixtures exist so downstream behavior tests can run test-first; the mutation-mock returns the existing id on a duplicate create keyed the same way the CLI resolves"
  artifacts:
    - path: "scripts/sync-gsd-linear/config.ts"
      provides: "SyncConfigSchema, LinearMapSchema, NormalizedModelSchema + resolved-state contracts (incl. ResolvedIssue + SyncOperation) + loaders"
      contains: "export const SyncConfigSchema"
    - path: "scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts"
      provides: "in-memory mutable mock GraphQL workspace for the idempotency test; dup-create returns the existing id"
    - path: "sync.config.json"
      provides: "committed allow-list of the three target repos (each with a name) + AGE team"
      contains: "claude-workflow"
  key_links:
    - from: "scripts/sync-gsd-linear/config.ts"
      to: "zod"
      via: "z.object schema definitions"
      pattern: "z\\.object"
---

<objective>
Interface-first foundation for the Phase-6 CLI. config.ts defines every schema and type
consumed by walker, parser, resolver, diff, dates, apply — so downstream plans build
against fixed contracts. This revision hardens the Wave-1 contracts per the cross-AI
review: a stable plan identity key + taskLines, a `name` on each config entry, issue
identity on the resolved read surface, and a SyncOperation union so the diff can enumerate
every write. Also lands the three Wave-0 fixture sets (VALIDATION.md) so all downstream
behavior tests are test-first.

Output: scripts/sync-gsd-linear/config.ts (+test), __fixtures__/ sets, seed sync.config.json / linear-map.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md
@.planning/phases/06/06-VALIDATION.md

<interfaces>
From src/lib/roadmap/schema.ts: ProjectSchema already has planAhead: z.boolean().nullish(); RoadmapJson = z.infer<typeof RoadmapJsonSchema>. Zod style: one const per concept, bottom-up, trailing export type.
From scripts/linear/map.ts: GqlResponse { data:{ initiatives, projects }; errors? }; mapWorkspace(json): RawWorkspace whose projects carry id, name, description, url, initiativeId, state{name,type}, priority, startedAt, targetDate, projectMilestones{nodes:[{id,name,targetDate}]}, issues; initiatives carry id, name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config, map, normalized-model schemas + resolved-state + operation contracts</name>
  <files>scripts/sync-gsd-linear/config.ts, scripts/sync-gsd-linear/config.test.ts</files>
  <read_first>
    - src/lib/roadmap/schema.ts (Zod styling analog)
    - src/lib/roadmap/loader.ts (safeParse-then-throw boundary)
    - .planning/phases/06/06-PATTERNS.md section config.ts
    - .planning/phases/06/06-CONTEXT.md (D-06-01 grain, D-06-02 initiative-attach, D-06-03 map pools, D-06-04 config entry, D-06-05 central map)
    - .planning/phases/06/06-REVIEWS.md Consensus items 1-4 (issue identity + plan body, resolved read surface, diff-equals-write-set, --project name)
  </read_first>
  <behavior>
    - SyncConfigSchema accepts entries {repoPath, name, label, initiative?, teamKey?, projectName?}; rejects entry missing repoPath, name, or label. `name` is the key `--project` matches against. `initiative` (when present) is a bare Initiative NAME, resolved to an id downstream.
    - LinearMapSchema accepts {projects, milestones, issues, projectLabels, issueLabels} each Record<string,{id:string}>; accepts all-empty.
    - NormalizedModelSchema accepts {repo, projectName, teamKey, initiative?, phases[]} where phase has slug, number("03.5"), completed, plans[]{file, title, key, taskLines:string[]}, proposedDate?; rejects phase missing slug or a plan missing key. `key` is the stable identity path (repo/phaseSlug/relativePlanPath) used for title-hashing; `title` is the display title only; `taskLines` are the plan's task/checklist lines for the issue description.
    - Resolved-state contracts express issue identity: ResolvedIssue{id, title, identityKey, projectId, milestoneId, labelIds}; ResolvedProject carries issues[]. ResolvedWorkspace carries initiativeId (the resolved Initiative id, or null when the entry names no initiative).
    - SyncOperation is a discriminated union covering every possible write; DiffSummary carries the operations[] list plus datesInformational[].
    - loadSyncConfig/loadLinearMap read+JSON.parse+safeParse, throw Error naming the file on failure, return typed data.
  </behavior>
  <action>
    Create config.ts (per D-06-01/02/03/04/05) exporting bottom-up: SyncConfigEntrySchema (fields {repoPath, name, label, initiative?, teamKey?, projectName?})/SyncConfigSchema, LinearMapSchema, NormalizedPlanSchema (fields {file, title, key, taskLines: z.array(z.string())})/NormalizedPhaseSchema/NormalizedModelSchema, each z.object with trailing `export type X = z.infer<typeof XSchema>` (SyncConfig, SyncConfigEntry, LinearMap, NormalizedModel, NormalizedPhase, NormalizedPlan). Import z from zod. Add resolved-state interfaces consumed by diff/resolve/apply: ResolvedMilestone{id: string; name: string; targetDate: string | null}, ResolvedIssue{id: string; title: string; identityKey: string | null; projectId: string; milestoneId: string | null; labelIds: string[]}, ResolvedProject{id: string; name: string; repoKey: string; milestones: ResolvedMilestone[]; issues: ResolvedIssue[]}, ResolvedWorkspace{teamId: string | null; project: ResolvedProject | null; projectLabelId: string | null; issueLabelId: string | null; initiativeId: string | null} (initiativeId is the resolved Linear Initiative id the apply passes to INITIATIVE_TO_PROJECT_CREATE, null when the config entry names no initiative). Add a SyncOperation discriminated union enumerating every write the apply may perform — `type SyncOperation = { kind: 'project-create' | 'project-label-create' | 'issue-label-create' | 'initiative-join' | 'milestone-create' | 'issue-create'; identityKey: string; detail: string }` (the diff renders these; apply executes exactly this list). Add DiffSummary{operations: SyncOperation[]; datesInformational: string[]; milestonesToCreate: number; issuesToCreate: number; labelsToCreate: number; datesToChange: number; detail: string[]} where operations is the immutable full write set and datesInformational lists drifted existing-milestone dates that v1 create-only apply does NOT write (surfaced so the reviewer is not misled). Add loadSyncConfig(path):SyncConfig and loadLinearMap(path):LinearMap via readFileSync+JSON.parse+safeParse throwing Error-with-filename on failure. readFileSync is the only Node-only reference. Write config.test.ts covering behavior with inline literals (incl. a plan-missing-key rejection and a name-present acceptance).
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/config.test.ts exits 0
    - grep -c 'export const SyncConfigSchema' scripts/sync-gsd-linear/config.ts returns 1 (same for LinearMapSchema, NormalizedModelSchema)
    - grep -c 'export interface ResolvedProject\|export interface ResolvedIssue\|export interface DiffSummary' scripts/sync-gsd-linear/config.ts returns 3
    - grep -c 'initiativeId' scripts/sync-gsd-linear/config.ts returns >= 1 (ResolvedWorkspace carries the resolved initiative id)
    - grep -c 'SyncOperation' scripts/sync-gsd-linear/config.ts returns >= 1
    - grep -c 'taskLines' scripts/sync-gsd-linear/config.ts returns >= 1 (NormalizedPlan carries plan body)
    - grep -c 'name' scripts/sync-gsd-linear/config.ts returns >= 1 (SyncConfigEntry has a name)
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>config.ts exports three Zod schemas (config entry has name; plan has key + taskLines), inferred types, ResolvedIssue/ResolvedProject/ResolvedWorkspace (with initiativeId)/DiffSummary contracts, a SyncOperation union, and two loaders; test green; no any.</done>
</task>

<task type="auto">
  <name>Task 2: Three Wave-0 fixture sets</name>
  <files>scripts/sync-gsd-linear/__fixtures__/planning-trees/README.md, scripts/sync-gsd-linear/__fixtures__/linear-responses.ts, scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts</files>
  <read_first>
    - scripts/linear/__fixtures__/main-response.ts, issues-page.ts (full-GqlResponse fixture contract)
    - functions/api/linear/[[path]].test.ts (per-call-index payload consumption)
    - .planning/phases/06/06-RESEARCH.md sections Wave 0 Gaps, Validation Architecture
    - .planning/phases/06/06-REVIEWS.md Consensus item 5 (mock must reject/return-existing on dup creates keyed like the CLI resolves)
    - scripts/linear/map.ts (GqlResponse shape)
  </read_first>
  <action>
    Create __fixtures__/planning-trees/ as real on-disk synthetic .planning/ trees (dirs+files, not TS) reproducing each RESEARCH quirk: (a) duplicate 01-go-routing/ + 01-gsd-bug-fixes/ each with a NN-MM-PLAN.md; (b) decimal 03.5-quality/, 04.10-x/, 04.2-y/ for component-wise ordering; (c) a PLAN.md with YAML frontmatter + descriptive H1 AND task/checklist lines under it (so taskLines extraction has real input); (d) a frontmatter-less PLAN.md with generic H1 (# Phase 09 — PLAN) plus a couple of task lines; (e) a phase with TWO generic-H1 NN-MM-PLAN.md files (to prove distinct identity keys, not a collapsed title-hash); (f) a bare single PLAN.md phase; (g) a phase with sibling VERIFICATION.md and one with NN-MM-SUMMARY.md siblings; (h) a ROADMAP.md stub covering only a phase subset. README.md documents each subtree's quirk. Create linear-responses.ts exporting named full-GqlResponse constants (teamsResponseAge, projectLabelsEmpty, projectLabelsExisting, issueLabelsEmpty, workspaceWithProject, workspaceEmpty, projectIssuesPage, initiativesResponse) in mainResponseClean style — projectIssuesPage carries issue id/title/projectMilestone{id}/labels{nodes{id}} so the resolver's issue read has fixture input; initiativesResponse carries at least one initiative {id,name} so resolveInitiative has fixture input. Create linear-mutation-mock.ts exporting createMutationMock():{fetchFn:typeof fetch, state} that dispatches on GraphQL operation name to an in-memory mutable workspace. CRITICAL (review item 5): each create handler must FIRST run the same resolve-key check the real CLI uses (stored id -> label -> title-hash of the identity key) against the current state and RETURN THE EXISTING id on a duplicate rather than blindly appending — so the idempotency test is a meaningful logic test, not a tautology (real Linear happily creates same-named projects). The same fetchFn answers resolve-side read queries against state.
  </action>
  <verify>
    <automated>test -f scripts/sync-gsd-linear/__fixtures__/linear-responses.ts && test -f scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts && ls scripts/sync-gsd-linear/__fixtures__/planning-trees && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - planning-trees/ contains duplicate-NN, decimal-phase, frontmatter-less, two-generic-plans-in-one-phase, bare-PLAN subtrees + a ROADMAP.md stub
    - grep -c 'export const' scripts/sync-gsd-linear/__fixtures__/linear-responses.ts returns >= 5
    - linear-mutation-mock.ts exports createMutationMock with fetchFn typed typeof fetch
    - A comment or code path in linear-mutation-mock.ts documents the dup-create -> return-existing-id behavior keyed on the resolve order
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>All three Wave-0 fixture sets exist, typecheck, match the full-GqlResponse fixture contract, and the mutation-mock returns the existing id on a duplicate create.</done>
</task>

<task type="auto">
  <name>Task 3: Seed sync.config.json and linear-map.json</name>
  <files>sync.config.json, linear-map.json</files>
  <read_first>
    - .planning/phases/06/06-CONTEXT.md (D-06-04 entries, D-06-05 central map, Specific Ideas target repos)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 4 AGE team; Open Question 2 fx-signal-agent first-run risk)
    - scripts/sync-gsd-linear/config.ts (schemas these files satisfy — note the new required `name` field)
  </read_first>
  <action>
    Create sync.config.json satisfying SyncConfigSchema with three entries (Specific Ideas), each with a `name` (the --project match key): {repoPath ../claude-workflow, name claude-workflow, label roadmap:claude-workflow, teamKey AGE}, {repoPath ../../factiv/cparx, name cparx, label roadmap:cparx, teamKey AGE}, {repoPath ../../factiv/fx-signal-agent, name fx-signal-agent, label roadmap:fx-signal-agent, teamKey AGE}. No token anywhere. Create linear-map.json satisfying LinearMapSchema with all five pools present and empty. Record the fx-signal-agent first-run duplication caveat (Open Question 2) in the SUMMARY, not the data file.
  </action>
  <verify>
    <automated>CI=true npx tsx -e "import {loadSyncConfig,loadLinearMap} from './scripts/sync-gsd-linear/config.ts'; loadSyncConfig('sync.config.json'); loadLinearMap('linear-map.json'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - loadSyncConfig returns an entry with repoPath ../claude-workflow and name claude-workflow without throwing
    - loadLinearMap returns five empty pools without throwing
    - grep -c 'lin_api_' sync.config.json linear-map.json returns 0 for both
  </acceptance_criteria>
  <done>Both data files parse through their schemas; token-free; three repos configured with a name + teamKey AGE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| sibling `.planning/` content -> CLI | Untrusted Markdown/YAML flows through the schemas defined here |
| committed sync.config.json -> CLI | Human-reviewed config; trusted, repo paths validated before walking |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | config/map/model loaders | mitigate | Zod safeParse-then-throw at the boundary; never trust unparsed JSON (V5 Input Validation) |
| T-06-02 | Information Disclosure | sync.config.json / linear-map.json | mitigate | Files never hold a token; acceptance greps `lin_api_` == 0; key read only via process.env in later plans |
| T-06-05 | Spoofing/duplication | mutation-mock fidelity | mitigate | Mock create handlers return the existing id on a dup keyed by the CLI resolve order, so the idempotency test can't pass green while hiding a duplicate-create bug |
| T-06-SC | Tampering | package installs | accept | No packages this phase (RESEARCH Package Legitimacy Audit: n/a — Node builtins + existing zod/tsx) |
</threat_model>

<success_criteria>
- config.ts is the single source of normalized-model, config, map, resolved-state, and operation contracts.
- NormalizedPlan carries key + taskLines; SyncConfigEntry carries name; ResolvedProject carries issue identity; ResolvedWorkspace carries initiativeId; DiffSummary carries the SyncOperation write set.
- All three Wave-0 fixture sets exist and typecheck; the mutation-mock is dup-safe.
- sync.config.json and linear-map.json are committed, schema-valid, token-free.
</success_criteria>

<output>
Create `.planning/phases/06/06-01-SUMMARY.md` when done; record the fx-signal-agent first-run caveat for the apply plan.
</output>
</content>
</invoke>
