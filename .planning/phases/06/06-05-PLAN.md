---
phase: 06
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified: [scripts/sync-gsd-linear/resolve.ts, scripts/sync-gsd-linear/resolve.test.ts]
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Every Linear entity is resolved by the locked order: stored map id -> roadmap:<repo> label -> title-hash, and only reported missing if all three miss"
    - "The roadmap:<repo> label is resolved in BOTH pools (ProjectLabel and IssueLabel) because they are distinct Linear types"
    - "A Linear Team is resolved before any create is contemplated (teamIds/teamId are required)"
    - "A configured Initiative NAME is resolved to its Linear id (fail-closed if named-but-missing), and left null when the entry names no initiative — so apply's initiative-join has a real ID, not a bare name"
    - "The resolved read surface carries issue identity (id, title, identityKey, milestoneId, labelIds) so issue dedup / second-run-no-op is expressible"
    - "Re-resolving after a create finds the existing record — no duplicate"
  artifacts:
    - path: "scripts/sync-gsd-linear/resolve.ts"
      provides: "resolveTeam, resolveLabels (both pools), resolveInitiative, resolveProject, resolveMilestone, resolveIssue + paginated issue read; honors map->label->hash order"
      contains: "export async function resolveProject"
  key_links:
    - from: "scripts/sync-gsd-linear/resolve.ts"
      to: "scripts/linear/fetch-workspace.ts"
      via: "reuses fetchAssembledWorkspace + mapWorkspace for base workspace read"
      pattern: "fetchAssembledWorkspace"
---

<objective>
Build the SYNC-02 resolver: for each Linear write target (Team, ProjectLabel, IssueLabel,
Initiative, Project, ProjectMilestone, Issue) look up the existing record by the locked
resolve order (stored linear-map.json id -> roadmap:<repo> label -> title-hash) and report
what is missing. Resolve-before-create is what makes SYNC-04's "re-run is a no-op" true.
Per the cross-AI review, the resolver now reads issue identity (id/title/milestone/labels)
via the paginated target-scoped PROJECT_ISSUES_QUERY into ResolvedProject.issues, so the
diff can dedup issues. Per D-06-02, when a config entry names an Initiative it is resolved
to its Linear id (name -> id) so apply's initiative-join has a real ID. Reuse the existing
read plumbing; new lookups use the typed queries from mutations.ts. Injected fetchFn keeps
every function unit-testable.

Output: resolve.ts (+test, mocked fetch + mutation-mock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06/06-CONTEXT.md
@.planning/phases/06/06-RESEARCH.md
@.planning/phases/06/06-PATTERNS.md

<interfaces>
From scripts/linear/fetch-workspace.ts: fetchAssembledWorkspace(fetchFn: typeof fetch, endpoint: string, authHeader: string): Promise<GqlResponse>. From scripts/linear/map.ts: mapWorkspace(json): RawWorkspace ({initiatives[], projects[]} — initiatives have id, name; projects have id, name, projectMilestones{nodes:[{id,name,targetDate}]}). From scripts/sync-gsd-linear/mutations.ts (06-03): TEAMS_QUERY, PROJECT_LABELS_QUERY, ISSUE_LABELS_QUERY, PROJECT_ISSUES_QUERY (paginated, target-scoped: id/title/projectMilestone{id}/labels) + typed payloads. From config.ts: LinearMap, ResolvedWorkspace (teamId, project, projectLabelId, issueLabelId, initiativeId), ResolvedProject, ResolvedMilestone, ResolvedIssue, NormalizedModel, SyncConfigEntry (entry.initiative is a bare NAME). From hash.ts: titleHash. Error convention (fetch-workspace.ts): throw new Error(`Linear <thing> failed: ${status}`) / `Linear GraphQL errors (<phase>): ...`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve.ts — team + both label pools</name>
  <files>scripts/sync-gsd-linear/resolve.ts, scripts/sync-gsd-linear/resolve.test.ts</files>
  <read_first>
    - scripts/linear/fetch-workspace.ts (injected-fetch signature, variables-not-interpolation, error-message conventions, pagination loop shape)
    - scripts/sync-gsd-linear/mutations.ts (TEAMS_QUERY, PROJECT_LABELS_QUERY, ISSUE_LABELS_QUERY + payload types)
    - .planning/phases/06/06-RESEARCH.md (Pitfall 2 two label pools; Pitfall 4 team required; Code Examples resolve-a-team-by-key)
    - scripts/sync-gsd-linear/__fixtures__/linear-responses.ts (teamsResponseAge, projectLabelsExisting/Empty, issueLabels)
  </read_first>
  <behavior>
    - resolveTeam(fetchFn, endpoint, auth, teamKey) returns the team id via TEAMS_QUERY {key} variable; throws a clear error if the configured key does not resolve.
    - resolveLabels(fetchFn, endpoint, auth, labelName) queries BOTH PROJECT_LABELS_QUERY and ISSUE_LABELS_QUERY by name and returns {projectLabelId, issueLabelId}, each null when the pool has no match.
    - All lookups pass values through GraphQL variables, never interpolation.
  </behavior>
  <action>
    Create resolve.ts. Export async resolveTeam(fetchFn: typeof fetch, endpoint: string, auth: string, teamKey: string): Promise<string> using TEAMS_QUERY with variables {key: teamKey}; throw `Linear team "${teamKey}" not found` if empty. Export async resolveLabels(fetchFn, endpoint, auth, labelName): Promise<{projectLabelId: string | null; issueLabelId: string | null}> issuing both label-pool queries with variables {name: labelName}. Reuse fetch-workspace error-message phrasing. No process/fs; fetchFn injected. Write resolve.test.ts (test-first) with vi.stubGlobal-style injected fetchFn using linear-responses fixtures: team resolves to AGE id; missing team throws; both label pools resolved independently (project pool has it, issue pool empty -> {projectLabelId: id, issueLabelId: null}). Include one test using a SECOND teamKey to prove per-entry team override resolves a different team (closes RESEARCH Assumption A1).
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/resolve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/resolve.test.ts exits 0
    - Test proves resolveLabels queries both pools and can return one id + one null
    - Test proves a second teamKey resolves a different team id (per-entry override)
    - grep -c 'export async function resolveTeam\|export async function resolveLabels' scripts/sync-gsd-linear/resolve.ts returns 2
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>Team + both label pools resolve via variables-only queries with clear errors; per-entry team override proven; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve.ts — initiative, project, milestone, issue (map -> label -> hash) + issue read surface</name>
  <files>scripts/sync-gsd-linear/resolve.ts, scripts/sync-gsd-linear/resolve.test.ts</files>
  <read_first>
    - scripts/linear/map.ts + scripts/linear/fetch-workspace.ts (mapWorkspace + fetchAssembledWorkspace reuse; mapWorkspace already returns {initiatives, projects}; cursor-pagination loop shape to copy for PROJECT_ISSUES_QUERY)
    - scripts/sync-gsd-linear/hash.ts (titleHash for the fallback step — phase slug for milestones, plan key for issues)
    - scripts/sync-gsd-linear/mutations.ts (PROJECT_ISSUES_QUERY + ProjectIssuesPage payload)
    - .planning/phases/06/06-CONTEXT.md (D-06-02 initiative-attach: optional entry.initiative names an existing Initiative to attach the newly-created project to; D-06-03 resolve order)
    - .planning/phases/06/06-RESEARCH.md (Pattern 2 resolve-then-create, idempotent apply pseudocode; INITIATIVE_TO_PROJECT_CREATE needs initiativeId: String!; mapWorkspace returns {initiatives, projects})
    - .planning/phases/06/06-REVIEWS.md Consensus item 1 (resolved read surface must carry issue identity)
    - scripts/sync-gsd-linear/__fixtures__/linear-mutation-mock.ts (re-resolve-after-create path); linear-responses.ts (initiativesResponse for resolveInitiative)
  </read_first>
  <behavior>
    - resolveInitiative(workspace, name) returns a Linear Initiative id by matching workspace.initiatives (already read via mapWorkspace) by name. Returns null when name is unset (entry has no initiative). Throws a clear `Linear initiative "<name>" not found` (fail-closed, mirroring resolveTeam) when a name IS configured but matches no initiative in the workspace. Never interpolates — it matches over the already-read workspace.
    - resolveProject(workspace, map, entry) returns a ResolvedProject or null using order: map.projects[repoKey].id -> project carrying the roadmap:<repo> label -> project whose name === entry.projectName. Only null if all three miss. The returned ResolvedProject.issues is populated from a paginated PROJECT_ISSUES_QUERY read of the resolved project.
    - buildResolvedWorkspace reads the project's issues via the cursor-paginated PROJECT_ISSUES_QUERY and maps each into ResolvedIssue{id, title, identityKey: null (unknown from Linear — matched by titleHash of the plan key against titleHash(issue.title) is NOT used; instead the map/label/hash resolve keys on the stored map id and the title-hash of the issue's own title where the CLI created it), projectId, milestoneId, labelIds}. (identityKey stays null for issues the CLI did not create; dedup relies on the stored map id first, then titleHash match — see resolveIssue.)
    - resolveMilestone / resolveIssue resolve within a project by map key -> titleHash match against existing milestone/issue names. resolveMilestone matches titleHash(phase.slug); resolveIssue matches titleHash(plan.key) — the stable identity, never the display title. No duplicate on re-resolve.
    - buildResolvedWorkspace(fetchFn, endpoint, auth, entry, map) composes fetchAssembledWorkspace + mapWorkspace + resolveTeam + resolveLabels + resolveInitiative(workspace, entry.initiative) + resolveProject (with the issue read) into a ResolvedWorkspace, populating ResolvedWorkspace.initiativeId from resolveInitiative.
  </behavior>
  <action>
    Extend resolve.ts. Export resolveInitiative(workspace: RawWorkspace, name: string | undefined): string | null — return null immediately when name is falsy; otherwise find workspace.initiatives by exact name match and return its id, or throw `Linear initiative "${name}" not found` when a configured name has no match (fail-closed, mirroring resolveTeam). Export resolveProject(workspace: RawWorkspace, map: LinearMap, entry: SyncConfigEntry): ResolvedProject | null implementing the D-06-03 three-step order (stored id, then label-bearing project, then name match), building ResolvedProject.milestones from the project's projectMilestones nodes. Add an async readProjectIssues(fetchFn, endpoint, auth, projectId): Promise<ResolvedIssue[]> that runs PROJECT_ISSUES_QUERY in the cursor-pagination loop (copy fetch-workspace.ts's loop + endCursor invariant check) and maps nodes to ResolvedIssue{id, title, identityKey: null, projectId, milestoneId: projectMilestone?.id ?? null, labelIds: labels.nodes.map(l=>l.id)}. Export resolveMilestone(project: ResolvedProject, phaseSlug: string, map: LinearMap): string | null (map key -> titleHash(phaseSlug) match) and resolveIssue(project: ResolvedProject, planKey: string, map: LinearMap): string | null (map key -> titleHash(planKey) match against project.issues by titleHash(issue.title); only the CLI-created issues will hash-match, which is correct for its own re-run). Export async buildResolvedWorkspace(...) that reads the base workspace via fetchAssembledWorkspace+mapWorkspace, resolves team + both labels + initiative (resolveInitiative(workspace, entry.initiative)), resolves the project, populates its issues via readProjectIssues, and assembles the ResolvedWorkspace {teamId, project, projectLabelId, issueLabelId, initiativeId}. Extend resolve.test.ts using the mutation-mock + initiativesResponse fixture: (a) a configured initiative name resolves to its Linear id; (b) an unset initiative (entry.initiative undefined) -> null; (c) a configured-but-missing initiative name throws `Linear initiative "<name>" not found`; plus the existing project/milestone/issue coverage — an empty map+empty workspace resolves project null; after the mock creates the project+milestone+issue, a second resolveProject/resolveMilestone/resolveIssue against the now-populated mock finds them (no duplicate); the resolved project carries its issues[] with id/milestoneId/labelIds.
  </action>
  <verify>
    <automated>CI=true npx vitest run scripts/sync-gsd-linear/resolve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - CI=true npx vitest run scripts/sync-gsd-linear/resolve.test.ts exits 0
    - Test proves a configured initiative name resolves to its id, an unset initiative -> null, and a configured-but-missing name throws
    - Test proves the map-id step wins over the label step when a stored id exists
    - Test proves the resolved project carries issues[] with id + milestoneId + labelIds
    - Test proves re-resolving an issue after a mock create returns the existing id (no second create)
    - grep -c 'export function resolveInitiative\|export async function resolveInitiative' scripts/sync-gsd-linear/resolve.ts returns 1
    - grep -c 'titleHash' scripts/sync-gsd-linear/resolve.ts returns >= 1
    - grep -c 'PROJECT_ISSUES_QUERY' scripts/sync-gsd-linear/resolve.ts returns >= 1
    - npx tsc -b --noEmit exits 0
  </acceptance_criteria>
  <done>Initiative name->id (fail-closed, null-when-unset) plus project/milestone/issue resolve honor map->label->hash order, the resolved surface carries issue identity via a paginated read and initiativeId, and re-resolve is idempotent; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| untrusted slugs/headings/keys -> Linear read queries | Values become GraphQL variables in label/name/project-issue lookups |
| configured initiative NAME -> initiative match | Name matched over already-read workspace, never interpolated into a query |
| LINEAR_API_KEY -> outbound Authorization header | Passed as `auth` param, never logged |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | resolve queries | mitigate | All {key}/{name}/{projectId} lookups pass via GraphQL variables (reuse fetch-workspace discipline); never interpolate slugs; resolveInitiative matches over the already-read workspace |
| T-06-02 | Information Disclosure | auth header | mitigate | auth passed as a parameter, never console.logged; error messages use status/phase/name only, not the header |
| T-06-05 | Spoofing/duplication | resolve-before-create | mitigate | Locked map->label->hash order + two-pool label resolution + issue identity read + fail-closed initiative resolve prevents duplicate/mis-attached records (SYNC-02) |
| T-06-SC | Tampering | package installs | accept | No packages this phase |
</threat_model>

<success_criteria>
- SYNC-02 satisfied: all entity types resolve by the locked order with two-pool label handling, team resolution, initiative name->id (fail-closed), and an issue-identity read surface.
- ResolvedWorkspace.initiativeId is populated (id when named, null when unset) so apply's initiative-join has a real ID.
- Re-resolve after create is idempotent (mutation-mock proves it), including issues.
- resolve.ts is green and typecheck-clean; no any.
</success_criteria>

<output>
Create `.planning/phases/06/06-05-SUMMARY.md` when done.
</output>
</content>
</output>
