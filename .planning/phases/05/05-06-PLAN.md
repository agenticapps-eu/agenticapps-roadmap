---
phase: 05-overview-dashboard
plan: 06
type: execute
wave: 3
depends_on: [05-04, 05-05]
files_modified:
  - src/pages/OverviewPage.tsx
autonomous: false            # 05-06 Task 2 is a human-verify checkpoint (Path B UAT)
requirements: [OV-01, OV-02, OV-03, OV-04]
must_haves:
  truths:
    - "OverviewPage replaces the placeholder and renders from the root loader snapshot with zero network."
    - "It wires FilterBar + selectors: decodeFilters(searchParams) → resolveRange → applyFilters → computeKpis/rollupInitiativeHealth over the FILTERED projects."
    - "A per-project list surface renders the FILTERED projects; clicking a project sets ?project=<id> (opening the drill-down) and each row mounts <SyncBadge project={p}/>."
    - "KpiCards, HealthStrip, FilterBar, the project list, and ProjectDrillDownDialog are all mounted; the empty state renders when the filtered project list is empty."
    - "The full test suite, typecheck, build, and lint are green."
  artifacts:
    - path: "src/pages/OverviewPage.tsx"
      provides: "assembled Overview route wiring selectors + all overview components + per-project list with SyncBadge"
      min_lines: 50
  key_links:
    - from: "src/pages/OverviewPage.tsx"
      to: "src/lib/overview/selectors.ts"
      via: "decodeFilters → resolveRange → applyFilters → computeKpis/rollupInitiativeHealth"
      pattern: "applyFilters|computeKpis"
    - from: "src/pages/OverviewPage.tsx"
      to: "src/components/overview/ProjectDrillDownDialog.tsx"
      via: "mounted with loader data; ?project drives open state"
      pattern: "ProjectDrillDownDialog"
    - from: "src/pages/OverviewPage.tsx"
      to: "src/components/overview/SyncBadge.tsx"
      via: "mounts <SyncBadge project={p}/> on each project row (OV-04 render surface #2)"
      pattern: "<SyncBadge"
    - from: "src/pages/OverviewPage.tsx"
      to: "URL ?project param"
      via: "clicking a project row sets ?project=<id> to open the drill-down"
      pattern: "setSearchParams|project"
---

<objective>
Assemble the Overview route: replace the placeholder `OverviewPage.tsx` with the real page
that reads the root loader snapshot, decodes filters from the URL, applies them, computes the
KPIs/health over the FILTERED projects, renders a per-project list (the click surface that
opens the `?project` drill-down and displays the OV-04 SyncBadge per row), and mounts
`FilterBar`, `KpiCards`, `HealthStrip`, and `ProjectDrillDownDialog`. This is the wiring wave —
all logic (05-02) and components (05-04, 05-05) already exist and are verified; this plan
connects them.

Purpose: closes OV-01..OV-04 as a working route. The per-project list is what actually
triggers the drill-down (sets `?project`) and is the primary OV-04 badge surface: an
out-of-sync project shows `<SyncBadge>` in the list once Phase 6 populates `planAhead` — so the
badge is not orphaned. Follows TimelinePage's loader-wiring skeleton (no new route — D-05-04/05),
reuses RoadmapBoundaries via the router for loading/error, and renders the empty state when
filters yield zero projects.
Output: a single replaced `src/pages/OverviewPage.tsx`, plus a green full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05/05-CONTEXT.md
@.planning/phases/05/05-PATTERNS.md

<interfaces>
<!-- Everything this page composes (from prior 05 plans + existing code). -->
import { useRouteLoaderData, useSearchParams } from "react-router-dom";
import type { RoadmapLoaderData } from "@/lib/roadmap/loader";
import { decodeFilters, resolveRange, applyFilters, computeKpis, rollupInitiativeHealth }
  from "@/lib/overview/selectors";
import { KpiCards } from "@/components/overview/KpiCards";
import { HealthStrip } from "@/components/overview/HealthStrip";
import { FilterBar } from "@/components/overview/FilterBar";
import { ProjectDrillDownDialog } from "@/components/overview/ProjectDrillDownDialog";
import { SyncBadge } from "@/components/overview/SyncBadge";

<!-- Loader-wiring skeleton to copy verbatim (TimelinePage.tsx:13-26, 28-38, 68-69): -->
const loaderData = useRouteLoaderData("root") as RoadmapLoaderData | null;
if (!loaderData) return <p ...>Could not load roadmap data. Switch to Snapshot mode above.</p>;
const { data } = loaderData;
// empty state: centered emoji + "No projects found" + muted hint
// root wrapper: <section aria-label="Overview"> with an <h1>
<!-- Named export `function OverviewPage()` — router.tsx imports it by name; do NOT add a route. -->
<!-- Open drill-down: setSearchParams((prev) => { prev.set("project", p.id); return prev; }) — PUSH
     (no replace) so Back closes it; thread prev so filter params survive (Pitfall 2 idiom). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace OverviewPage — wire selectors + project list + all overview components</name>
  <files>src/pages/OverviewPage.tsx</files>
  <read_first>
    - src/pages/OverviewPage.tsx (current placeholder to replace)
    - src/pages/TimelinePage.tsx (loader-wiring + empty-state + section/h1 skeleton to copy)
    - src/lib/overview/selectors.ts (decodeFilters, resolveRange, applyFilters, computeKpis, rollupInitiativeHealth)
    - src/components/overview/SyncBadge.tsx (mounted per project row — OV-04 surface #2)
    - src/router.tsx (confirm index route imports OverviewPage by name; no route change)
    - .planning/phases/05/05-PATTERNS.md § "OverviewPage.tsx (REPLACE)"
  </read_first>
  <action>
    Replace the file with `export function OverviewPage()` (named export, no default). Wire the
    pipeline in the derive-then-render order:
      1. `const loaderData = useRouteLoaderData("root") as RoadmapLoaderData | null;` guard null
         with the muted "Could not load…" line (copy TimelinePage).
      2. `const { data } = loaderData; const [searchParams, setSearchParams] = useSearchParams();`
      3. `const filters = decodeFilters(searchParams);`
         `const range = resolveRange(filters);`
         `const filtered = applyFilters(data.projects, filters, range);`
      4. `const kpis = computeKpis(filtered, data.initiatives.length);`
         `const health = rollupInitiativeHealth(filtered, data.initiatives);`
      5. Define `openProject(id)` = `setSearchParams((prev) => { prev.set("project", id); return
         prev; })` (PUSH, no replace, so Back closes; thread prev so filter params survive).
      6. Render `<section aria-label="Overview">` with an `<h1>`, then `<FilterBar data={data} />`,
         `<KpiCards kpis={kpis} />`, `<HealthStrip rows={health} initiatives={data.initiatives} />`,
         then a per-project LIST over `filtered`: each item is a button/row that calls
         `openProject(p.id)` on click AND renders `<SyncBadge project={p} />` inline (so out-of-sync
         projects are visibly flagged in the list — OV-04). Finally mount `<ProjectDrillDownDialog
         data={data} />` once (it self-guards on ?project and reads the same searchParams).
      7. Empty state: when `filtered.length === 0`, render the centered emoji + "No projects
         found" + muted hint block (copy TimelinePage:28-38) IN PLACE OF the KPI/health/list section,
         but keep FilterBar visible so the user can clear filters. (KPIs computed over `filtered`
         reflect the active filter — OV-02.)
    Do NOT add a new route, do NOT add inline loading/error (RoadmapBoundaries handles those via
    the router — D-05-06). Each project row must be keyboard-activatable (a real <button> or
    role/tabIndex + onKeyDown). Tailwind v4 `bg-(--color-*)` token syntax; responsive + dark mode
    via tokens. Pass computed selector results down as props (dumb components) — do not recompute
    in children.
  </action>
  <acceptance_criteria>
    - `grep -n "applyFilters" src/pages/OverviewPage.tsx` and `computeKpis` and `rollupInitiativeHealth` all match — KPIs/health computed over the FILTERED list.
    - `grep -c "<SyncBadge" src/pages/OverviewPage.tsx` returns >= 1 (badge mounted per project row — OV-04 render surface #2; badge is not orphaned).
    - `grep -n "\"project\"" src/pages/OverviewPage.tsx` shows a project row click setting `?project` (opens the drill-down).
    - FilterBar, KpiCards, HealthStrip, the per-project list, and ProjectDrillDownDialog are all mounted.
    - Empty state renders when `filtered.length === 0` while FilterBar stays visible.
    - Named `export function OverviewPage` (no default); no new route added to router.tsx.
    - `CI=true npx vitest run` (full suite), `npx tsc -b --noEmit`, `CI=true npx vite build`, `npx eslint .` all green.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "<SyncBadge" src/pages/OverviewPage.tsx && CI=true npx vitest run && npx tsc -b --noEmit && CI=true npx vite build && npx eslint .</automated>
  </verify>
  <done>OverviewPage renders the assembled dashboard (KPIs + health + filters + per-project list with SyncBadge + drill-down) over the filtered snapshot with zero network; SyncBadge is mounted (not orphaned); full suite, typecheck, build, and lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete Overview route: KPI cards + per-initiative health strip (OV-01), URL-shareable
    filters (OV-02), a per-project list that opens the ?project drill-down dialog with guarded
    Linear deep-link (OV-03), and the out-of-sync badge UI mounted on both the project list rows
    and the dialog header (OV-04, invisible until Phase 6 populates planAhead). There is no
    React-render harness (D-05-06 Path B), so visual/interaction fidelity needs a human eye.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open the Overview route (`/`).
    2. KPIs: confirm the five KPI cards (initiatives, projects, scheduled vs undated, by-priority,
       by-status) and one health-strip row per initiative render from the snapshot; a color chip
       + backlog/started/done bar per row; an "Unassigned" row only if null-initiative projects exist.
    3. Filters: pick an initiative, a status, a priority, and a quarter preset; confirm the KPI
       numbers + health strip + project list update and the URL gains ?initiative/?status/?priority/?quarter.
       COPY the URL, reload → the same filters are still applied (shareable + survives reload).
       Set a custom from/to → confirm it overrides the quarter; confirm undated projects drop out
       when a range is active. Clear filters → params disappear, full data returns.
    4. Drill-down: click a project row → a dialog opens showing counts + milestones + (if the snapshot
       has a linear.app url) an "Open in Linear ↗" link; the URL gains ?project=<id> ALONGSIDE any
       active filters. Reload with that URL → the dialog re-opens. Close it → ?project is removed and
       Back does not re-open it; filters remain intact. Hand-edit ?project to a garbage id → NO dialog,
       no crash.
    5. Check dark mode + a narrow (mobile) width render acceptably.
    6. OV-04 badge: expected INVISIBLE on both the list rows and the dialog header (planAhead absent in
       the current snapshot) — confirm no badge and no error. Optional smoke test: temporarily set
       `"planAhead": true` on one project in `public/roadmap.json`, reload, and confirm the
       "Out of sync with plan" badge appears in that project's list row AND in its drill-down header;
       then revert the edit. (It lights up for real in Phase 6.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams -> page render | Filter + ?project params (attacker-controllable) drive the whole page; all consumed via the defensive selectors (05-02) and guarded dialog (05-05). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | DoS | OverviewPage filter pipeline | mitigate | All URL parsing is delegated to the tested defensive `decodeFilters`/`applyFilters`; malformed params yield a well-formed (possibly empty) result → empty state, never a crash. |
| T-05-08 | Tampering | project-row click sets ?project | mitigate | `openProject` writes only a known `p.id` from the in-memory snapshot (never free user input); the dialog re-guards the id against `data.projects` before rendering. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH § Package Legitimacy Audit — zero external packages). |
</threat_model>

<verification>
- Phase gate: `CI=true npx vitest run` + `npx tsc -b --noEmit` + `CI=true npx vite build` + `npx eslint .` all green, plus the grep that `<SyncBadge` is mounted in OverviewPage.
- Human-UAT checkpoint confirms OV-01..OV-04 visual/interaction fidelity (Path B), including the OV-04 badge appearing when planAhead is truthy.
</verification>

<success_criteria>
- OV-01..OV-04 delivered: a working Overview dashboard rendering from the snapshot with shareable filters, a per-project drill-down, and the out-of-sync badge mounted on real surfaces (list rows + dialog header) ready for Phase-6 data.
</success_criteria>

<output>
Create `.planning/phases/05/05-06-SUMMARY.md` when done.
</output>
