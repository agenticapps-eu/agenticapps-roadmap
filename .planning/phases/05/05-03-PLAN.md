---
phase: 05-overview-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/card.tsx
autonomous: true
requirements: [OV-01, OV-03]
must_haves:
  truths:
    - "dialog and card components exist in src/components/ui/ and compile."
    - "Neither scaffolded file imports from @radix-ui — dialog resolves to @base-ui/react (zero new npm deps)."
    - "package.json and pnpm-lock.yaml are UNCHANGED by the scaffold (no dependency drift) — asserted by a git manifest-diff check."
    - "If the registry is unreachable, the dialog is hand-authored from the verified base-ui fallback AND typechecks; a flagged-but-missing primitive is a BLOCKING failure, never a passing plan."
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "base-ui Dialog primitive (Root/Portal/Backdrop/Popup/Title/Description/Close) for the drill-down container"
    - path: "src/components/ui/card.tsx"
      provides: "Card container for KPI cards"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "@base-ui/react/dialog"
      via: "scaffolded import"
      pattern: "@base-ui/react"
---

<objective>
Scaffold the two shadcn UI primitives Phase 5 needs — `dialog` (drill-down container,
D-05-04) and `card` (KPI card container, OV-01) — via the PROJECT-LOCAL shadcn CLI. The project
uses the `base-nova` style, which sources these from `@base-ui/react` (already installed at
v1.6.0), so this adds ZERO new npm dependencies — identical to Phase-4 04-03 which scaffolded
hover-card/popover/badge.

Purpose: OV-03/D-05-04 needs the URL-controlled Dialog; OV-01 uses Card for the KPI grid.
Scaffolding first (Wave 1) unblocks the component plans (05-04, 05-05) — so a missing dialog
MUST block, not pass with a warning (05-REVIEWS finding 7).
Output: two source-only files matching the existing `popover.tsx`/`hover-card.tsx` style,
verified free of Radix, with the dependency manifest provably unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05/05-RESEARCH.md
@.planning/phases/05/05-PATTERNS.md
@.planning/phases/05/05-REVIEWS.md
@.planning/phases/04/04-03-PLAN.md

<interfaces>
<!-- Expected backing primitives after scaffold (VERIFIED in 05-RESEARCH.md § Standard Stack
     and node_modules/@base-ui/react/dialog/index.parts.d.ts). -->
dialog.tsx -> @base-ui/react/dialog (Dialog.Root/Portal/Backdrop/Popup/Title/Description/Close/Trigger)
card.tsx   -> plain token-styled container (no primitive dependency required)
Existing analogs: src/components/ui/popover.tsx, src/components/ui/hover-card.tsx (base-ui, cn + data-slot).
components.json: "style": "base-nova", registries: {} — CLI emits base-ui-wrapped files.

<!-- Project-local shadcn binary VERIFIED present: node_modules/.bin/shadcn (devDep shadcn ^4.11.0).
     Lockfile is pnpm-lock.yaml. base-nova scaffolds NO new deps → both files unchanged after add. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold dialog + card via the local CLI, verify base-ui backing + zero manifest drift</name>
  <files>src/components/ui/dialog.tsx, src/components/ui/card.tsx</files>
  <read_first>
    - src/components/ui/popover.tsx (existing base-ui scaffold — style/import reference for the expected dialog shape)
    - components.json (confirms base-nova style + empty registries)
    - .planning/phases/04/04-03-PLAN.md (the process analog — scaffold-or-flag rules)
    - .planning/phases/05/05-REVIEWS.md (finding 7 — dependency-drift + blocking failure semantics)
    - .planning/phases/05/05-RESEARCH.md § "Supporting — scaffold via shadcn CLI" + § "Hand-authored dialog fallback"
  </read_first>
  <action>
    Capture the pre-scaffold manifest state FIRST: `git diff --quiet -- package.json pnpm-lock.yaml`
    should already be clean (record if not). Then run the PROJECT-LOCAL CLI — `pnpm exec shadcn add
    dialog card` (uses node_modules/.bin/shadcn, VERIFIED present) — NOT bare `npx shadcn` (avoids
    non-deterministic version resolution). Do NOT hand-write the dialog unless the registry is
    unreachable (below). After scaffolding, verify the generated `dialog.tsx` imports from
    `@base-ui/react` (expect `@base-ui/react/dialog`, matching popover.tsx's `@base-ui/react/popover`)
    and NOT from `@radix-ui/*`. If it references `@radix-ui`, the wrong style was fetched — re-run
    with base-nova and confirm no `@radix-ui/*` was added to package.json.

    DEPENDENCY DRIFT (05-REVIEWS finding 7): base-nova adds no runtime deps, so assert
    `git diff --quiet -- package.json pnpm-lock.yaml` (manifest UNCHANGED). If the CLI DID modify
    either file, STOP and document the exact added dependency + reason in the SUMMARY for human
    review — do NOT silently accept dependency drift. (This is a manifest-diff check; package.json
    and pnpm-lock.yaml are deliberately NOT in files_modified because no change is expected.)

    REGISTRY FAILURE (UX-ASSUMPTION A4, SETTLED — BLOCKING): `pnpm exec shadcn add` fetches from the
    shadcn registry and may fail where the executor restricts network/writes. Failure is BLOCKING —
    Wave 2 (05-04, 05-05) cannot proceed without both files:
      - For `dialog`: if the fetch/write fails, hand-author `src/components/ui/dialog.tsx` STRICTLY
        from the verified fallback in 05-RESEARCH.md § "Hand-authored dialog fallback (if CLI offline)"
        — which mirrors popover.tsx against the installed `@base-ui/react/dialog` parts — and it MUST
        typecheck. Do NOT hand-fabricate a Radix-shaped file. If you can neither scaffold NOR
        hand-author a typechecking dialog.tsx, the task FAILS (report failure with the exact CLI error
        + re-run command); a flagged-but-missing primitive does NOT count as plan completion.
      - For `card`: the research explicitly sanctions a plain token-styled `<div>` — if the CLI is
        unavailable, hand-author a minimal `card.tsx` exporting `Card`/`CardHeader`/`CardTitle`/
        `CardContent` as `<div>`s using `cn(...)` and `bg-(--color-card)`/`border-(--color-border)`
        tokens (no primitive needed). This is NOT a Radix risk (no third-party primitive).
    Match the existing `data-slot` + `cn(...)` conventions from popover.tsx either way.
  </action>
  <acceptance_criteria>
    - Both files exist: src/components/ui/dialog.tsx, src/components/ui/card.tsx (BLOCKING — a missing file fails the task).
    - `grep -rl "@radix-ui" src/components/ui/dialog.tsx src/components/ui/card.tsx` returns nothing.
    - `grep -l "@base-ui/react" src/components/ui/dialog.tsx` lists it.
    - `git diff --quiet -- package.json pnpm-lock.yaml` exits 0 (manifest unchanged) — OR any change is explicitly documented in the SUMMARY as an approved dependency addition.
    - `grep -c "@radix-ui" package.json` returns 0 (no new Radix dependency added).
    - `npx tsc -b --noEmit` passes (a hand-authored fallback that does not typecheck is a FAILURE, not a pass).
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/ui/dialog.tsx && test -f src/components/ui/card.tsx && test -z "$(grep -rl '@radix-ui' src/components/ui/dialog.tsx src/components/ui/card.tsx)" && grep -q '@base-ui/react' src/components/ui/dialog.tsx && git diff --quiet -- package.json pnpm-lock.yaml && npx tsc -b --noEmit</automated>
  </verify>
  <done>dialog + card scaffolded from base-ui (no Radix, no new npm dep, manifest unchanged), typecheck clean — OR, if the registry was unreachable, dialog was hand-authored from the verified fallback AND typechecks. A missing/non-typechecking primitive is a BLOCKING failure, never a passing plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry -> local source tree | `shadcn add` writes generated component source; a wrong-style/compromised registry entry could pull unexpected dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | shadcn scaffold of dialog/card | mitigate | Project-local CLI (`pnpm exec shadcn`, pinned devDep 4.11.0) against the official base-nova registry (components.json registries={}); verify generated imports resolve to the already-vetted `@base-ui/react`, that no `@radix-ui/*` (or any new package) is added, and that `git diff --quiet -- package.json pnpm-lock.yaml` confirms ZERO manifest drift. Source-only scaffold; hand-authored fallback restricted to the verified base-ui parts. Zero new npm installs. |
</threat_model>

<verification>
- Both primitives present, dialog base-ui-backed, no Radix, no new dependency (manifest-diff clean), typecheck clean.
- If the registry was unreachable, dialog was hand-authored from the verified fallback AND typechecks — otherwise the plan FAILS (blocks Wave 2).
</verification>

<success_criteria>
- OV-03 drill-down container and OV-01 KPI card container available with zero dependency drift, ready to unblock Wave 2.
</success_criteria>

<output>
Create `.planning/phases/05/05-03-SUMMARY.md` when done.
</output>
