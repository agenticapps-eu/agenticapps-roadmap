---
phase: 05-overview-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/ui/card.tsx
autonomous: true
requirements: [OV-01, OV-03]
must_haves:
  truths:
    - "dialog and card components exist in src/components/ui/ and compile."
    - "Neither scaffolded file imports from @radix-ui — dialog resolves to @base-ui/react (zero new npm deps)."
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "base-ui Dialog primitive (Root/Portal/Backdrop/Popup/Title/Description/Close) for the drill-down container"
    - path: "src/components/ui/card.tsx"
      provides: "Card container for KPI cards"
  key_links:
    - from: "src/components/ui/dialog.tsx"
      to: "@base-ui/react/dialog"
      via: "scaffolded import"
      pattern: "@base-ui/react"
---

<objective>
Scaffold the two shadcn UI primitives Phase 5 needs — `dialog` (drill-down container,
D-05-04) and `card` (KPI card container, OV-01) — via the shadcn CLI. The project uses the
`base-nova` style, which sources these from `@base-ui/react` (already installed at v1.6.0),
so this adds ZERO new npm dependencies — identical to Phase-4 04-03 which scaffolded
hover-card/popover/badge.

Purpose: OV-03/D-05-04 needs the URL-controlled Dialog; OV-01 uses Card for the KPI grid.
Scaffolding first (Wave 1) unblocks the component plans (05-04, 05-05).
Output: two source-only files matching the existing `popover.tsx`/`hover-card.tsx` style,
verified free of Radix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05/05-RESEARCH.md
@.planning/phases/05/05-PATTERNS.md
@.planning/phases/04/04-03-PLAN.md

<interfaces>
<!-- Expected backing primitives after scaffold (VERIFIED in 05-RESEARCH.md § Standard Stack
     and node_modules/@base-ui/react/dialog/index.parts.d.ts). -->
dialog.tsx -> @base-ui/react/dialog (Dialog.Root/Portal/Backdrop/Popup/Title/Description/Close/Trigger)
card.tsx   -> plain token-styled container (no primitive dependency required)
Existing analogs: src/components/ui/popover.tsx, src/components/ui/hover-card.tsx (base-ui, cn + data-slot).
components.json: "style": "base-nova", registries: {} — CLI emits base-ui-wrapped files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold dialog + card and verify base-ui backing</name>
  <files>src/components/ui/dialog.tsx, src/components/ui/card.tsx</files>
  <read_first>
    - src/components/ui/popover.tsx (existing base-ui scaffold — style/import reference for the expected dialog shape)
    - components.json (confirms base-nova style + empty registries)
    - .planning/phases/04/04-03-PLAN.md (the process analog — scaffold-or-flag rules)
    - .planning/phases/05/05-RESEARCH.md § "Supporting — scaffold via shadcn CLI" + § "Hand-authored dialog fallback"
  </read_first>
  <action>
    Run `npx shadcn add dialog card` to scaffold both into `src/components/ui/`. Do NOT
    hand-write the dialog. After scaffolding, verify the generated `dialog.tsx` imports from
    `@base-ui/react` (expect `@base-ui/react/dialog`, matching popover.tsx's
    `@base-ui/react/popover`) and NOT from `@radix-ui/*`. If it references `@radix-ui`, the
    wrong style was fetched — re-run with base-nova and confirm no `@radix-ui/*` was added to
    package.json.

    Network fallback (UX-ASSUMPTION A4, SETTLED): `npx shadcn add` fetches from the shadcn
    registry and may fail where the executor restricts network/writes.
      - For `dialog`: if the fetch/write fails, do NOT silently proceed and do NOT hand-fabricate
        a Radix-shaped file. Either (a) hand-author `src/components/ui/dialog.tsx` STRICTLY from
        the verified fallback in 05-RESEARCH.md § "Hand-authored dialog fallback (if CLI offline)"
        — which mirrors popover.tsx against the installed `@base-ui/react/dialog` parts — OR
        (b) if you cannot, FLAG the failure in the SUMMARY as manual-intervention-required with
        the exact CLI error + re-run command. The verify below fails until the file exists.
      - For `card`: the research explicitly sanctions a plain token-styled `<div>` — if the CLI
        is unavailable, hand-author a minimal `card.tsx` exporting `Card`/`CardHeader`/`CardTitle`/
        `CardContent` as `<div>`s using `cn(...)` and `bg-(--color-card)`/`border-(--color-border)`
        tokens (no primitive needed). This is NOT a Radix risk (no third-party primitive).
    Match the existing `data-slot` + `cn(...)` conventions from popover.tsx either way.
  </action>
  <acceptance_criteria>
    - Both files exist: src/components/ui/dialog.tsx, src/components/ui/card.tsx.
    - `grep -rl "@radix-ui" src/components/ui/dialog.tsx src/components/ui/card.tsx` returns nothing.
    - `grep -l "@base-ui/react" src/components/ui/dialog.tsx` lists it.
    - `grep -c "@radix-ui" package.json` returns 0 (no new Radix dependency added).
    - If the registry was unreachable AND the verified hand-authored fallback was not applied, the task is FLAGGED in the SUMMARY as manual-intervention-required (CLI error + re-run command) rather than reported passing.
    - `npx tsc -b --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/ui/dialog.tsx && test -f src/components/ui/card.tsx && test -z "$(grep -rl '@radix-ui' src/components/ui/dialog.tsx src/components/ui/card.tsx)" && grep -q '@base-ui/react' src/components/ui/dialog.tsx && npx tsc -b --noEmit</automated>
  </verify>
  <done>dialog + card scaffolded from base-ui (no Radix, no new npm dep), typecheck clean — OR, if the registry was unreachable, dialog was hand-authored from the verified fallback, OR the task is explicitly flagged for manual intervention (not silently passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry -> local source tree | `npx shadcn add` writes generated component source; a wrong-style/compromised registry entry could pull unexpected dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | shadcn scaffold of dialog/card | mitigate | Official shadcn registry only (components.json registries={}); verify generated imports resolve to the already-vetted `@base-ui/react` and that no `@radix-ui/*` (or any new package) is added to package.json. Source-only scaffold; hand-authored fallback restricted to the verified base-ui parts. Zero new npm installs. |
</threat_model>

<verification>
- Both primitives present, dialog base-ui-backed, no Radix, no new dependency, typecheck clean.
- If the registry was unreachable, dialog was hand-authored from the verified fallback OR flagged for manual intervention.
</verification>

<success_criteria>
- OV-03 drill-down container and OV-01 KPI card container available with zero dependency drift.
</success_criteria>

<output>
Create `.planning/phases/05/05-03-SUMMARY.md` when done.
</output>
