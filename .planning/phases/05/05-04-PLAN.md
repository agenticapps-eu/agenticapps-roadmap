---
phase: 05-overview-dashboard
plan: 04
type: execute
wave: 2
depends_on: [05-02, 05-03]
files_modified:
  - src/components/overview/KpiCards.tsx
  - src/components/overview/HealthStrip.tsx
autonomous: true
requirements: [OV-01]
must_haves:
  truths:
    - "KpiCards renders the five KPIs (initiatives, projects, scheduled vs undated, by-priority, by-status) from a Kpis object passed as a prop, labeling priorities via the canonical PRIORITY_LABELS imported from selectors.ts."
    - "HealthStrip renders one row per InitiativeHealth (incl. the Unassigned row when present) with a color chip and a backlog/started/done stacked bar."
  artifacts:
    - path: "src/components/overview/KpiCards.tsx"
      provides: "presentational KPI card grid consuming Kpis"
    - path: "src/components/overview/HealthStrip.tsx"
      provides: "per-initiative health strip consuming InitiativeHealth[]"
  key_links:
    - from: "src/components/overview/KpiCards.tsx"
      to: "src/lib/overview/selectors.ts"
      via: "import type { Kpis } + import { PRIORITY_LABELS }"
      pattern: "@/lib/overview/selectors"
    - from: "src/components/overview/HealthStrip.tsx"
      to: "src/lib/timeline/colorUtils.ts"
      via: "resolveInitiativeColor for the color chip"
      pattern: "resolveInitiativeColor"
---

<objective>
Build the thin presentational OV-01 components: `KpiCards` (the five KPI tiles) and
`HealthStrip` (one row per initiative). All logic already lives in the tested `selectors.ts`
(05-02) — these components are dumb: they receive already-computed `Kpis` /
`InitiativeHealth[]` as props and render, reusing the Phase-4 stacked-bar + color-chip
patterns.

(NOTE: the OV-04 `SyncBadge` component moved to 05-05 — it is created there so the drill-down
dialog can render it in its header within one plan, and it is reused by the OverviewPage
per-project list in 05-06. SyncBadge is NOT built in this plan.)

Purpose: OV-01 (KPI cards + health strip). No React-render harness exists (D-05-06 Path B),
so correctness of the underlying numbers is covered by 05-02's unit tests; these components'
visual fidelity is human-UAT. Verification here is compile-level.
Output: two presentational components under `src/components/overview/`.

(depends_on is [05-02, 05-03] only — neither component reads `planAhead`, so 05-01 is NOT a
dependency; dropping it removes needless critical-path coupling — 05-REVIEWS LOW finding.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05/05-RESEARCH.md
@.planning/phases/05/05-PATTERNS.md
@.planning/phases/05/05-REVIEWS.md

<interfaces>
<!-- Consumed contracts (from 05-02 selectors.ts + 05-03 card.tsx + existing code). -->
import type { Kpis, InitiativeHealth } from "@/lib/overview/selectors";
import { PRIORITY_LABELS } from "@/lib/overview/selectors";  // canonical 0..4 map (05-REVIEWS finding 4) — DO NOT declare a local copy
import type { RoadmapJson } from "@/lib/roadmap/schema";
import { resolveInitiativeColor } from "@/lib/timeline/colorUtils";  // (initiative, allInitiatives) => hex
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";  // from 05-03 (names may vary — read the scaffold)

<!-- Reuse patterns (copy, do NOT modify the timeline files): -->
Stacked bar (ProjectPopoverContent.tsx:45-64): `flex h-2 overflow-hidden rounded-full`, segment
  width `style={{ width: `${(n/total)*100}%` }}`, `total === 0` guard → single bg-(--color-muted) fill,
  accent fills bg-sky-500 (started) / bg-emerald-500 (done), bg-(--color-muted) (backlog).
Color chip (ProjectPopoverContent.tsx:75): inline `style={{ color }}`.
PRIORITY_LABELS: import the canonical `{0:"No priority",1:"Urgent",2:"High",3:"Medium",4:"Low"}`
  from selectors.ts (05-02). Do NOT re-declare a local copy and do NOT import the pre-existing
  off-by-one ProjectPopoverContent copy — the shared selectors export is the single source of truth,
  so a priority=4 project renders "Low" (never the "—" fallback).
Tailwind v4 tokens: bg-(--color-*), text-(--color-muted-foreground), border-(--color-border).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KpiCards — five KPI tiles from a Kpis prop</name>
  <files>src/components/overview/KpiCards.tsx</files>
  <read_first>
    - src/components/ui/card.tsx (the 05-03 scaffold — confirm exported subcomponent names before importing)
    - src/components/timeline/ProjectPopoverContent.tsx (stacked-bar + total-guard patterns to reuse)
    - src/lib/overview/selectors.ts (Kpis shape + the exported PRIORITY_LABELS to import)
    - .planning/phases/05/05-PATTERNS.md § "KpiCards.tsx + HealthStrip.tsx"
  </read_first>
  <action>
    Create `KpiCards` with signature `export function KpiCards({ kpis }: { kpis: Kpis })`.
    Render a responsive grid of Card tiles: (1) Initiatives count, (2) Projects count,
    (3) Scheduled vs Undated (two numbers + a two-segment bar scheduled/undated over
    projects total, reusing the stacked-bar pattern with the total===0 guard), (4) By-priority
    distribution (map kpis.byPriority keys → PRIORITY_LABELS + count, rendered as a segmented
    bar or labeled rows), (5) By-status distribution (map kpis.byStatus → status + count).
    IMPORT `PRIORITY_LABELS` from "@/lib/overview/selectors" (the canonical 0..4 map) — do NOT
    declare a local copy (05-REVIEWS finding 4: one shared domain, no drift). Use Card
    subcomponents from 05-03 (read the scaffold for exact export names — fall back to plain
    token-styled divs if the scaffold differs). Tailwind v4 `bg-(--color-*)` token syntax only
    (never `bg-[var(--color-*)]`). Dumb component: no selector calls, no useSearchParams — it
    only renders the passed `kpis`.
  </action>
  <acceptance_criteria>
    - `export function KpiCards` present; sole data input is a `kpis: Kpis` prop (no data fetch, no hook).
    - `grep -n "import { PRIORITY_LABELS" src/components/overview/KpiCards.tsx` (imported from selectors) AND no locally-declared `const PRIORITY_LABELS` in this file.
    - All five KPIs rendered; scheduled/undated + distribution bars reuse the total===0 guard.
    - `grep -c "bg-\[var(" src/components/overview/KpiCards.tsx` returns 0 (uses bg-(--color-*) form).
    - `npx tsc -b --noEmit` and `npx eslint src/components/overview/KpiCards.tsx` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit && npx eslint src/components/overview/KpiCards.tsx</automated>
  </verify>
  <done>KpiCards renders all five OV-01 KPIs from a Kpis prop using the shared canonical PRIORITY_LABELS, compiles, and lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: HealthStrip — per-initiative rows (incl. Unassigned)</name>
  <files>src/components/overview/HealthStrip.tsx</files>
  <read_first>
    - src/lib/overview/selectors.ts (InitiativeHealth shape — note initiative can be null = Unassigned)
    - src/lib/timeline/colorUtils.ts (resolveInitiativeColor signature)
    - src/components/timeline/InitiativeLane.tsx (row layout analog)
    - src/components/timeline/ProjectPopoverContent.tsx (stacked-bar + total-guard)
    - .planning/phases/05/05-PATTERNS.md § "HealthStrip"
  </read_first>
  <action>
    Create `export function HealthStrip({ rows, initiatives }: { rows: InitiativeHealth[];
    initiatives: RoadmapJson["initiatives"] })`. Render one row per InitiativeHealth: a color
    chip via `resolveInitiativeColor(row.initiative, initiatives)` when row.initiative is
    non-null, else a neutral chip + the literal label "Unassigned" (row.initiative === null);
    the initiative name; the scheduled/undated split; and a backlog/started/done stacked bar
    (reuse the ProjectPopoverContent bar + total===0 guard). Deterministic order: render `rows`
    as given (05-02 already orders + appends Unassigned last). Use `divide-(--color-border)`
    between rows. Dumb component: no selector calls, no hook — renders only the passed props.
  </action>
  <acceptance_criteria>
    - HealthStrip handles a null-initiative row (renders "Unassigned", no crash on resolveInitiativeColor).
    - Backlog/started/done bar reuses the total===0 guard.
    - `npx tsc -b --noEmit` and `npx eslint src/components/overview/HealthStrip.tsx` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit && npx eslint src/components/overview/HealthStrip.tsx</automated>
  </verify>
  <done>HealthStrip renders per-initiative rows incl. the Unassigned case; compiles and lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational-only; all inputs are already-validated snapshot data + tested selector output. No untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | DoS | KpiCards/HealthStrip distribution bars | mitigate | Reuse the `total === 0` guard so an empty distribution renders a neutral bar (no division-by-zero / NaN width). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH § Package Legitimacy Audit — zero external packages). |
</threat_model>

<verification>
- `npx tsc -b --noEmit` + `npx eslint src/components/overview` clean.
- Numeric correctness of KPIs/health is covered by 05-02 selector tests; component visual fidelity is human-UAT (Path B).
</verification>

<success_criteria>
- OV-01 KPI cards + health strip exist as thin presentational components (shared canonical priority labels) ready for OverviewPage assembly (05-06).
</success_criteria>

<output>
Create `.planning/phases/05/05-04-SUMMARY.md` when done.
</output>
