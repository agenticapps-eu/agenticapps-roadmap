---
phase: 05-overview-dashboard
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - src/components/overview/FilterBar.tsx
  - src/components/overview/ProjectDrillDownDialog.tsx
  - src/components/overview/SyncBadge.tsx
autonomous: true
requirements: [OV-02, OV-03, OV-04]
must_haves:
  truths:
    - "FilterBar reads/writes filter state via useSearchParams, delegating all serialization to selectors.ts encode/decode, and preserves co-resident params (?project, ?source) on every change."
    - "FilterBar lets ?quarter and ?from/?to COEXIST in the URL — setting one does NOT clear the other; resolveRange (05-02) gives custom from/to precedence at read time."
    - "ProjectDrillDownDialog opens iff ?project=<id> resolves to a known project; an unknown/absent id renders no dialog."
    - "The drill-down shows issueCounts breakdown + milestones + a guarded linear.app deep-link; it does NOT list individual issues."
    - "Closing the dialog deletes the ?project param (with { replace: true }) and preserves filter params."
    - "SyncBadge renders the 'Out of sync with plan' badge ONLY when project.planAhead is truthy; absent/false/null renders nothing (invisible-not-error)."
    - "The drill-down dialog header mounts <SyncBadge project={project}/>, so an out-of-sync project shows the badge in the dialog once Phase 6 populates planAhead."
  artifacts:
    - path: "src/components/overview/FilterBar.tsx"
      provides: "URL-encoded filter controls (initiative multi-select, time range, status, priority)"
    - path: "src/components/overview/ProjectDrillDownDialog.tsx"
      provides: "?project-controlled base-ui Dialog drill-down; mounts SyncBadge in its header"
    - path: "src/components/overview/SyncBadge.tsx"
      provides: "OV-04 nullish-guarded badge (reused by the OverviewPage project list in 05-06)"
  key_links:
    - from: "src/components/overview/FilterBar.tsx"
      to: "src/lib/overview/selectors.ts"
      via: "decodeFilters(searchParams) / encodeFilters(next, prev) / PRIORITY_LABELS"
      pattern: "encodeFilters|decodeFilters"
    - from: "src/components/overview/ProjectDrillDownDialog.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "URL-controlled Dialog (open + onOpenChange, no Trigger)"
      pattern: "@/components/ui/dialog"
    - from: "src/components/overview/ProjectDrillDownDialog.tsx"
      to: "src/components/overview/SyncBadge.tsx"
      via: "renders <SyncBadge project={project}/> in the dialog header (OV-04 render surface #1)"
      pattern: "<SyncBadge"
    - from: "src/components/overview/SyncBadge.tsx"
      to: "src/lib/roadmap/schema.ts"
      via: "reads the optional project.planAhead field (added in 05-01)"
      pattern: "planAhead"
---

<objective>
Build the three URL-driven / OV-04 components: `FilterBar` (shareable filter controls, OV-02),
`ProjectDrillDownDialog` (the `?project`-controlled drill-down, OV-03), and `SyncBadge` (the
graceful-nullish OV-04 badge). FilterBar and the dialog own `useSearchParams` but delegate ALL
parsing/serialization to the tested `selectors.ts` (05-02). Filter params and the `?project`
param coexist in one URL (D-05-04/D-05-05) via the mutate-and-return-`prev` idiom from AppHeader.

SyncBadge lives here (rather than 05-04) so the drill-down dialog can mount it in its header
within this one plan — no cross-plan dependency needed — AND so the OverviewPage project list
in 05-06 can reuse the SAME component (05-06 already depends on 05-05). This closes the OV-04
seam: the badge is rendered on TWO real surfaces (dialog header here, project list in 05-06),
so when Phase 6 sets `planAhead: true` the badge actually appears (D-05-02).

Purpose: OV-02 (filters shareable + survive reload), OV-03 (drill-down → milestones + guarded
Linear deep-link, no individual issues), OV-04 (badge UI, mounted). No React-render harness
(Path B) — the encode/decode/apply invariants are covered by 05-02; these components'
interaction fidelity is human-UAT. Verification here is compile-level.
Output: `FilterBar.tsx` + `ProjectDrillDownDialog.tsx` + `SyncBadge.tsx` under `src/components/overview/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05/05-RESEARCH.md
@.planning/phases/05/05-PATTERNS.md
@.planning/phases/05/05-REVIEWS.md

<interfaces>
<!-- Consumed contracts (05-01 schema field + 05-02 selectors + 05-03 dialog + existing code). -->
import { useSearchParams } from "react-router-dom";
import { decodeFilters, encodeFilters, PRIORITY_LABELS, type Filters } from "@/lib/overview/selectors";
import type { RoadmapJson, Project } from "@/lib/roadmap/schema";  // Project.planAhead is optional (added 05-01)
import { Dialog, DialogContent, DialogTitle, DialogClose } from "@/components/ui/dialog"; // read the 05-03 scaffold for exact export names
import { Badge } from "@/components/ui/badge";                      // variant: default|secondary|destructive|outline (destructive VERIFIED present)
import { SyncBadge } from "@/components/overview/SyncBadge";        // created in Task 2 of THIS plan

<!-- PRIORITY_LABELS: import the canonical 0..4 map from selectors.ts (05-02) — do NOT declare a
     local copy and do NOT import the pre-existing off-by-one ProjectPopoverContent copy
     (05-REVIEWS finding 4: one shared priority domain, no drift). -->

<!-- URL idiom (AppHeader.tsx:15-29) — MUTATE prev and return it; NEVER `new URLSearchParams()`: -->
setParams((prev) => { prev.set/delete(...); return prev; })
<!-- Multi-value: prev.getAll("initiative") / repeated prev.append("initiative", id). Toggle-to-default deletes the key. -->

<!-- base-ui Dialog onOpenChange arity is (open: boolean, details) — NOT Radix's single arg
     (VERIFIED node_modules/@base-ui/react/dialog/root/DialogRoot.d.ts). Type the handler as
     (open: boolean) => void and derive close from open === false. -->

<!-- Guarded Linear link (ProjectPopoverContent.tsx:94, copy verbatim): -->
{project.url?.startsWith("https://linear.app/") && (
  <a href={project.url} target="_blank" rel="noopener noreferrer">Open in Linear ↗</a>
)}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterBar — URL-encoded filter controls</name>
  <files>src/components/overview/FilterBar.tsx</files>
  <read_first>
    - src/components/AppHeader.tsx (the ONLY existing useSearchParams consumer — copy the setParams(prev => {...; return prev}) idiom)
    - src/lib/overview/selectors.ts (Filters, decodeFilters, encodeFilters, PRIORITY_LABELS)
    - .planning/phases/05/05-RESEARCH.md § "Pattern 2" + § "Pitfall 2 (dropped co-resident params)"
    - .planning/phases/05/05-REVIEWS.md (finding 3 — quarter/custom COEXIST, do NOT clear the other)
    - .planning/phases/05/05-PATTERNS.md § "FilterBar.tsx"
  </read_first>
  <action>
    Create `export function FilterBar({ data }: { data: RoadmapJson })`. Read
    `const [searchParams, setSearchParams] = useSearchParams()` and derive
    `const filters = decodeFilters(searchParams)` on every render (the URL IS the state — no
    useState mirror). Render controls that encode to searchParams:
      - initiative: multi-select over data.initiatives (repeated ?initiative=<id>) — toggling
        appends/removes from filters.initiatives.
      - status: multi-select over the distinct data.projects statuses (repeated ?status=).
      - priority: multi-select over 0..4 (repeated ?priority=, labeled via the imported
        canonical PRIORITY_LABELS from selectors.ts — NOT a local copy).
      - time range: quarter-preset buttons (Claude's-discretion preset set, e.g. current + next
        3 quarters as "YYYY-Qn") writing ?quarter, PLUS a custom from/to (two date inputs)
        writing ?from/?to. COEXISTENCE (05-REVIEWS finding 3, SETTLED): quarter and custom
        from/to may both be present in the URL at once — do NOT clear ?from/?to when a quarter is
        set, and do NOT clear ?quarter when a custom date is set. resolveRange (05-02) gives
        custom from/to precedence at READ time, so any pasted URL is well-defined without a
        write-side mutual-exclusion rule.
      - a "Clear filters" affordance that deletes all six filter keys (preserving ?project/?source).
    Every mutation uses `setSearchParams((prev) => { const next = decodeFilters(prev); /* edit
    next */ return encodeFilters(next, prev); })` — threading `prev` as the base so ?project and
    ?source SURVIVE (Pitfall 2). Delete a key entirely when a dimension goes empty (clean URL).
    Presentational widgets are Claude's discretion; the only hard constraint is that all state
    round-trips through selectors.ts encode/decode. For Path-B accessibility (no render harness),
    give each of the four control groups (initiative, status, priority, time range) and the Clear
    button an explicit `aria-label` / associated label so they are keyboard-operable.
  </action>
  <acceptance_criteria>
    - `grep -n "decodeFilters" src/components/overview/FilterBar.tsx` and `encodeFilters` both match (no inline serialization).
    - `grep -n "PRIORITY_LABELS" src/components/overview/FilterBar.tsx` shows it imported from @/lib/overview/selectors (no local `const PRIORITY_LABELS` declared in this file).
    - `grep -c "new URLSearchParams(" src/components/overview/FilterBar.tsx` returns 0 (always threads prev).
    - Controls exist for all four dimensions (initiative, time range, status, priority) plus a Clear affordance; no code clears ?from/?to when ?quarter is set or vice-versa (coexistence preserved).
    - `npx tsc -b --noEmit` and `npx eslint src/components/overview/FilterBar.tsx` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit && npx eslint src/components/overview/FilterBar.tsx</automated>
  </verify>
  <done>FilterBar drives all four filter dimensions through selectors.ts encode/decode, lets quarter+custom coexist (no write-side clearing), preserves co-resident params, and compiles/lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: SyncBadge (OV-04) + ProjectDrillDownDialog mounting it in the header</name>
  <files>src/components/overview/SyncBadge.tsx, src/components/overview/ProjectDrillDownDialog.tsx</files>
  <read_first>
    - src/components/ui/badge.tsx (Badge variants — `destructive` VERIFIED present at badge.tsx:15; reuse, do not build a new chip)
    - src/components/ui/dialog.tsx (05-03 scaffold — confirm exact export names: Dialog/DialogContent/DialogTitle/DialogClose)
    - src/components/timeline/ProjectPopoverContent.tsx (issueCounts bar + milestones list + guarded Linear link — the body to mirror; also its status/priority Badge header row)
    - src/lib/roadmap/schema.ts (Project.planAhead is optional/nullish — added 05-01)
    - src/lib/overview/selectors.ts (import the canonical PRIORITY_LABELS)
    - .planning/phases/05/05-RESEARCH.md § "Pattern 3: URL-param-controlled base-ui Dialog" + § "Pitfall 1 (onOpenChange arity)" + § "Optional schema field (OV-04)"
    - .planning/phases/05/05-PATTERNS.md § "SyncBadge.tsx" + § "ProjectDrillDownDialog.tsx"
  </read_first>
  <action>
    FIRST create `SyncBadge`: `export function SyncBadge({ project }: { project: Project })`
    returning `project.planAhead ? <Badge variant="destructive">Out of sync with plan</Badge>
    : null`. Exact graceful-nullish guard (D-05-02) — absent/false/null renders nothing, never
    errors. Reuse the existing `Badge` (`destructive` variant VERIFIED present); do NOT build a
    new chip. This single component is the OV-04 render primitive, reused by the dialog header
    (below) and the OverviewPage list (05-06).

    THEN create `export function ProjectDrillDownDialog({ data }: { data: RoadmapJson })`. Read
    `const [searchParams, setSearchParams] = useSearchParams()`, `const projectId =
    searchParams.get("project")`, then guard: `const project = projectId ? data.projects.find(
    (p) => p.id === projectId) ?? null : null`. An unknown/absent id → project === null → NO
    dialog (D-05-04 guard). Render `<Dialog open={project !== null} onOpenChange={(open) => {
    if (!open) setSearchParams((prev) => { prev.delete("project"); return prev; }, { replace:
    true }); }}>`. Type the handler as `(open: boolean) => void` (base-ui passes a 2nd details
    arg — ignore it; do NOT copy Radix single-arg assumptions). Use `{ replace: true }` on close
    so Back doesn't re-open. Inside, only when `project`, render the body mirroring
    ProjectPopoverContent: header (name + status/priority Badges using the imported canonical
    PRIORITY_LABELS) with `<SyncBadge project={project}/>` rendered in that header row (OV-04
    surface #1 — shows only when planAhead truthy); issueCounts stacked bar (backlog/started/done
    + total===0 guard); milestones list (project.milestones); and the guarded Linear deep-link
    copied VERBATIM (`project.url?.startsWith("https://linear.app/")`). Do NOT list individual
    issues (D-05-03 — snapshot has only aggregate issueCounts). Include a DialogTitle and a
    visible, keyboard-accessible DialogClose (accessible name) for accessibility.
  </action>
  <acceptance_criteria>
    - `grep -n "project.planAhead" src/components/overview/SyncBadge.tsx` matches; the badge is behind that truthy guard and returns null otherwise; SyncBadge imports Badge from @/components/ui/badge.
    - `grep -c "<SyncBadge" src/components/overview/ProjectDrillDownDialog.tsx` returns >= 1 (badge mounted in the dialog header — OV-04 render surface).
    - `grep -n "PRIORITY_LABELS" src/components/overview/ProjectDrillDownDialog.tsx` shows it imported from @/lib/overview/selectors (no local copy).
    - `grep -n "\.find((p) =>" src/components/overview/ProjectDrillDownDialog.tsx` (or equivalent) shows the guarded lookup; unknown id → null → no dialog.
    - `grep -n "startsWith(\"https://linear.app/\")" src/components/overview/ProjectDrillDownDialog.tsx` matches (guarded link copied verbatim).
    - `grep -c "replace: true" src/components/overview/ProjectDrillDownDialog.tsx` returns >= 1 (close uses replace).
    - A DialogClose with an accessible name is rendered; no mapping over individual issues (only issueCounts aggregate rendered).
    - `npx tsc -b --noEmit` and `npx eslint src/components/overview` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit && npx eslint src/components/overview</automated>
  </verify>
  <done>SyncBadge renders only when planAhead is truthy; the drill-down opens only for a known ?project id, shows counts + milestones + guarded Linear link (no individual issues) + a header-mounted SyncBadge + a visible DialogClose, and closes by deleting the param with replace; compiles/lints clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams -> dialog open state / href | `?project` and filter params are attacker-controllable via shareable links; `?project` gates a component render and `project.url` becomes an href. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (open-redirect) | drill-down Linear link | mitigate | Reuse the Phase-4 04-04 guard verbatim: render the anchor only when `project.url?.startsWith("https://linear.app/")`, with `rel="noopener noreferrer"`. A `javascript:`/foreign URL is never linked. |
| T-05-05 | Tampering / DoS | `?project` open state | mitigate | Guarded lookup: an unknown/stale/oversized id resolves to null and renders NO dialog (never crashes). No param value is interpolated unescaped into markup. |
| T-05-03 | Information Disclosure | SyncBadge / planAhead render | accept | planAhead is a boolean flag only (no PII); rendered as a static label behind a truthy guard. Low-value, client-only. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH § Package Legitimacy Audit — zero external packages). |
</threat_model>

<verification>
- `npx tsc -b --noEmit` + `npx eslint src/components/overview` clean.
- Filter serialization + ?project-survival are covered by 05-02 round-trip tests; dialog/filter/badge interaction fidelity is human-UAT (Path B).
</verification>

<success_criteria>
- OV-02 filter controls (quarter+custom coexisting), OV-03 drill-down (with a header-mounted OV-04 badge), and the reusable OV-04 SyncBadge exist, URL-driven and coexisting, ready for OverviewPage assembly (05-06).
</success_criteria>

<output>
Create `.planning/phases/05/05-05-SUMMARY.md` when done.
</output>
