---
phase: 04-roadmap-timeline-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/timeline/dateUtils.ts
  - src/lib/timeline/dateUtils.test.ts
  - src/lib/timeline/colorUtils.ts
  - src/lib/timeline/colorUtils.test.ts
autonomous: true
requirements: [TL-01, TL-02, TL-04]
must_haves:
  truths:
    - "getMonthColumns returns 7 columns Jul 2026 .. Jan 2027 for a July 2026 window."
    - "barPosition returns left/width percentages, clampedLeft/clampedRight flags, and a kind discriminator (\"span\" | \"fixedEnd\" | \"stub\") that distinguishes a normal/partly-in-window bar from a D-07 targetDate-only bar and a D-03 entirely-off-window stub, clamping symmetrically at both window edges."
    - "resolveInitiativeColor returns the API color when set, else a stable fallback from a fixed palette keyed by lexicographic null-id order (D-11)."
  artifacts:
    - path: "src/lib/timeline/dateUtils.ts"
      provides: "getWindow, daysBetween, barPosition, getMonthColumns, todayLeftPercent"
      exports: ["getWindow", "daysBetween", "barPosition", "getMonthColumns", "todayLeftPercent"]
    - path: "src/lib/timeline/colorUtils.ts"
      provides: "resolveInitiativeColor, luminanceFor"
      exports: ["resolveInitiativeColor", "luminanceFor"]
    - path: "src/lib/timeline/dateUtils.test.ts"
      provides: "coverage for month columns, bar position, the kind discriminator (span/fixedEnd/stub), all three D-03 clamp branches (before-left, before-both, entirely-after), D-07, today marker"
    - path: "src/lib/timeline/colorUtils.test.ts"
      provides: "coverage for color fallback stability + luminance contrast"
  key_links:
    - from: "src/lib/timeline/dateUtils.test.ts"
      to: "src/lib/timeline/dateUtils.ts"
      via: "imports the pure functions under test"
      pattern: "from \"./dateUtils"
    - from: "src/lib/timeline/colorUtils.test.ts"
      to: "src/lib/timeline/colorUtils.ts"
      via: "imports the pure functions under test"
      pattern: "from \"./colorUtils"
---

<objective>
Create the pure, testable timeline calculation modules: date/window math (fixed 7-month
window, bar positioning, D-03 clamping, D-07 short bar, D-02 today marker) and initiative
color resolution (D-11 deterministic fallback + bar text contrast). These are the Wave 0
test-backed foundation the timeline components render from.

Purpose: TL-01 (month axis + bar positions), TL-02 (scheduled/undated split via targetDate),
and TL-04 (color-by-initiative) all depend on deterministic, offline-testable functions.
Output: two pure modules with no external deps and full unit coverage of the branch matrix.

Known risk (no plan change — synthetic coverage is sufficient): both currently-scheduled
projects fall outside the July 2026 window, so `roadmap.json` only exercises the clamp/stub
paths at runtime; the normal un-clamped "span" path is covered here only by synthetic unit
tests until more projects are dated (OpenCode review C2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04/04-RESEARCH.md
@.planning/phases/04/04-PATTERNS.md
@.planning/phases/04/04-UI-SPEC.md
@.planning/phases/04/04-VALIDATION.md

<interfaces>
<!-- Signatures to implement. Full bodies in RESEARCH.md Pattern 1/2 + Code Examples. -->
getWindow(now?: Date): { windowStart: Date; windowEnd: Date; windowDays: number }
daysBetween(a: Date, b: Date): number
barPosition(startDate: string | null, targetDate: string, window: { windowStart: Date; windowEnd: Date; windowDays: number }): { left: number; width: number; clampedLeft: boolean; clampedRight: boolean; kind: "span" | "fixedEnd" | "stub" }
<!-- kind discriminator (return-shape contract for the 04-05 caller — do NOT branch on width <= 0):
       "span"     = normal or partly-in-window bar (has real width; may be clampedLeft/Right at an edge)
       "fixedEnd" = D-07 targetDate present but startDate null (caller renders a fixed 64px right-aligned bar)
       "stub"     = D-03 bar entirely off-window before or after (caller renders a 32px stub with a ◀/▶ cue) -->
getMonthColumns(windowStart: Date): Array<{ label: string; date: Date }>
todayLeftPercent(now: Date, windowStart: Date, windowDays: number): number
resolveInitiativeColor(initiative: { id: string; color: string | null }, allInitiatives: Array<{ id: string; color: string | null }>): string
luminanceFor(hex: string): number   // sRGB approximation; caller picks white text when < 0.4
FALLBACK_PALETTE = ["#10b981", "#6366f1", "#14b8a6", "#ec4899", "#f97316"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dateUtils module (window, bar position, clamping, today)</name>
  <files>src/lib/timeline/dateUtils.ts, src/lib/timeline/dateUtils.test.ts</files>
  <behavior>
    - getMonthColumns(new Date(2026,6,1)) -> 7 items with labels ["Jul 2026","Aug 2026","Sep 2026","Oct 2026","Nov 2026","Dec 2026","Jan 2027"].
    - getWindow(new Date(2026,6,1)).windowStart = 2026-07-01; windowEnd = 2027-01-31 (last day of month+6).
    - barPosition with startDate & targetDate fully inside window -> left>0, width>0, clampedLeft=false, clampedRight=false, kind="span".
    - D-03 starts-before-window (start 2026-06-22, target 2026-08-17) -> clampedLeft=true, left=0, width>0, kind="span" (partly-in-window bar, still a span).
    - D-03 entirely-before-window (start 2026-04-13, target 2026-05-08) -> clampedLeft=true, effective width <= 0, kind="stub" (caller renders 32px stub at col 0 with a ◀ cue).
    - D-03 entirely-after-window (start 2028-01-01, target 2028-02-01, both after windowEnd) -> clampedRight=true, effective left=100%, width=0, kind="stub" (caller renders a 32px stub at the right edge of column 6 with a ▶ "continues" cue).
    - D-07 targetDate present, startDate null -> width 0, kind="fixedEnd" (caller applies fixed 64px).
    - todayLeftPercent(2026-07-01, windowStart 2026-07-01, windowDays) -> 0.
  </behavior>
  <read_first>
    - src/lib/utils.ts (named-export module style, no default export)
    - .planning/phases/04/04-RESEARCH.md § "Pattern 1: Date Math" + § "Code Examples" (full function bodies)
    - .planning/phases/04/04-UI-SPEC.md § "Time Axis Contract" + § "Bar Contract"
    - .planning/phases/04/04-PATTERNS.md § "src/lib/timeline/dateUtils.ts" + § "dateUtils.test.ts"
    - scripts/linear/transform.test.ts (vitest describe/it/expect + inline-fixture idiom)
  </read_first>
  <action>
    Write test-first (RED): create dateUtils.test.ts importing the five functions and
    asserting the behaviors above. Then implement dateUtils.ts (GREEN) with the exact
    signatures in <interfaces>, copying the bodies from RESEARCH.md Pattern 1 / Code
    Examples. NO external date library — use only the `Date` constructor and arithmetic
    (RESEARCH anti-patterns). Parse date strings as `new Date(s + "T00:00:00")` to avoid
    UTC-offset drift. barPosition clamps symmetrically at BOTH window edges: effectiveStart =
    max(start, windowStart) and effectiveEnd = min(target, windowEnd), reporting
    clampedLeft when start < windowStart (before → left=0, caller shows a ◀ cue). Additionally
    guard the entirely-after-window case: when effectiveStart > windowEnd, clamp effectiveStart
    to windowEnd so left=100% and width=0, and set clampedRight=true (after → right edge, caller
    shows a ▶ cue). Return an explicit `kind` discriminator so the 04-05 caller never has to
    infer intent from `width <= 0`: `kind="fixedEnd"` when startDate is null (D-07, caller
    renders the fixed 64px bar); `kind="stub"` when the bar is entirely off-window (effective
    width <= 0 after clamping, i.e. the entirely-before or entirely-after branches, caller
    renders a 32px stub); otherwise `kind="span"` (normal or partly-in-window bar with real
    width, including the clampedLeft partial case). When startDate is null it still returns
    `width: 0` alongside `kind: "fixedEnd"`. Named exports only; no default export.
  </action>
  <acceptance_criteria>
    - dateUtils.test.ts asserts the 7 month labels, all three D-03 clamp branches (before-left, entirely-before, entirely-after), the D-07 case, and todayLeftPercent==0.
    - dateUtils.test.ts asserts the kind discriminator per branch: "span" for the fully-inside and starts-before-clamped (partly-in-window) cases, "stub" for entirely-before and entirely-after, "fixedEnd" for the startDate-null D-07 case.
    - dateUtils.test.ts asserts the entirely-after-window case returns clampedRight=true, left=100%, width=0, kind="stub"; and barPosition clamps effectiveStart to windowEnd when effectiveStart > windowEnd.
    - dateUtils.ts exports getWindow, daysBetween, barPosition, getMonthColumns, todayLeftPercent (named); barPosition's return type includes the `kind: "span" | "fixedEnd" | "stub"` field.
    - `grep -c "date-fns\|dayjs\|luxon\|moment" src/lib/timeline/dateUtils.ts` returns 0.
    - `pnpm test src/lib/timeline/dateUtils.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test src/lib/timeline/dateUtils.test.ts</automated>
  </verify>
  <done>All five date functions implemented with no date library; the branch matrix (normal/span, clamp-left span, clamp-before-both stub, clamp-right/after stub, D-07 fixedEnd, today) is green with the kind discriminator asserted per branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: colorUtils module (fallback palette + contrast)</name>
  <files>src/lib/timeline/colorUtils.ts, src/lib/timeline/colorUtils.test.ts</files>
  <behavior>
    - resolveInitiativeColor with a non-null color returns that color unchanged.
    - For the null-color initiative "agenticapps-workflow" (only null in current data) returns FALLBACK_PALETTE[0] = "#10b981".
    - Given two null-color initiatives, the lexicographically-first id gets palette[0], the second gets palette[1] (stable, deterministic).
    - Same input yields same output across repeated calls (no randomness / Date use).
    - luminanceFor("#f2c94c") (yellow) > 0.4 (caller uses #1a1a1a text); luminanceFor("#5e6ad2") (purple) < 0.4 (caller uses #ffffff).
  </behavior>
  <read_first>
    - src/lib/utils.ts (named-export module style)
    - .planning/phases/04/04-RESEARCH.md § "Pattern 2: Initiative Color Resolution"
    - .planning/phases/04/04-UI-SPEC.md § "Initiative Color Ramp" + § "Bar and Pill Surface Colors"
    - .planning/phases/04/04-PATTERNS.md § "src/lib/timeline/colorUtils.ts" + § "colorUtils.test.ts"
  </read_first>
  <action>
    Write test-first (RED): colorUtils.test.ts covering the behaviors above. Then implement
    (GREEN) colorUtils.ts with `FALLBACK_PALETTE = ["#10b981","#6366f1","#14b8a6","#ec4899","#f97316"]`,
    `resolveInitiativeColor` (return color if truthy, else index into the palette by the
    initiative's position in the sorted list of null-color ids, modulo palette length), and
    `luminanceFor(hex)` returning the sRGB approximation `0.2126*r + 0.7152*g + 0.0722*b`
    over the parsed hex channels. Named exports only.
  </action>
  <acceptance_criteria>
    - colorUtils.test.ts asserts pass-through, palette[0] for the first null id, palette[1] for a second, and stability across two calls.
    - colorUtils.test.ts asserts luminanceFor yellow > 0.4 and purple < 0.4.
    - colorUtils.ts exports resolveInitiativeColor and luminanceFor (named).
    - `pnpm test src/lib/timeline/colorUtils.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test src/lib/timeline/colorUtils.test.ts</automated>
  </verify>
  <done>Deterministic color fallback + luminance contrast implemented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot data -> pure functions | Functions receive already-Zod-validated dates/colors as plain values; no I/O, no untrusted parsing beyond `Date`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-U1 | Denial of Service | Date parsing of malformed date strings | accept | Inputs are snapshot fields already validated by ProjectSchema; functions are pure and total (no throw path exercised by valid data). No network or user-supplied input reaches these modules. |
| T-04-U2 | Tampering | color hex parsing | accept | Colors originate from the validated snapshot or a hardcoded palette; no external color input. |
</threat_model>

<verification>
- `pnpm test && pnpm typecheck` green.
- Both modules are pure (no imports beyond types), no date library present.
</verification>

<success_criteria>
- TL-01/TL-02: deterministic window + bar-position + split math is unit-proven.
- TL-04: initiative color resolution with stable fallback is unit-proven.
</success_criteria>

<output>
Create `.planning/phases/04/04-02-SUMMARY.md` when done.
</output>
</content>
</output>
