---
phase: 04-roadmap-timeline-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/hover-card.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/badge.tsx
autonomous: true
requirements: [TL-03, TL-04]
must_haves:
  truths:
    - "hover-card, popover, and badge components exist in src/components/ui/ and compile."
    - "None of the three scaffolded files import from @radix-ui — they resolve to @base-ui/react (zero new npm deps)."
  artifacts:
    - path: "src/components/ui/hover-card.tsx"
      provides: "base-ui PreviewCard-backed HoverCard for desktop popover trigger"
    - path: "src/components/ui/popover.tsx"
      provides: "base-ui Popover for touch tap trigger"
    - path: "src/components/ui/badge.tsx"
      provides: "CVA Badge for status/priority chips"
  key_links:
    - from: "src/components/ui/hover-card.tsx"
      to: "@base-ui/react/preview-card"
      via: "scaffolded import"
      pattern: "@base-ui/react"
---

<objective>
Scaffold the three shadcn UI primitives the popover needs — `hover-card` (desktop hover
trigger), `popover` (touch tap trigger), and `badge` (status/priority chips) — via the
shadcn CLI. The project uses the `base-nova` style which sources these from
`@base-ui/react` (already installed at v1.6.0), so this adds ZERO new npm dependencies.

Purpose: TL-03 needs the hover/tap popover container; TL-04 needs the badge for
status/priority chips. Scaffolding first (Wave 1) unblocks the popover plan (04-04).
Output: three source-only component files matching the existing `button.tsx` style, with a
verification that no Radix packages were pulled in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-RESEARCH.md
@.planning/phases/04/04-PATTERNS.md
@.planning/phases/04/04-UI-SPEC.md

<interfaces>
<!-- Expected backing primitives after scaffold (VERIFIED in RESEARCH.md § Standard Stack). -->
hover-card.tsx -> @base-ui/react/preview-card (PreviewCard.Root/Trigger/Popup/Positioner/Portal)
popover.tsx    -> @base-ui/react/popover (Popover.Root/Trigger/Popup/Positioner/Portal)
badge.tsx      -> @base-ui/react (mergeProps/useRender) + cva variants (default/secondary/destructive/outline)
Existing analog: src/components/ui/button.tsx (cn + cva + data-slot + {...props}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold hover-card, popover, badge and verify base-ui backing</name>
  <files>src/components/ui/hover-card.tsx, src/components/ui/popover.tsx, src/components/ui/badge.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (the only existing shadcn component — style/import reference)
    - components.json (confirms base-nova style + empty registries)
    - .planning/phases/04/04-RESEARCH.md § "Shadcn Components to Add" + § "State of the Art" (base-ui, not Radix)
    - .planning/phases/04/04-UI-SPEC.md § "Components to Add This Phase"
    - package.json (confirm @base-ui/react present; no @radix-ui expected)
  </read_first>
  <action>
    Run `npx shadcn add hover-card popover badge` to scaffold the three components into
    `src/components/ui/`. Do NOT hand-write them. After scaffolding, verify the generated
    files import from `@base-ui/react` (preview-card / popover) and NOT from `@radix-ui/*`.
    If any file references `@radix-ui`, the wrong style was fetched — re-run with the
    base-nova style (`--style base-nova` or via the components.json preset) and confirm no
    `@radix-ui/*` package was added to package.json. Note: `openDelay`/`closeDelay` prop
    naming on the PreviewCard is deferred to 04-04 (consumer) — do not modify the scaffold.
  </action>
  <acceptance_criteria>
    - The three files exist: src/components/ui/hover-card.tsx, popover.tsx, badge.tsx.
    - `grep -rl "@radix-ui" src/components/ui/` returns nothing (no matches).
    - `grep -l "@base-ui/react" src/components/ui/hover-card.tsx src/components/ui/popover.tsx` lists both files.
    - `grep -c "@radix-ui" package.json` returns 0 (no new Radix dependency added).
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && test -z "$(grep -rl '@radix-ui' src/components/ui/)"</automated>
  </verify>
  <done>hover-card, popover, badge scaffolded from base-ui (no Radix, no new npm dep); typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry -> local source tree | `npx shadcn add` writes generated component source into the repo; a compromised/wrong-style registry entry could pull unexpected dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | shadcn scaffold of hover-card/popover/badge | mitigate | Official shadcn registry only (components.json registries={}); verify generated imports resolve to the already-vetted `@base-ui/react` and that no `@radix-ui/*` (or any new package) is added to package.json. Zero new npm installs — source-only scaffold. |
</threat_model>

<verification>
- Three components present, base-ui-backed, no Radix, no new dependency, typecheck clean.
</verification>

<success_criteria>
- TL-03/TL-04 UI primitives available for the popover and badge chips with zero dependency drift.
</success_criteria>

<output>
Create `.planning/phases/04/04-03-SUMMARY.md` when done.
</output>
