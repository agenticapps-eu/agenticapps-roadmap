---
phase: 04-roadmap-timeline-ui
plan: 06
type: execute
wave: 4
depends_on: ["04-02", "04-05"]
files_modified:
  - src/components/timeline/AxisRow.tsx
  - src/components/timeline/InitiativeLane.tsx
  - src/pages/TimelinePage.tsx
  - src/components/RoadmapBoundaries.tsx
autonomous: true
requirements: [TL-01, TL-02, TL-04]
must_haves:
  truths:
    - "The timeline shows a 7-month axis header (D-01) with a today marker line (D-02)."
    - "One lane per initiative (all 5, including Factiv with 0 projects) ordered by scheduled-count desc then name asc; each lane shows a left parking rail of undated pills and a right grid of scheduled bars (D-04 parking rail, D-06 scheduled=has-targetDate split)."
    - "Empty and error states render in the page body per the UI-SPEC; the loading state renders skeleton swimlanes via the root-level HydrateFallback (RoadmapLoading) per UI-SPEC § Loading State; layout is dark-mode aware and horizontally scrollable below 840px (D-12)."
  artifacts:
    - path: "src/components/timeline/AxisRow.tsx"
      provides: "rail header + 7 month columns + today marker"
      exports: ["AxisRow"]
    - path: "src/components/timeline/InitiativeLane.tsx"
      provides: "lane header + parking rail + scheduled grid"
      exports: ["InitiativeLane"]
    - path: "src/pages/TimelinePage.tsx"
      provides: "assembled timeline page reading loader data + states"
      exports: ["TimelinePage"]
      min_lines: 40
    - path: "src/components/RoadmapBoundaries.tsx"
      provides: "root HydrateFallback skeleton swimlanes (D-12 loading state)"
      contains: "animate-pulse"
  key_links:
    - from: "src/pages/TimelinePage.tsx"
      to: "src/lib/roadmap/loader.ts"
      via: "useRouteLoaderData(\"root\")"
      pattern: "useRouteLoaderData"
    - from: "src/components/timeline/InitiativeLane.tsx"
      to: "src/components/timeline/ScheduledBar.tsx"
      via: "renders bars + pills per lane"
      pattern: "ScheduledBar"
    - from: "src/components/timeline/AxisRow.tsx"
      to: "src/lib/timeline/dateUtils.ts"
      via: "getMonthColumns + todayLeftPercent"
      pattern: "getMonthColumns"
  # Follow-up (RECOMMENDED, NOT this phase — reviews item 7, Path B): this repo has no
  # component-render test harness (vitest env=node, no @testing-library/react, no jsdom/happy-dom,
  # no *.test.tsx; include glob is *.test.ts only). A lightweight timeline smoke-render test
  # (assert all 5 lanes render, Factiv header-only, Callbot's 9 pills, scheduled bars present)
  # would materially de-risk the UI, but adding it requires NEW infra (deps + jsdom env + glob
  # change) that is out of scope for this phase under SIMPLICITY FIRST. The Task 3 human-check
  # remains the fidelity gate; installing a render-test harness is a recommended future addition.
---

<objective>
Assemble the timeline hero view: `AxisRow` (D-01 7-month header + D-02 today marker),
`InitiativeLane` (lane header + D-04 parking rail + scheduled grid), and the full
`TimelinePage` replacement (loader data, lane ordering, empty/loading/error states,
responsive + dark-mode shell).

Purpose: TL-01 (month axis + lanes), TL-02 (undated pills in-lane), TL-04 (color-by-
initiative, responsive, dark mode, empty/loading/error states). This wires the leaves and
primitives from prior waves into the final page.
Output: a working Timeline route rendering all 16 projects from `public/roadmap.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-UI-SPEC.md
@.planning/phases/04/04-PATTERNS.md
@.planning/phases/04/04-RESEARCH.md
@src/pages/TimelinePage.tsx
@src/pages/OverviewPage.tsx

<interfaces>
<!-- Consumed. -->
useRouteLoaderData("root") as RoadmapLoaderData | null; const { data } = loaderData; // data: RoadmapJson { initiatives, projects }
getWindow, getMonthColumns, todayLeftPercent  // 04-02
resolveInitiativeColor(initiative, allInitiatives)  // 04-02
UndatedPill({ project, color }), ScheduledBar({ project, color, window })  // 04-05
Scheduled = project.targetDate !== null (D-06). Undated = targetDate === null.
Lane order: scheduled-count desc, then initiative.name asc (UI-SPEC § Lane Ordering).
Pill order within rail: priority asc, then name asc (UI-SPEC § Parking Rail).
AxisRow props: { window, monthColumns }. InitiativeLane props: { initiative, projects, color, window }.
Loading: React Router 7 renders the root HydrateFallback (RoadmapLoading, wired in src/router.tsx) during loader suspension — TimelinePage does NOT mount until data resolves, so the loading skeleton lives in RoadmapLoading, not the page body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AxisRow (month columns + today marker)</name>
  <files>src/components/timeline/AxisRow.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Layout Architecture" (AxisRow tree) + § "Time Axis Contract"
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/AxisRow.tsx"
    - src/lib/timeline/dateUtils.ts (getMonthColumns, todayLeftPercent)
    - src/components/AppHeader.tsx (flex + fixed-left-column idiom, aria-label pattern)
  </read_first>
  <action>
    Create `AxisRow({ window, monthColumns })`: a `flex h-8` row with a `w-40 shrink-0 px-4`
    RailHeader captioned "Needs dates" (12px, `--muted-foreground`) and a `relative flex-1
    grid grid-cols-7` MonthColumns region rendering the 7 `{ label }` cells (text-xs
    semibold). Overlay a TodayMarker: an absolutely positioned 1.5px vertical line at
    `left: ${todayLeftPercent(new Date(), window.windowStart, window.windowDays)}%`, color
    `--foreground`, `aria-label="Today"`, non-interactive. Use Tailwind v4
    custom-property tokens throughout.
  </action>
  <acceptance_criteria>
    - `grep -c "grid-cols-7" src/components/timeline/AxisRow.tsx` returns >= 1.
    - `grep -c "aria-label=\"Today\"" src/components/timeline/AxisRow.tsx` returns >= 1.
    - `grep -c "Needs dates" src/components/timeline/AxisRow.tsx` returns >= 1.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Axis renders 7 month cells with a labeled rail header and a positioned today marker; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: InitiativeLane (header + parking rail + scheduled grid)</name>
  <files>src/components/timeline/InitiativeLane.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Layout Architecture" (InitiativeLane/LaneBody tree) + § "Parking Rail Contract" + § "States Contract" (No-Projects lane)
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/InitiativeLane.tsx"
    - src/components/timeline/UndatedPill.tsx + ScheduledBar.tsx
  </read_first>
  <action>
    Create `InitiativeLane({ initiative, projects, color, window })`. LaneHeader: `flex h-8
    items-center gap-2 px-4 border-b` with an 8x8px ColorSwatch (`style={{ backgroundColor:
    color }}` circle) + `initiative.name` (text-xs semibold). LaneBody: `flex min-h-12`
    with a `w-40 shrink-0 bg-(--color-muted) p-2 space-y-2` ParkingRail rendering
    `UndatedPill` for each undated project (targetDate === null) sorted by priority asc then
    name asc, and a `flex-1 relative bg-(--color-background)` ScheduledGrid with per-month
    grid lines and a `ScheduledBar` for each scheduled project (targetDate !== null). When
    the initiative has zero projects, render only the LaneHeader (lane collapses to header
    height — do NOT hide the initiative; Factiv case). `key={project.id}` on every map.
  </action>
  <acceptance_criteria>
    - File renders UndatedPill for undated projects and ScheduledBar for scheduled projects.
    - `grep -c "targetDate === null\|targetDate !== null" src/components/timeline/InitiativeLane.tsx` returns >= 1 (D-06 split).
    - `grep -c "bg-(--color-muted)" src/components/timeline/InitiativeLane.tsx` returns >= 1 (parking rail bg).
    - A zero-project initiative renders the lane header (not hidden).
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Each lane shows header + sorted undated pills + scheduled bars; empty initiatives keep a header-only lane; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: TimelinePage assembly + states</name>
  <files>src/pages/TimelinePage.tsx, src/components/RoadmapBoundaries.tsx</files>
  <read_first>
    - src/pages/TimelinePage.tsx (current placeholder — full replacement; keep the data-access + split idiom)
    - .planning/phases/04/04-UI-SPEC.md § "Layout Architecture" + § "Initiative Lane Ordering" + § "States Contract" (incl. § "Loading State") + § "Responsive Contract" + § "Dark Mode Contract"
    - .planning/phases/04/04-PATTERNS.md § "src/pages/TimelinePage.tsx" + § "Error State (D-12)"
    - src/components/AppHeader.tsx (existing "live unavailable" banner reuse) + src/components/RoadmapBoundaries.tsx (RoadmapLoading HydrateFallback — currently a text spinner to upgrade)
    - src/router.tsx (HydrateFallback wiring: `HydrateFallback: RoadmapLoading`)
    - src/components/timeline/AxisRow.tsx + InitiativeLane.tsx
  </read_first>
  <action>
    Replace TimelinePage. Read `useRouteLoaderData("root") as RoadmapLoaderData | null`;
    return null if null. Compute `window = getWindow()` and `monthColumns =
    getMonthColumns(window.windowStart)`. Build lanes: for each initiative resolve its color
    via `resolveInitiativeColor(initiative, data.initiatives)`, collect its projects, and
    sort lanes by scheduled-count desc then name asc. Render
    `section[aria-label="Timeline"]` with an `h1 text-xl font-semibold mb-8 "Timeline"`, a
    `min-w-[840px]` grid container holding `AxisRow` then a `divide-y ... border-t`
    InitiativeLanes list. The page section sits inside `main.overflow-x-auto` (per
    RootLayout) — add `overflow-x-auto` on the section wrapper. Empty state (`data.projects.length
    === 0`): the centered `flex flex-col items-center gap-4 py-24 text-center` block with the
    📅 emoji + "No projects found" + the sync copy. Error state: the muted "Could not load
    timeline data. Switch to Snapshot mode above." line (reuse the existing loader/
    liveUnavailable pattern — do NOT build a new error component). Dark mode uses existing
    `.dark` tokens (no new tokens). Use Tailwind v4 custom-property tokens throughout.

    Loading state (D-12): in React Router 7 the root `HydrateFallback` (`RoadmapLoading`,
    wired in `src/router.tsx`) is what renders during loader suspension — TimelinePage never
    mounts until data resolves, so the loading skeleton belongs in `RoadmapLoading`, NOT in
    the page body. Upgrade `RoadmapLoading` in `src/components/RoadmapBoundaries.tsx` from
    the current text spinner to the UI-SPEC § "Loading State" skeleton swimlanes: an axis row
    of 7 muted `bg-(--color-muted)` skeleton rectangles (~16×8px) plus 2–3 skeleton lanes,
    each with 2–3 skeleton pill shapes in the rail and one skeleton bar shape in the grid.
    Apply `animate-pulse`; render no text/copy. Leave `RoadmapError` and the router
    `HydrateFallback: RoadmapLoading` wiring unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "useRouteLoaderData(\"root\")" src/pages/TimelinePage.tsx` returns >= 1.
    - `grep -c "min-w-\[840px\]" src/pages/TimelinePage.tsx` returns >= 1 (responsive scroll).
    - `grep -c "No projects found" src/pages/TimelinePage.tsx` returns >= 1 (empty state).
    - `grep -c "Could not load timeline data" src/pages/TimelinePage.tsx` returns >= 1 (error state).
    - `grep -c "resolveInitiativeColor" src/pages/TimelinePage.tsx` returns >= 1.
    - `grep -c "animate-pulse" src/components/RoadmapBoundaries.tsx` returns >= 1 (skeleton loading state per UI-SPEC).
    - `grep -c "HydrateFallback" src/router.tsx` returns >= 1 (loading state wired to the root HydrateFallback).
    - `pnpm test && pnpm typecheck && pnpm build` all pass.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm build</automated>
    <human-check>
      Run `pnpm dev`, open the Timeline route. Verify against 04-UI-SPEC.md:
      (1) 7 month columns Jul 2026..Jan 2027 with a today marker at the far left;
      (2) 5 initiative lanes ordered agenticapps-workflow, cPARX, Callbot, Factiv, fx-signals;
          Factiv shows a header-only lane;
      (3) AgenticApps Roadmap bar clamped left with a ◀ cue; cPARX Prototype as a 32px ◀ stub;
      (4) Callbot rail shows 9 dashed pills; fx-signals shows 3;
      (5) hover a bar/pill (desktop) opens the popover with badges, dates, issue-counts bar,
          milestones, summary (Linear link omitted until 04-07 runs);
      (6) toggle dark mode and narrow the viewport below 840px -> horizontal scroll, no column collapse;
      (7) reload with the browser network throttled (or a hard refresh) — confirm the skeleton
          swimlanes (pulsing axis + lane shapes, no text) appear during loader suspension,
          not the old text spinner; also navigate to the Overview route under the same throttle
          and confirm the root skeleton does not visually regress that page.
    </human-check>
  </verify>
  <done>Timeline route renders all 16 projects with axis, ordered lanes, rails, bars, empty/error states; the loading state shows skeleton swimlanes via the root HydrateFallback; test + typecheck + build green; visual human-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roadmap.json -> loader -> page | Snapshot is Zod-validated by the loader before reaching the page; the page renders validated fields only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Linear link surfaced via popover in lanes | mitigate | Link + guard remain in ProjectPopoverContent (04-04); this plan passes validated project data only. |
| T-04-A1 | Denial of Service | render of many pills/lanes | accept | Bounded dataset (5 initiatives, 16 projects) from a validated snapshot; no unbounded loop. |
| T-04-A2 | Information Disclosure | error-state copy | mitigate | Error state shows a generic "Could not load timeline data" message; no token/PII/stack surfaced (reuses existing loader fallback). |
</threat_model>

<verification>
- `pnpm test && pnpm typecheck && pnpm build` green (phase gate per 04-VALIDATION.md).
- Human-check confirms TL-04 visual/responsive/dark-mode fidelity, the skeleton loading state (incl. no Overview-route regression), and TL-01/TL-02 layout.
</verification>

<success_criteria>
- TL-01: month axis + one lane per initiative + scheduled bars render.
- TL-02: undated projects render as dashed pills in-lane.
- TL-04: color-by-initiative, responsive, dark mode, and empty/loading/error states present (loading = skeleton swimlanes via the root HydrateFallback).
</success_criteria>

<output>
Create `.planning/phases/04/04-06-SUMMARY.md` when done.
</output>
