---
phase: 04-roadmap-timeline-ui
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - src/components/timeline/ProjectPopoverContent.tsx
  - src/components/timeline/MilestoneMarker.tsx
autonomous: true
requirements: [TL-03]
must_haves:
  truths:
    - "The popover shows project name, status + priority badges, start/target dates, issue-counts bar, milestones list, and summary (D-09)."
    - "The Linear link renders only when project.url starts with https://linear.app/ and uses target=_blank rel=noopener noreferrer."
    - "A milestone marker is a diamond positioned at its targetDate and is omitted when targetDate is null (D-10)."
  artifacts:
    - path: "src/components/timeline/ProjectPopoverContent.tsx"
      provides: "shared popover body used by both HoverCard and Popover triggers"
      exports: ["ProjectPopoverContent"]
      min_lines: 40
    - path: "src/components/timeline/MilestoneMarker.tsx"
      provides: "diamond milestone marker + cluster count badge"
      exports: ["MilestoneMarker"]
  key_links:
    - from: "src/components/timeline/ProjectPopoverContent.tsx"
      to: "project.url"
      via: "guarded external anchor"
      pattern: "startsWith\\(\"https://linear.app/\"\\)"
    - from: "src/components/timeline/ProjectPopoverContent.tsx"
      to: "@/components/ui/badge"
      via: "status/priority chips"
      pattern: "Badge"
---

<objective>
Build the two leaf popover components consumed by bars and pills: `ProjectPopoverContent`
(the shared D-09 popover body — name, status/priority badges, dates, issue-counts bar,
milestones list, summary, and the guarded Linear link) and `MilestoneMarker` (the D-10
diamond marker on the bar with cluster-collapse count badge).

Purpose: TL-03 requires milestone markers plus a hover popover with project summary and a
Linear link. Building these leaves first (interface-first) lets 04-05 wrap them in the
bar/pill trigger elements without re-deriving the contract.
Output: two presentational components matching the UI-SPEC popover tree and marker visual,
with the defense-in-depth link guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-UI-SPEC.md
@.planning/phases/04/04-PATTERNS.md
@.planning/phases/04/04-RESEARCH.md

<interfaces>
<!-- Project fields (from src/lib/roadmap/schema.ts, url added by 04-01). -->
Project: { id, name: string, summary: string | null, url?: string | null, status: string, priority: number, startDate: string | null, targetDate: string | null, milestones: { id: string; name: string; targetDate: string | null }[], issueCounts: { backlog: number; started: number; done: number } }
Priority labels: 0=Urgent, 1=High, 2=Medium, 3=Low, other="—".
ProjectPopoverContent props: { project: Project; color: string }
MilestoneMarker props: { milestone: { name: string; targetDate: string | null }; color: string; leftPercent: number }
Badge (src/components/ui/badge.tsx): variant="outline" for status/priority chips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProjectPopoverContent (D-09 body + guarded Linear link)</name>
  <files>src/components/timeline/ProjectPopoverContent.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Hover/Tap Popover Contract" (full popover tree + copy + priority labels)
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/ProjectPopoverContent.tsx" (field access + link guard + Badge usage)
    - src/pages/OverviewPage.tsx (project field render idiom)
    - src/components/ui/badge.tsx (scaffolded — variant="outline")
    - .planning/phases/04/04-RESEARCH.md § "Security Domain" (open-redirect mitigation)
  </read_first>
  <action>
    Create `ProjectPopoverContent({ project, color })` rendering the UI-SPEC tree: header
    row with `project.name` (truncate) + a status Badge and priority Badge (variant
    "outline", priority mapped via 0=Urgent/1=High/2=Medium/3=Low/other="—"); a date row
    "Start: {startDate | '—'}" / "Target: {targetDate | '—'}"; an issue-counts segmented
    bar (backlog `bg-(--color-muted)`, started `bg-sky-500`, done `bg-emerald-500`, widths
    = count/total*100, all-zero -> single muted bar) with the "N backlog · N started · N
    done" count row; a milestones list (up to 5, "◆ {name} {targetDate | '—'}", ◆ in
    `color`); the summary (3-line clamp, muted) when present; and a footer link. Guard the
    issue-counts bar against divide-by-zero: compute `total = backlog + started + done` and,
    when `total === 0`, render a single muted full-width bar (never `count/total*100` when
    total is 0 — that yields NaN width). The footer renders `<a href={project.url}
    target="_blank" rel="noopener noreferrer">Open in Linear ↗</a>` ONLY when
    `project.url?.startsWith("https://linear.app/")` (defense-in-depth per RESEARCH Security
    Domain) — omit entirely otherwise. Use Tailwind v4 CSS custom-property tokens
    (`text-(--color-muted-foreground)`, `text-(--color-primary)`); never Tailwind v3 bare
    token names. Width 280px desktop / 260px mobile, `max-h-[420px] overflow-y-auto`.
  </action>
  <acceptance_criteria>
    - `grep -c "noopener noreferrer" src/components/timeline/ProjectPopoverContent.tsx` returns >= 1.
    - `grep -c "startsWith(\"https://linear.app/\")" src/components/timeline/ProjectPopoverContent.tsx` returns >= 1.
    - `grep -c "target=\"_blank\"" src/components/timeline/ProjectPopoverContent.tsx` returns >= 1.
    - File imports Badge from "@/components/ui/badge" and renders status + priority chips.
    - The issue-counts bar handles the all-zero case (backlog=started=done=0) by rendering a single muted bar — no divide-by-zero / NaN width (guarded on `total === 0` before any `count/total` division).
    - `grep -c "text-muted-foreground\b" src/components/timeline/ProjectPopoverContent.tsx` returns 0 (no v3 bare tokens).
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Popover body renders all D-09 sections; issue-counts bar is divide-by-zero-safe; Linear link is prefix-guarded + noopener; no v3 token classes; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: MilestoneMarker (diamond + cluster badge)</name>
  <files>src/components/timeline/MilestoneMarker.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Milestone Marker Contract" (diamond, stroke, title, cluster collapse)
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/MilestoneMarker.tsx"
    - src/components/ui/badge.tsx (cluster count badge)
    - src/components/ui/button.tsx (minimal standalone component idiom)
  </read_first>
  <action>
    Create `MilestoneMarker({ milestone, color, leftPercent })` rendering an absolutely
    positioned 10x10px diamond (8x8 square + `rotate-45`), fill `color`, 2px white ring,
    centered vertically (`top-1/2 -translate-y-1/2 -translate-x-1/2`), at
    `style={{ left: leftPercent% }}`. Set native `title={`${milestone.name} — ${milestone.targetDate}`}`
    for the accessibility tooltip. Return null when `milestone.targetDate` is null (omit).
    Export a helper or accept an optional `count` prop so that when a caller detects two or
    more milestones within 12px it can render a single stacked indicator with a count Badge
    (`text-xs` semibold) instead of overlapping diamonds. Use `cn` from "@/lib/utils".
  </action>
  <acceptance_criteria>
    - `grep -c "rotate-45" src/components/timeline/MilestoneMarker.tsx` returns >= 1.
    - `grep -c "title=" src/components/timeline/MilestoneMarker.tsx` returns >= 1.
    - The component returns null / renders nothing when milestone.targetDate is null.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Diamond marker positioned at targetDate with title tooltip, omitted when undated, cluster count path present; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot project.url -> rendered href | The `url` string (from the leak-gated pipeline) becomes an external anchor target opened in a new tab. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | ProjectPopoverContent footer link (window.opener / open redirect) | mitigate | Render with `rel="noopener noreferrer"` so the new tab cannot access `window.opener`; only render the anchor when `url.startsWith("https://linear.app/")`. |
| T-04-02 | Tampering | project.url injected into href | mitigate | `url` originates from Linear's own API through the `assertNoLeak`-cleared pipeline; the `https://linear.app/` prefix guard is defense-in-depth against any future data anomaly. |
| T-04-P1 | Information Disclosure | milestone `title` tooltip | accept | Milestone name/date are non-sensitive snapshot fields already exposed in the UI. |
</threat_model>

<verification>
- `pnpm typecheck` green; link guard + noopener present; marker omits undated milestones.
- Visual fidelity is verified end-to-end in 04-06 (full render).
</verification>

<success_criteria>
- TL-03: the popover body (summary + guarded Linear link + issue counts + milestones +
  status/priority + dates) and the on-bar milestone marker exist as reusable leaves.
</success_criteria>

<output>
Create `.planning/phases/04/04-04-SUMMARY.md` when done.
</output>
