---
phase: 04-roadmap-timeline-ui
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-04"]
files_modified:
  - src/components/timeline/UndatedPill.tsx
  - src/components/timeline/ScheduledBar.tsx
autonomous: true
requirements: [TL-01, TL-02, TL-03]
must_haves:
  truths:
    - "An undated project renders as a dashed initiative-colored pill that opens the project popover on hover (desktop) / tap (touch)."
    - "A scheduled project renders as an absolutely positioned bar from startDate to targetDate, clamped to the window with a continues cue when off-window."
    - "A scheduled project with targetDate but no startDate renders a fixed 64px bar ending at the targetDate; entirely-before-window renders a 32px stub with a ◀ cue."
    - "Both pill and bar embed ProjectPopoverContent and (bars) MilestoneMarkers."
  artifacts:
    - path: "src/components/timeline/UndatedPill.tsx"
      provides: "dashed needs-backfill pill + hover/tap popover trigger"
      exports: ["UndatedPill"]
    - path: "src/components/timeline/ScheduledBar.tsx"
      provides: "positioned+clamped bar with milestone markers + hover/tap popover trigger"
      exports: ["ScheduledBar"]
  key_links:
    - from: "src/components/timeline/ScheduledBar.tsx"
      to: "src/lib/timeline/dateUtils.ts"
      via: "barPosition for left/width + clamp flags"
      pattern: "barPosition"
    - from: "src/components/timeline/ScheduledBar.tsx"
      to: "src/components/timeline/ProjectPopoverContent.tsx"
      via: "popover body child"
      pattern: "ProjectPopoverContent"
    - from: "src/components/timeline/UndatedPill.tsx"
      to: "src/lib/timeline/colorUtils.ts"
      via: "initiative color tint"
      pattern: "color"
---

<objective>
Build the two in-lane project elements: `UndatedPill` (D-04 dashed needs-backfill pill) and
`ScheduledBar` (D-05/D-06/D-07 positioned bar with D-03 clamping and D-10 milestone
markers). Both wrap themselves in the correct popover trigger — `HoverCard` on desktop,
`Popover` on touch (D-08) — around the shared `ProjectPopoverContent`.

Purpose: TL-01 (scheduled bars), TL-02 (undated pills), and TL-03 (markers + popover
trigger). These consume the pure math (04-02) and popover leaves (04-04).
Output: the two interactive timeline primitives that InitiativeLane (04-06) places.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04/04-UI-SPEC.md
@.planning/phases/04/04-PATTERNS.md
@.planning/phases/04/04-RESEARCH.md

<interfaces>
<!-- Consumed from prior plans. -->
barPosition(startDate, targetDate, window): { left, width, clampedLeft, clampedRight }  // 04-02
resolveInitiativeColor / luminanceFor  // 04-02 (luminanceFor(color) < 0.4 -> white text else #1a1a1a)
ProjectPopoverContent({ project, color })  // 04-04
MilestoneMarker({ milestone, color, leftPercent })  // 04-04
HoverCard/HoverCardTrigger/HoverCardContent (@/components/ui/hover-card)  // 04-03
Popover/PopoverTrigger/PopoverContent (@/components/ui/popover)  // 04-03
Trigger switch: const [isTouch,setIsTouch]=useState(false); useEffect(()=>setIsTouch(window.matchMedia("(pointer: coarse)").matches),[]) — RESEARCH Pattern 3 / Pitfall 5.
Component props: UndatedPill({ project, color }); ScheduledBar({ project, color, window }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UndatedPill (dashed pill + hover/tap trigger)</name>
  <files>src/components/timeline/UndatedPill.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Parking Rail Contract" + § "Hover/Tap Popover Contract" (trigger)
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/UndatedPill.tsx"
    - .planning/phases/04/04-RESEARCH.md § "Pattern 3: Hover vs. Tap Trigger" + § "Pitfall 5" (matchMedia in useEffect)
    - src/components/ui/button.tsx (props->className idiom, cn import)
    - src/components/timeline/ProjectPopoverContent.tsx (popover body)
  </read_first>
  <action>
    Create `UndatedPill({ project, color })`. Render an h-7 dashed pill: inline style
    `borderColor: color`, `backgroundColor: ${color}14` (~8% opacity), `color`; class
    `truncate rounded-[0.875rem] border border-dashed px-2 text-xs`. Truncate the project
    name. Wrap the pill as the trigger of a popover showing `<ProjectPopoverContent
    project={project} color={color} />`. Select the trigger at mount via the isTouch state +
    `window.matchMedia("(pointer: coarse)")` pattern (call matchMedia inside useEffect,
    initialize isTouch=false so desktop gets HoverCard by default — Pitfall 5): touch ->
    Popover/PopoverTrigger asChild/PopoverContent; desktop -> HoverCard (openDelay 300 /
    closeDelay 200) with HoverCardTrigger asChild/HoverCardContent. Verify the actual base-ui
    delay prop names against `@base-ui/react/preview-card` types before committing (RESEARCH
    A1) and use the correct prop. Use `cn` from "@/lib/utils".
  </action>
  <acceptance_criteria>
    - `grep -c "border-dashed" src/components/timeline/UndatedPill.tsx` returns >= 1.
    - `grep -c "matchMedia(\"(pointer: coarse)\")" src/components/timeline/UndatedPill.tsx` returns >= 1.
    - `grep -c "useEffect" src/components/timeline/UndatedPill.tsx` returns >= 1 (matchMedia not called at module/render top level).
    - File renders ProjectPopoverContent inside both HoverCard and Popover branches.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Dashed initiative-colored pill toggles HoverCard/Popover by pointer type and shows the shared popover body; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: ScheduledBar (positioned, clamped, markers, trigger)</name>
  <files>src/components/timeline/ScheduledBar.tsx</files>
  <read_first>
    - .planning/phases/04/04-UI-SPEC.md § "Bar Contract" + § "Time Axis Contract" (Off-Window Clamping table) + § "Milestone Marker Contract"
    - .planning/phases/04/04-PATTERNS.md § "src/components/timeline/ScheduledBar.tsx"
    - .planning/phases/04/04-RESEARCH.md § "Pattern 1" (barPosition) + § "Pitfall 4" (D-07 fixed 64px)
    - src/lib/timeline/dateUtils.ts (barPosition) + src/lib/timeline/colorUtils.ts (luminanceFor)
    - src/components/timeline/MilestoneMarker.tsx + ProjectPopoverContent.tsx
  </read_first>
  <action>
    Create `ScheduledBar({ project, color, window })`. Compute `barPosition(project.startDate,
    project.targetDate, window)`. Render an absolutely positioned `h-7 rounded overflow-hidden`
    bar: `left: ${left}%`; width = `${width}%` normally. Special cases: (D-07) when
    startDate is null, right-align a fixed 64px bar to the targetDate position
    (`right: ${100 - rightPct}%; width: 64px`); (D-03 entirely-before-window, width<=0) render
    a 32px stub at column 0. When `clampedLeft` render a ChevronLeft (10px, `--muted-foreground`,
    aria-label "Continues before window start") at the left edge; when `clampedRight` render a
    ChevronRight (aria-label "Continues past window end") at the right edge — import both from
    `lucide-react`. Bar fill `backgroundColor: ${color}cc` (light) — use the dark-mode 70%
    variant per UI-SPEC; bar text color = `luminanceFor(color) < 0.4 ? "#ffffff" : "#1a1a1a"`,
    project name truncated, suppressed when the bar is narrower than 48px. Map each
    `project.milestones` (with a targetDate) to a `MilestoneMarker` positioned by its own
    targetDate offset; collapse markers within 12px to a cluster count. Wrap the bar in the
    same isTouch-driven HoverCard/Popover trigger as UndatedPill, child
    `<ProjectPopoverContent project={project} color={color} />`.
  </action>
  <acceptance_criteria>
    - `grep -c "barPosition" src/components/timeline/ScheduledBar.tsx` returns >= 1.
    - `grep -c "ChevronLeft\|ChevronRight" src/components/timeline/ScheduledBar.tsx` returns >= 1 and they import from "lucide-react".
    - `grep -c "64px" src/components/timeline/ScheduledBar.tsx` returns >= 1 (D-07 fixed width).
    - `grep -c "Continues before window start" src/components/timeline/ScheduledBar.tsx` returns >= 1.
    - File renders MilestoneMarker and ProjectPopoverContent.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Bar positions via barPosition, handles D-07 64px + D-03 clamp cues, renders markers + popover; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot data -> DOM positioning | Validated dates/colors drive inline `style` (left/width/background); no HTML injection path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | popover link inside bar/pill | mitigate | The link + guard live in ProjectPopoverContent (04-04, `rel=noopener noreferrer` + https://linear.app/ prefix); bar/pill only pass validated `project`. |
| T-04-B1 | Tampering | inline style values from data | accept | left/width/color derive from Zod-validated numeric/string snapshot fields and the hardcoded palette; no user-supplied style string. |
</threat_model>

<verification>
- `pnpm test && pnpm typecheck` green (util tests still pass; components typecheck).
- Visual/interaction fidelity verified in 04-06 (full render + human-check).
</verification>

<success_criteria>
- TL-01: scheduled projects render as positioned/clamped bars with markers.
- TL-02: undated projects render as dashed pills.
- TL-03: both open the shared popover via the correct pointer-type trigger.
</success_criteria>

<output>
Create `.planning/phases/04/04-05-SUMMARY.md` when done.
</output>
