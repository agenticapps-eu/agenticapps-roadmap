---
phase: 07
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07/07-HUMAN-UAT.md
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "snapshot.yml is confirmed to already satisfy LIVE-03: daily cron + workflow_dispatch + commit-roadmap.json-on-change + concurrency guard (D-07-08 verify-and-reuse, no rebuild)."
    - "A Phase-8 HUMAN-UAT artifact records the deferred live-path verifications (backfill dispatch/apply end-to-end, sibling checkout layout, scheduled cron run) and the R-1 secret prerequisites."
  artifacts:
    - path: ".planning/phases/07/07-HUMAN-UAT.md"
      provides: "Consolidated Phase-8 live-verification checklist for the LIVE-01..03 paths"
      contains: "GH_CROSS_REPO_TOKEN"
  key_links:
    - from: ".planning/phases/07/07-HUMAN-UAT.md"
      to: ".github/workflows/snapshot.yml and backfill.yml"
      via: "verification checklist referencing both workflows + required secrets"
      pattern: "LINEAR_API_KEY"
---

<objective>
Deliver LIVE-03 (verify-and-reuse) and consolidate the phase's deferred live checks:
confirm `.github/workflows/snapshot.yml` already provides the scheduled snapshot refresh
(D-07-08 — no rebuild), and record a Phase-8 HUMAN-UAT artifact for every live path this
phase builds but cannot verify without bound secrets (R-1).

Purpose: Close LIVE-03 by verification rather than new code, and give Phase 8 a single,
concrete checklist for the live-dispatch/apply/cron proofs — mirroring Phase 3's
Access-proof deferral and Phase 6's live-verify deferral.
Output: `.planning/phases/07/07-HUMAN-UAT.md`. This is a read-only/verification path;
no application code and no write path are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-RESEARCH.md
@.github/workflows/snapshot.yml
@.planning/phases/03/03-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify snapshot.yml satisfies LIVE-03 (D-07-08)</name>
  <files>.planning/phases/07/07-HUMAN-UAT.md</files>
  <read_first>
    - .github/workflows/snapshot.yml (confirm: `schedule: cron "0 6 * * *"`, `workflow_dispatch`, `concurrency` guard, and the commit-`public/roadmap.json`-on-change step).
    - .planning/phases/07/07-CONTEXT.md § "D-07-08" (the claims to check the file against).
    - .planning/phases/07/07-RESEARCH.md § "LIVE-03" row (verification, not new code).
  </read_first>
  <action>
    Inspect `snapshot.yml` and record in `07-HUMAN-UAT.md` a short verification block confirming each D-07-08 claim is present (daily 06:00 UTC cron, manual `workflow_dispatch`, `concurrency: snapshot`, commit-on-change of `public/roadmap.json`). If any claim is NOT present, note the exact gap instead of asserting it — do not modify `snapshot.yml` (D-07-08 is verify-and-reuse). State explicitly that LIVE-03's only runtime prerequisite is the Phase-8 `LINEAR_API_KEY` secret binding (R-1), already tracked since Phase 2.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.github/workflows/snapshot.yml','utf8'); for(const t of ['workflow_dispatch','cron:','concurrency','public/roadmap.json']){ if(!s.includes(t)){ console.error('snapshot.yml MISSING: '+t); process.exit(1);} } console.log('snapshot.yml satisfies LIVE-03 mechanisms');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check confirms snapshot.yml contains cron + workflow_dispatch + concurrency + the roadmap.json commit target (exit 0).
    - `07-HUMAN-UAT.md` records the per-claim verification result and the `LINEAR_API_KEY` Phase-8 prerequisite.
    - `snapshot.yml` is unmodified.
  </acceptance_criteria>
  <done>LIVE-03 is satisfied by confirming the existing scheduled-refresh mechanism, with its one Phase-8 secret dependency documented.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Phase-8 HUMAN-UAT checklist for the deferred live paths</name>
  <files>.planning/phases/07/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/03/03-HUMAN-UAT.md (the deferral doc format to mirror).
    - .planning/phases/07/07-RESEARCH.md § "Environment Availability" + "Open Questions" (the R-1 secrets, `return_run_details` uncertainty, job-logs timestamp uncertainty).
    - .planning/phases/07/07-VALIDATION.md § "Manual-Only Verifications".
  </read_first>
  <action>
    Extend `07-HUMAN-UAT.md` with a Phase-8 live-verification checklist covering: (1) bind `GH_BACKFILL_TOKEN` (Pages Function binding) and `GH_CROSS_REPO_TOKEN` + `LINEAR_API_KEY` (repo secrets), noting the fine-grained-PAT minimal scope (4 named `agenticapps-eu` repos: Actions:write on roadmap, Contents:read on all 4); (2) dispatch a dry-run via the UI, confirm the diff renders and the `___DIFF_JSON___` readback works; (3) confirm `backfill.yml`'s sibling checkout layout resolves `sync.config.json` relative paths in a real run (R-2); (4) apply one project, confirm the Linear write + committed refreshed `roadmap.json` (R-3, durable planAhead); (5) confirm a scheduled `snapshot.yml` cron run commits a fresh snapshot (LIVE-03 live); (6) verify `return_run_details` support and the job-logs timestamp-prefix behavior (Open Questions A2/A3) against a real run. Mark each item as BLOCKED-until-Phase-8 with the reason (secrets unbound). Do NOT bind any secret here — that is Phase 8.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/07/07-HUMAN-UAT.md','utf8'); for(const t of ['GH_BACKFILL_TOKEN','GH_CROSS_REPO_TOKEN','LINEAR_API_KEY','___DIFF_JSON___','roadmap.json','Phase 8']){ if(!s.includes(t)){ console.error('UAT MISSING: '+t); process.exit(1);} } console.log('07-HUMAN-UAT.md complete');"</automated>
  </verify>
  <acceptance_criteria>
    - `07-HUMAN-UAT.md` lists all six deferred live-verification items, each marked BLOCKED-until-Phase-8 with its secret prerequisite.
    - The minimal PAT scope (4 named repos) is documented.
    - No secret is bound and no workflow file is modified by this task.
  </acceptance_criteria>
  <done>Phase 8 has a single concrete checklist to verify the LIVE-01..03 live paths once secrets are bound.</done>
</task>

</tasks>

<verification>
- `snapshot.yml` confirmed to satisfy LIVE-03's mechanisms (automated check exit 0).
- `07-HUMAN-UAT.md` exists with the full deferred-live-verification checklist + secret prerequisites.
</verification>

<success_criteria>
LIVE-03 is satisfied by verified reuse of `snapshot.yml`, and every live path this phase
builds-but-cannot-verify is captured in a Phase-8 HUMAN-UAT artifact — no secret bound,
no code path left unaccounted for.
</success_criteria>

<output>
Create `.planning/phases/07/07-05-SUMMARY.md` when done.
</output>
