---
phase: 07
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-06"]
files_modified:
  - .planning/phases/07/07-HUMAN-UAT.md
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "snapshot.yml is confirmed to still satisfy LIVE-03's mechanism (daily cron + workflow_dispatch + commit-roadmap.json-on-change) AND now shares the roadmap-git-writer concurrency group with backfill.yml."
    - "LIVE-02 and LIVE-03 are recorded as OPERATIONALLY PENDING (code built, live behavior unverified until Phase-8 secrets) — 07-05 does NOT claim either complete."
    - "A Phase-8 HUMAN-UAT artifact records every deferred live proof, including unauth rejection, direct-apply-without-preview rejection, cancelled/failed-run rollback, token absence, 204 fallback, concurrent-writer behavior, and a real scheduled cron run."
    - "backfill.yml and snapshot.yml are verified beyond substring matching: valid YAML, correct if: branches, working-directory, and cache-dependency-path."
  artifacts:
    - path: ".planning/phases/07/07-HUMAN-UAT.md"
      provides: "Consolidated Phase-8 live-verification checklist for LIVE-01..03 + operationally-pending record"
      contains: "GH_CROSS_REPO_TOKEN"
  key_links:
    - from: ".planning/phases/07/07-HUMAN-UAT.md"
      to: ".github/workflows/snapshot.yml and backfill.yml"
      via: "verification checklist referencing both workflows + required secrets"
      pattern: "LINEAR_API_KEY"
---

<objective>
Deliver LIVE-03 (verify-and-reuse) and consolidate the phase's deferred live checks:
confirm `.github/workflows/snapshot.yml` still provides the scheduled snapshot refresh
(D-07-08, now serialized with backfill.yml), record that LIVE-02/LIVE-03 remain
OPERATIONALLY PENDING (code built this phase; live behavior needs Phase-8 secrets, R-1),
and write a single Phase-8 HUMAN-UAT checklist for every live proof this phase cannot run.

Purpose: Close LIVE-03's MECHANISM by verification rather than new code, without
overclaiming; give Phase 8 one concrete checklist for the live-dispatch/apply/cron proofs.
This runs LAST because the checklist references the finalized route/hook/UI/workflow
contracts from 07-01..07-04 and 07-06 (finding: 07-05 was ordered too early).
Output: `.planning/phases/07/07-HUMAN-UAT.md`. Verification-only; no application code touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-RESEARCH.md
@.planning/phases/07/07-REVIEWS.md
@.github/workflows/snapshot.yml
@.github/workflows/backfill.yml
@.planning/phases/03/03-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify snapshot.yml + backfill.yml structurally (beyond substring) and record LIVE-03 mechanism</name>
  <files>.planning/phases/07/07-HUMAN-UAT.md</files>
  <read_first>
    - .github/workflows/snapshot.yml (confirm: `schedule: cron "0 6 * * *"`, `workflow_dispatch`, the commit-`public/roadmap.json`-on-change step, and the NEW shared `concurrency: group: roadmap-git-writer` from 07-06).
    - .github/workflows/backfill.yml (confirm: the `if: ${{ inputs.mode == 'dry-run' }}` / `if: ${{ inputs.mode == 'apply' }}` branches, `working-directory: agenticapps-roadmap`, `cache-dependency-path: agenticapps-roadmap/pnpm-lock.yaml`, env-var project passing, and the shared concurrency group).
    - .planning/phases/07/07-CONTEXT.md § "D-07-08" (the claims to check) and 07-REVIEWS finding MEDIUM "workflow verification is substring-only".
  </read_first>
  <action>
    Inspect both workflows and record in `07-HUMAN-UAT.md` a verification block. For snapshot.yml confirm each D-07-08 claim (daily 06:00 UTC cron, manual `workflow_dispatch`, commit-on-change of `public/roadmap.json`) AND that its concurrency group is now the shared `roadmap-git-writer` (07-06's race fix). For backfill.yml, go BEYOND substring matching: confirm it parses as valid YAML, that both `if:` branches (`inputs.mode == 'dry-run'` and `inputs.mode == 'apply'`) are present, that git/pnpm steps carry `working-directory: agenticapps-roadmap`, that setup-node has `cache-dependency-path: agenticapps-roadmap/pnpm-lock.yaml`, and that no `${{ inputs.project }}` appears inside a `run:` command (env-var passing). If any claim is NOT present, record the exact gap instead of asserting it — do not modify either workflow (D-07-08 is verify-and-reuse; 07-06 owns backfill.yml). Recommend `actionlint` as the human-run validator for Phase-8. State explicitly that LIVE-03's only runtime prerequisite is the Phase-8 `LINEAR_API_KEY` binding (R-1), already tracked since Phase 2.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sn=fs.readFileSync('.github/workflows/snapshot.yml','utf8'); const bf=fs.readFileSync('.github/workflows/backfill.yml','utf8'); const needSn=['workflow_dispatch','cron:','public/roadmap.json','group: roadmap-git-writer']; const needBf=[\"inputs.mode == 'dry-run'\",\"inputs.mode == 'apply'\",'working-directory: agenticapps-roadmap','cache-dependency-path: agenticapps-roadmap/pnpm-lock.yaml','group: roadmap-git-writer']; for(const t of needSn){ if(!sn.includes(t)){ console.error('snapshot.yml MISSING: '+t); process.exit(1);} } for(const t of needBf){ if(!bf.includes(t)){ console.error('backfill.yml MISSING: '+t); process.exit(1);} } if(/run:[^\n]*\$\{\{\s*inputs\.project/.test(bf)){ console.error('backfill.yml UNSAFE inputs.project in run'); process.exit(1);} console.log('workflow structural verification OK');"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check confirms snapshot.yml (cron + workflow_dispatch + roadmap.json commit target + shared concurrency group) and backfill.yml (both if: branches + working-directory + cache-dependency-path + shared group + no unsafe run-interpolation) — exit 0.
    - `07-HUMAN-UAT.md` records the per-claim verification result, the beyond-substring structural checks, the `actionlint` recommendation, and the `LINEAR_API_KEY` Phase-8 prerequisite.
    - Neither workflow file is modified by this task.
  </acceptance_criteria>
  <done>LIVE-03's mechanism is re-confirmed (now race-serialized), and both workflows are structurally verified beyond substring — recorded without overclaiming.</done>
</task>

<task type="auto">
  <name>Task 2: Record LIVE-02/03 as operationally pending + the expanded Phase-8 HUMAN-UAT checklist</name>
  <files>.planning/phases/07/07-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/03/03-HUMAN-UAT.md (the deferral doc format to mirror).
    - .planning/phases/07/07-RESEARCH.md § "Environment Availability" + "Open Questions" (R-1 secrets, `return_run_details` uncertainty, job-logs timestamp uncertainty).
    - .planning/phases/07/07-REVIEWS.md finding #12 (phase claims exceed evidence — LIVE-02/03 operationally pending) + the "Expand Phase-8 UAT" list.
    - .planning/phases/07/07-VALIDATION.md § "Manual-Only Verifications" (if present).
  </read_first>
  <action>
    Add to `07-HUMAN-UAT.md` an explicit STATUS block stating LIVE-02 and LIVE-03 are OPERATIONALLY PENDING: their code paths are built and unit-tested this phase, but neither is verified complete until Phase-8 binds secrets and a real dispatch/write/snapshot/cron proof runs — 07-05 does NOT mark them complete. Then extend the Phase-8 live-verification checklist to cover: (1) bind `GH_BACKFILL_TOKEN` (Pages Function binding) and `GH_CROSS_REPO_TOKEN` + `LINEAR_API_KEY` (repo secrets), noting the fine-grained-PAT minimal scope (4 named `agenticapps-eu` repos: Actions:write on roadmap, Contents:read on all 4); (2) UNAUTHENTICATED rejection of `/api/backfill/dispatch` and `/api/backfill/status` (Access gate); (3) direct `{mode:"apply"}` POST WITHOUT a valid previewRunId is rejected 403 (server-side two-phase enforcement); (4) dispatch a dry-run via the UI, confirm the TYPED diff renders and the `___DIFF_JSON___` typed-counts readback works; (5) confirm `backfill.yml`'s sibling checkout resolves sync.config.json relative paths in a real run (R-2); (6) apply one project, confirm the Linear write + committed refreshed `roadmap.json` AND `linear-map.json`, and that the badge stays in-sync after reload (R-3, durable planAhead); (7) cancelled/failed-run rollback reverts the badge + shows the error; (8) GitHub token absence in every `/api/backfill/*` network response body; (9) the 204 `return_run_details` fallback → correlationId resolve path (Open Question A3); (10) concurrent-writer behavior: a backfill apply and a snapshot run do not race (shared `roadmap-git-writer` group) (finding #9); (11) a REAL scheduled `snapshot.yml` cron run commits a fresh snapshot (LIVE-03 live); (12) the job-logs timestamp-prefix behavior against a real run (Open Question A2). Mark each item BLOCKED-until-Phase-8 with its reason (secrets unbound). Do NOT bind any secret here.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/07/07-HUMAN-UAT.md','utf8'); for(const t of ['OPERATIONALLY PENDING','GH_BACKFILL_TOKEN','GH_CROSS_REPO_TOKEN','LINEAR_API_KEY','unauthenticated','previewRunId','___DIFF_JSON___','linear-map.json','roadmap.json','204','roadmap-git-writer','cron','Phase 8']){ if(!s.includes(t)){ console.error('UAT MISSING: '+t); process.exit(1);} } console.log('07-HUMAN-UAT.md complete');"</automated>
  </verify>
  <acceptance_criteria>
    - `07-HUMAN-UAT.md` states LIVE-02 and LIVE-03 are OPERATIONALLY PENDING and does not claim either complete.
    - The checklist lists all twelve deferred live-verification items (incl. unauth rejection, direct-apply-without-preview 403, cancelled/failed rollback, token absence, 204 fallback, concurrent-writer, real cron run), each BLOCKED-until-Phase-8 with its prerequisite.
    - The minimal PAT scope (4 named repos) is documented; no secret is bound and no workflow file is modified.
  </acceptance_criteria>
  <done>Phase 8 has a single concrete checklist to verify the LIVE-01..03 live paths once secrets are bound, and LIVE-02/03 are honestly recorded as operationally pending.</done>
</task>

</tasks>

<verification>
- Both workflows confirmed structurally (automated check exit 0), including the shared concurrency group and backfill.yml's if: branches / working-directory / cache-dependency-path.
- `07-HUMAN-UAT.md` exists with the operationally-pending record + the full expanded deferred-live checklist + secret prerequisites.
</verification>

<success_criteria>
LIVE-03's mechanism is satisfied by verified reuse of a now-race-serialized `snapshot.yml`;
LIVE-02 and LIVE-03 are recorded as operationally pending (not overclaimed); and every live
path this phase builds-but-cannot-verify is captured in a single Phase-8 HUMAN-UAT artifact.
</success_criteria>

<output>
Create `.planning/phases/07/07-05-SUMMARY.md` when done.
</output>
