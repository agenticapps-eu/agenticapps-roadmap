---
phase: 07
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/lib/backfill/projects.ts
  - src/components/overview/SyncBadge.tsx
  - src/components/overview/ProjectDrillDownDialog.tsx
  - src/pages/OverviewPage.tsx
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "Only projects with a known Linear-project-id → sync.config-key mapping show the Backfill control; it dispatches the config KEY, never the Linear display name."
    - "OverviewPage owns the optimistic backfill-state Map and threads it (unconditionally, before any guard) into SyncBadge and ProjectDrillDownDialog."
    - "The drill-down dialog exposes a Backfill: <project> control that previews the fresh dry-run diff (rendered from TYPED counts, no ANSI/regex parsing), then on Apply optimistically flips the project's SyncBadge in-sync + shows a 'backfilling…' pending indicator; Apply is disabled until a successful preview exists."
    - "On polled success the badge stays in-sync and pending clears; on failure/cancelled the badge reverts and a dismissible inline error is shown; an 'unknown' outcome clears pending without reverting or erroring."
  artifacts:
    - path: "src/lib/backfill/projects.ts"
      provides: "Linear-project-id → sync.config-key allow-map (client-side eligibility)"
      exports: ["BACKFILL_PROJECTS"]
    - path: "src/components/overview/SyncBadge.tsx"
      provides: "Additive planAheadOverride + pending props"
      contains: "planAheadOverride"
    - path: "src/components/overview/ProjectDrillDownDialog.tsx"
      provides: "Two-phase Backfill control (typed-diff preview → Apply) + dismissible inline error"
      contains: "useBackfill"
    - path: "src/pages/OverviewPage.tsx"
      provides: "Optimistic backfill-state Map owner threaded to children"
      contains: "backfillState"
  key_links:
    - from: "src/components/overview/ProjectDrillDownDialog.tsx"
      to: "src/lib/backfill/projects.ts"
      via: "BACKFILL_PROJECTS[project.id] resolves the config key + gates eligibility"
      pattern: "BACKFILL_PROJECTS"
    - from: "src/pages/OverviewPage.tsx"
      to: "src/lib/backfill/useBackfill.ts"
      via: "state Map + setter passed to ProjectDrillDownDialog"
      pattern: "backfillState"
---

<objective>
Wire the LIVE-02 optimistic UI: a Linear-id→config-key eligibility map, OverviewPage as the
ephemeral backfill-state Map owner, a two-phase Backfill control in the drill-down dialog
(preview typed diff → Apply), and SyncBadge rendering the optimistic override + pending
indicator (D-07-06, D-07-02).

Purpose: Make the write path usable and observable from the UI, with a correct UI→CLI
project-identity contract (finding #4), optimistic flip + rollback grounded in the one real
snapshot signal (planAhead / OV-04), and diff rendering from typed counts (RESOLUTION 1).
This is UI glue over the proven `useBackfill` core (07-03) and the `/api/backfill/*` routes
(07-02).
Output: new `src/lib/backfill/projects.ts`; modified `SyncBadge.tsx`,
`ProjectDrillDownDialog.tsx`, `OverviewPage.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-PATTERNS.md
@.planning/phases/07/07-REVIEWS.md
@.planning/phases/07/07-03-SUMMARY.md

<interfaces>
<!-- Contracts from 07-03 + the existing components. -->

From src/lib/backfill/useBackfill.ts (07-03) — the EXACT contract:
  useBackfill(setBackfillState) → {
    startPreview(projectKey: string): Promise<void>;
    applyBackfill(projectId: string, projectKey: string): Promise<void>;
    diffFor(projectId: string): { milestones: number; issues: number; labels: number; dates: number } | undefined;
    statusFor(projectId: string): "idle" | "previewing" | "pending" | "done" | "error" | "unknown";
    errorFor(projectId: string): string | undefined;
    clearError(projectId: string): void;
  }
  Optimistic-state Map value: { pendingBackfill: boolean; planAheadOverride?: boolean }.

PROJECT IDENTITY MAP (finding #4 — the missing UI→CLI contract). The snapshot carries Linear
DISPLAY names ("AgenticApps Roadmap", "cPARX Prototype", "fx-signals …"), but the CLI /
dispatch allow-list keys are sync.config keys (`claude-workflow`, `cparx`, `fx-signal-agent`).
Bridge them with an ID-based (rename-proof) map in `src/lib/backfill/projects.ts`:
  export const BACKFILL_PROJECTS: Record<string /*Linear project id*/, string /*sync.config key*/>
Seed it from the committed `linear-map.json` `projects` section (repoKey → { id }), inverted
to id → repoKey. Today that resolves ONLY `claude-workflow`
(id `28f29610-6662-46ad-a5d5-ca93a5c53872`); cparx/fx-signal-agent gain entries once first
applied and their ids land in linear-map.json. A project whose `project.id` is NOT a key in
this map is NOT backfill-eligible → no Backfill control rendered. The control dispatches
`BACKFILL_PROJECTS[project.id]` (the config KEY), never `project.name`.

Existing components (07-PATTERNS.md):
- SyncBadge: `({ project }: { project: Project })` → renders `<Badge variant="destructive">Out of sync with plan</Badge>` when `project.planAhead`, else null. Extend props ADDITIVELY.
- ProjectDrillDownDialog: `?project`-driven open/close via `useSearchParams`; SyncBadge mount at ~line 53; guarded Linear deep-link footer (`border-t border-(--color-border) pt-2`) at ~line 109-121 — place the Backfill control in a sibling footer section of the same style.
- OverviewPage: two unconditional hooks before the `if (!loaderData)` guard (~lines 24-40); SyncBadge in the project-list row (~line 99); ProjectDrillDownDialog mount (~line 109). New `useState` Map MUST be added before the guard (Rules-of-Hooks).

DIFF RENDER (RESOLUTION 1): `diffFor(projectId)` returns TYPED counts `{ milestones, issues,
labels, dates }`. Render `+ N milestones, + M issues, + L labels, ~ D dates` directly from
the object — NO ANSI stripping, NO regex parsing of CLI text on the UI side.

Error surface (07-PATTERNS "No Analog Found"): no toast component exists — use a minimal
inline DISMISSIBLE element (reuse `Badge variant="destructive"` or a small banner) wired to
`clearError(projectId)`; NOT a new toast library (Simplicity First). Styling/placement is
Claude's discretion (D-07-05/06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BACKFILL_PROJECTS eligibility map + additive optimistic props on SyncBadge</name>
  <files>src/lib/backfill/projects.ts, src/components/overview/SyncBadge.tsx</files>
  <read_first>
    - linear-map.json (the `projects` section: repoKey → { id } — the source of the id→key inversion).
    - sync.config.json (the three config keys the map values must be drawn from).
    - src/components/overview/SyncBadge.tsx (the current pure-render shape + the header comment's graceful-nullish discipline).
    - .planning/phases/07/07-REVIEWS.md finding #4 (UI→CLI project identity contract missing).
    - .planning/phases/07/07-PATTERNS.md § "src/components/overview/SyncBadge.tsx" (the exact additive prop surface).
  </read_first>
  <action>
    Create `src/lib/backfill/projects.ts` exporting `BACKFILL_PROJECTS: Record<string, string>` mapping Linear project id → sync.config key, seeded from `linear-map.json`'s `projects` map inverted to id→key (today: `{ "28f29610-6662-46ad-a5d5-ca93a5c53872": "claude-workflow" }`), with a short comment that new entries are added as cparx/fx-signal-agent are first applied. Then extend `SyncBadge`'s props ADDITIVELY to `{ project: Project; planAheadOverride?: boolean; pending?: boolean }`. Compute `outOfSync = planAheadOverride ?? project.planAhead` and render the existing destructive "Out of sync with plan" badge when `outOfSync` is truthy; when `pending`, also render a `<Badge variant="outline">backfilling…</Badge>`. Preserve the graceful-nullish contract (absent/false/null renders nothing, never throws). Do NOT change existing `{ project }`-only call sites' behavior — the new props are optional and default to current behavior.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
    <human-check>Badge shows "Out of sync" honoring the override, and a "backfilling…" pill when pending.</human-check>
  </verify>
  <acceptance_criteria>
    - `BACKFILL_PROJECTS` maps Linear project ids → sync.config keys (values ⊆ `["claude-workflow","cparx","fx-signal-agent"]`), seeded from linear-map.json, and includes the claude-workflow id.
    - SyncBadge props are additive/optional; `outOfSync = planAheadOverride ?? project.planAhead`; `pending` renders a distinct "backfilling…" indicator; nullish inputs render nothing.
    - `npx tsc -b --noEmit` clean; no `any`.
  </acceptance_criteria>
  <done>The UI has an id-based project-eligibility map, and SyncBadge can display the optimistic override + pending state without breaking existing call sites.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewPage owns the backfill-state Map; dialog gains the eligibility-gated two-phase Backfill control</name>
  <files>src/pages/OverviewPage.tsx, src/components/overview/ProjectDrillDownDialog.tsx</files>
  <read_first>
    - src/pages/OverviewPage.tsx (the unconditional-hooks-before-guard discipline; the SyncBadge row + ProjectDrillDownDialog mount).
    - src/components/overview/ProjectDrillDownDialog.tsx (the `useSearchParams` open/close, the SyncBadge mount point, the guarded Linear-link footer to mirror for the Backfill section).
    - src/lib/backfill/useBackfill.ts (the hook contract from 07-03) and src/lib/backfill/projects.ts (BACKFILL_PROJECTS, from Task 1).
    - .planning/phases/07/07-REVIEWS.md finding #4 (eligibility gate + config key), RESOLUTION 1 (typed diff render), finding MEDIUM (dismissible error / clearError).
    - .planning/phases/07/07-PATTERNS.md § "OverviewPage.tsx" and "ProjectDrillDownDialog.tsx".
  </read_first>
  <action>
    In `OverviewPage.tsx`, add `const [backfillState, setBackfillState] = useState<Map<string, { pendingBackfill: boolean; planAheadOverride?: boolean }>>(new Map())` UNCONDITIONALLY before the `if (!loaderData)` guard (Rules-of-Hooks). Pass `planAheadOverride`/`pending` from the Map entry into the project-list-row `SyncBadge`, and pass `backfillState` + `setBackfillState` into `ProjectDrillDownDialog`. In `ProjectDrillDownDialog.tsx`, call `useBackfill(setBackfillState)`; thread the Map entry for `project.id` into its `SyncBadge` mount (`planAheadOverride`/`pending`). Resolve `const backfillKey = BACKFILL_PROJECTS[project.id]` and render the Backfill control ONLY when `backfillKey` is defined (eligibility gate, finding #4). In a sibling `border-t border-(--color-border) pt-2` footer section: a `Backfill: {project.name}` button that calls `startPreview(backfillKey)`; render the preview from `diffFor(project.id)` as `+ N milestones, + M issues, + L labels, ~ D dates` (typed counts, no parsing); an `Apply` button that calls `applyBackfill(project.id, backfillKey)` and is DISABLED until `diffFor(project.id)` is defined (a successful preview must exist — server also enforces this). Render `errorFor(project.id)` as a minimal inline element with a dismiss action wired to `clearError(project.id)` (no toast library). Preserve the existing `useSearchParams` open/close shape unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && CI=true npx vitest run src/lib/backfill</automated>
    <human-check>In `pnpm dev` (`?source=live`), open an ELIGIBLE project's drill-down (e.g. the claude-workflow-mapped project): Backfill previews a typed diff, Apply (enabled only after preview) flips the badge to in-sync + shows "backfilling…"; a simulated failure reverts the badge and shows a dismissible inline error; an ineligible project shows NO Backfill control. Live end-to-end write is a Phase-8 UAT item.</human-check>
  </verify>
  <acceptance_criteria>
    - `backfillState` `useState` is declared before the `if (!loaderData)` guard (hook order preserved).
    - The Backfill control renders ONLY for projects with a `BACKFILL_PROJECTS[project.id]` entry and dispatches that config KEY (not the display name); Apply is disabled until `diffFor` is present.
    - The preview renders from typed counts (`+ N milestones, + M issues, + L labels, ~ D dates`) — no ANSI/regex parsing in the UI.
    - Rollback shows a DISMISSIBLE inline error wired to `clearError`; no toast/state library added.
    - `npx tsc -b --noEmit` clean; the 07-03 pure-core suite still passes.
  </acceptance_criteria>
  <done>The UI can preview + apply a per-project backfill only for eligible projects, sending the config key, rendering a typed diff, with a visible optimistic flip, dismissible-error rollback, and Apply gated on a successful preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → `/api/backfill/*` | The Backfill control triggers the write path; enforcement is the edge Access gate (D-07-03) + the server-side allow-list/preview-verify (07-02), not client code. The UI additionally gates eligibility client-side via BACKFILL_PROJECTS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | ProjectDrillDownDialog Backfill control | mitigate | The client sends only a config KEY resolved from `BACKFILL_PROJECTS[project.id]`; the 07-02 dispatch Function re-validates it against the server allow-list and enforces preview-before-apply; an unmatched project is a hard CI failure (create-only/idempotent CLI — bounded blast radius, D-06-03). |
| T-07-04 (inherited) | Spoofing | Backfill control reachability | accept | Access-gated at the edge (D-07-03); no new client auth logic. Proven end-to-end in Phase-8 UAT (07-05). |
| T-07-09 | Repudiation / stale optimism | optimistic Map (client-only) | accept | Optimistic state is client-only and lost on tab close; the CI job completes server-side and the next snapshot reflects true state (D-07-07). An "unknown" outcome intentionally does not revert, avoiding a duplicate dispatch over a possibly-live run. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean.
- `CI=true npx vitest run src/lib/backfill` still green.
- Human-check confirms eligibility-gated two-phase preview→apply + typed-diff render + optimistic flip/rollback + dismissible error.
</verification>

<success_criteria>
LIVE-02 UI is complete: an eligible per-project backfill can be previewed (fresh dry-run
typed diff) and applied (Apply gated on a successful preview) from the drill-down dialog,
sending the correct sync.config key, with an optimistic planAhead flip + "backfilling…"
pending state, a dismissible inline error on failure/cancelled, and a non-reverting
"unknown" path.
</success_criteria>

<output>
Create `.planning/phases/07/07-04-SUMMARY.md` when done.
</output>
