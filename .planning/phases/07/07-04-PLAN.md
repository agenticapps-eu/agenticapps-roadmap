---
phase: 07
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/components/overview/SyncBadge.tsx
  - src/components/overview/ProjectDrillDownDialog.tsx
  - src/pages/OverviewPage.tsx
autonomous: true
requirements: [LIVE-02]
must_haves:
  truths:
    - "OverviewPage owns the optimistic backfill-state Map and threads it (unconditionally, before any guard) into SyncBadge and ProjectDrillDownDialog."
    - "The drill-down dialog exposes a Backfill: <project> control that previews the fresh dry-run diff, then on Apply optimistically flips the project's SyncBadge in-sync + shows a 'backfilling…' pending indicator."
    - "On polled success the badge stays in-sync and pending clears; on failure/cancelled the badge reverts to out-of-sync and an inline error is shown."
    - "SyncBadge renders the override when provided and falls back to project.planAhead otherwise; existing call sites without the override are unaffected."
  artifacts:
    - path: "src/components/overview/SyncBadge.tsx"
      provides: "Additive planAheadOverride + pending props"
      contains: "planAheadOverride"
    - path: "src/components/overview/ProjectDrillDownDialog.tsx"
      provides: "Two-phase Backfill control (preview diff → Apply) + inline error"
      contains: "useBackfill"
    - path: "src/pages/OverviewPage.tsx"
      provides: "Optimistic backfill-state Map owner threaded to children"
      contains: "backfillState"
  key_links:
    - from: "src/pages/OverviewPage.tsx"
      to: "src/lib/backfill/useBackfill.ts"
      via: "state Map + setter passed to ProjectDrillDownDialog"
      pattern: "backfillState"
    - from: "src/components/overview/ProjectDrillDownDialog.tsx"
      to: "src/components/overview/SyncBadge.tsx"
      via: "planAheadOverride + pending props from the Map entry"
      pattern: "planAheadOverride"
---

<objective>
Wire the LIVE-02 optimistic UI: OverviewPage owns the ephemeral backfill-state Map, the
drill-down dialog gains the two-phase Backfill control (preview fresh diff → Apply), and
SyncBadge renders the optimistic override + pending indicator (D-07-06, D-07-02).

Purpose: Make the write path usable and observable from the UI, with optimistic flip +
rollback grounded in the one real snapshot signal (planAhead / OV-04). This is UI glue
over the proven `useBackfill` core (07-03) and the `/api/backfill/*` routes (07-02).
Output: modified `SyncBadge.tsx`, `ProjectDrillDownDialog.tsx`, `OverviewPage.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-PATTERNS.md
@.planning/phases/07/07-03-SUMMARY.md

<interfaces>
<!-- Contracts from 07-03 + the existing components. -->

From src/lib/backfill/useBackfill.ts (07-03):
- `useBackfill(setBackfillState)` → `{ startPreview(project), applyBackfill(projectId, project), statusFor(projectId), errorFor(projectId) }`.
- Optimistic-state Map value: `{ pendingBackfill: boolean; planAheadOverride?: boolean }`.

Existing components (07-PATTERNS.md):
- SyncBadge: `({ project }: { project: Project })` → renders `<Badge variant="destructive">Out of sync with plan</Badge>` when `project.planAhead`, else null. Extend props ADDITIVELY.
- ProjectDrillDownDialog: `?project`-driven open/close via `useSearchParams`; SyncBadge mount at ~line 53; guarded Linear deep-link footer (`border-t border-(--color-border) pt-2`) at ~line 109-121 — place the Backfill control in a sibling footer section of the same style.
- OverviewPage: two unconditional hooks before the `if (!loaderData)` guard (~lines 24-40); SyncBadge in the project-list row (~line 99); ProjectDrillDownDialog mount (~line 109). New `useState` Map MUST be added before the guard (Rules-of-Hooks).

Error surface (07-PATTERNS "No Analog Found"): no toast component exists — use a minimal
inline dismissible element (e.g. reuse `Badge variant="destructive"` or a small banner),
NOT a new toast library (Simplicity First). Styling/placement is Claude's discretion (D-07-05/06).

Two-phase UX (07-CONTEXT "Specific Ideas"): `[Backfill: <project>]` → preview dry-run →
render `+ N milestones, + M issues, ~ D dates` from the diff → `[Apply]` → optimistic flip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive optimistic props on SyncBadge</name>
  <files>src/components/overview/SyncBadge.tsx</files>
  <read_first>
    - src/components/overview/SyncBadge.tsx (the current pure-render shape + the header comment's graceful-nullish discipline).
    - .planning/phases/07/07-PATTERNS.md § "src/components/overview/SyncBadge.tsx" (the exact additive prop surface).
  </read_first>
  <action>
    Extend `SyncBadge`'s props ADDITIVELY to `{ project: Project; planAheadOverride?: boolean; pending?: boolean }`. Compute `outOfSync = planAheadOverride ?? project.planAhead` and render the existing destructive "Out of sync with plan" badge when `outOfSync` is truthy; when `pending`, also render a `<Badge variant="outline">backfilling…</Badge>`. Preserve the graceful-nullish contract (absent/false/null renders nothing, never throws). Do NOT change existing `{ project }`-only call sites' behavior — the new props are optional and default to current behavior.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
    <human-check>Badge shows "Out of sync" honoring the override, and a "backfilling…" pill when pending.</human-check>
  </verify>
  <acceptance_criteria>
    - Props are additive and optional; `outOfSync = planAheadOverride ?? project.planAhead`.
    - `pending` renders a distinct "backfilling…" indicator; nullish inputs render nothing.
    - `npx tsc -b --noEmit` clean; no `any`.
  </acceptance_criteria>
  <done>SyncBadge can display the optimistic override + pending state without breaking existing call sites.</done>
</task>

<task type="auto">
  <name>Task 2: OverviewPage owns the backfill-state Map; dialog gains the Backfill control</name>
  <files>src/pages/OverviewPage.tsx, src/components/overview/ProjectDrillDownDialog.tsx</files>
  <read_first>
    - src/pages/OverviewPage.tsx (the unconditional-hooks-before-guard discipline; the SyncBadge row + ProjectDrillDownDialog mount).
    - src/components/overview/ProjectDrillDownDialog.tsx (the `useSearchParams` open/close, the SyncBadge mount point, the guarded Linear-link footer to mirror for the Backfill section).
    - src/lib/backfill/useBackfill.ts (the hook API from 07-03).
    - .planning/phases/07/07-PATTERNS.md § "OverviewPage.tsx" and "ProjectDrillDownDialog.tsx".
  </read_first>
  <action>
    In `OverviewPage.tsx`, add `const [backfillState, setBackfillState] = useState<Map<string, { pendingBackfill: boolean; planAheadOverride?: boolean }>>(new Map())` UNCONDITIONALLY before the `if (!loaderData)` guard (Rules-of-Hooks — do not add a hook after the guard). Pass `planAheadOverride`/`pending` from the Map entry into the project-list-row `SyncBadge`, and pass `backfillState` + `setBackfillState` into `ProjectDrillDownDialog`. In `ProjectDrillDownDialog.tsx`, call `useBackfill(setBackfillState)`; thread the Map entry for `project.id` into its `SyncBadge` mount (`planAheadOverride`/`pending`); and add a Backfill control in a sibling `border-t border-(--color-border) pt-2` footer section: a `Backfill: {project.name}` button that calls `startPreview(project)` and renders the returned dry-run diff summary (`+ N milestones, + M issues, ~ D dates`), then an `Apply` button that calls `applyBackfill(project.id, project)` (optimistic flip via the Map). Render `errorFor(project.id)` as a minimal inline dismissible error element on rollback (no toast library). Preserve the existing `useSearchParams` open/close shape unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit && CI=true npx vitest run src/lib/backfill</automated>
    <human-check>In `pnpm dev` (`?source=live`), open a project drill-down: Backfill previews a diff, Apply flips the badge to in-sync + shows "backfilling…"; a simulated failure reverts the badge and shows the inline error. Live end-to-end write is a Phase-8 UAT item.</human-check>
  </verify>
  <acceptance_criteria>
    - `backfillState` `useState` is declared before the `if (!loaderData)` guard (hook order preserved).
    - The dialog renders a two-phase Backfill control (preview diff → Apply) and threads override/pending into SyncBadge.
    - Rollback shows an inline error element; no toast/state library added.
    - `npx tsc -b --noEmit` clean; the 07-03 pure-core suite still passes.
  </acceptance_criteria>
  <done>The UI can preview + apply a per-project backfill with a visible optimistic flip and rollback, wired to the proven core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → `/api/backfill/*` | The Backfill control triggers the write path; enforcement is the edge Access gate (D-07-03), not client code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | ProjectDrillDownDialog Backfill control | mitigate | The client sends only `project`/`mode`; the 07-02 dispatch Function validates them server-side, and an unmatched project is a hard CI failure (create-only/idempotent CLI — bounded blast radius, D-06-03). |
| T-07-04 (inherited) | Spoofing | Backfill control reachability | accept | Access-gated at the edge (D-07-03); no new client auth logic. Proven end-to-end in Phase-8 UAT (07-05). |
| T-07-09 | Repudiation / stale optimism | optimistic Map (client-only) | accept | Optimistic state is client-only and lost on tab close; the CI job completes server-side and the next snapshot reflects true state (D-07-07) — acceptable per decision. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean.
- `CI=true npx vitest run src/lib/backfill` still green.
- Human-check confirms the two-phase preview→apply flow + optimistic flip/rollback.
</verification>

<success_criteria>
LIVE-02 UI is complete: a per-project backfill can be previewed (fresh dry-run diff) and
applied from the drill-down dialog, with an optimistic planAhead flip + "backfilling…"
pending state and full rollback + inline error on failure/cancelled.
</success_criteria>

<output>
Create `.planning/phases/07/07-04-SUMMARY.md` when done.
</output>
