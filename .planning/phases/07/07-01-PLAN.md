---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/roadmap/loader.ts
  - src/lib/roadmap/loader.test.ts
  - src/lib/roadmap/freshness.ts
  - src/lib/roadmap/freshness.test.ts
  - src/components/AppHeader.tsx
autonomous: true
requirements: [LIVE-01]
must_haves:
  truths:
    - "In Live mode, clicking Refresh re-runs the root loader and full-replaces the rendered dataset with a fresh /api/linear/snapshot projection."
    - "shouldRevalidateRoadmap allows an explicit same-URL revalidate through, and still suppresses filter/drill-down (?project, ?status) navigations."
    - "The Refresh control renders only in Live mode; Snapshot mode stays network-free with no Refresh button."
    - "A 'last refreshed' hint derived from the snapshot timestamp is shown next to the control in Live mode."
  artifacts:
    - path: "src/lib/roadmap/loader.ts"
      provides: "Additive R-4 fix to shouldRevalidateRoadmap (same-URL revalidate returns true)"
      contains: "shouldRevalidateRoadmap"
    - path: "src/lib/roadmap/freshness.ts"
      provides: "Pure formatFreshness(generatedAt, now) → human 'updated Nh ago' string"
      exports: ["formatFreshness"]
    - path: "src/components/AppHeader.tsx"
      provides: "Live-mode-only Refresh button (useRevalidator) + freshness hint"
      contains: "useRevalidator"
  key_links:
    - from: "src/components/AppHeader.tsx"
      to: "react-router-dom useRevalidator"
      via: "revalidator.revalidate() onClick"
      pattern: "useRevalidator"
    - from: "shouldRevalidateRoadmap"
      to: "roadmapLoader re-run"
      via: "returns true on identical currentUrl/nextUrl"
      pattern: "shouldRevalidateRoadmap"
---

<objective>
Deliver LIVE-01: a "Refresh from Linear" control that, in Live mode, re-pulls the
live `/api/linear/snapshot` projection and full-replaces the rendered dataset in
memory (never writes `roadmap.json`), plus a "last refreshed" freshness hint.

Purpose: Give the user an on-demand, ephemeral refresh without leaving snapshot-first
mode (D-07-04, D-07-05). This is a read-only path — it does not touch the write path.
Output: The R-4 loader fix (with a failing-test-first proof), a pure freshness helper,
and the wired Refresh button + hint in `AppHeader`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07/07-CONTEXT.md
@.planning/phases/07/07-RESEARCH.md
@.planning/phases/07/07-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. No exploration required. -->

From src/lib/roadmap/loader.ts (current, to be modified):
- `export type RoadmapLoaderData = { data: RoadmapJson; live: boolean; liveUnavailable: boolean }`
- `roadmapLoader({ request })` — unchanged in this plan.
- `shouldRevalidateRoadmap({ currentUrl, nextUrl }: ShouldRevalidateFunctionArgs): boolean`
  currently returns `sourceMode(currentUrl) !== sourceMode(nextUrl)` — the R-4 bug.

From src/components/AppHeader.tsx (current):
- Derives `live = params.get("source") === "live"` via `useSearchParams`.
- Reads `useRouteLoaderData("root") as RoadmapLoaderData | null` and `useNavigation()`.
- Right-side slot is a `<div className="ml-auto flex items-center gap-3">` holding the
  `liveUnavailable` notice + the Snapshot/Live toggle button.

React Router 7 (installed 7.18.0):
- `useRevalidator()` → `{ revalidate(): void; state: "idle" | "loading" }`.
  An explicit `revalidate()` re-navigates to the identical current URL, so
  `shouldRevalidateRoadmap` sees `currentUrl === nextUrl` byte-for-byte (R-4, RESEARCH Pitfall 1).

Snapshot timestamp: confirm the exact field name in src/lib/roadmap/schema.ts
(D-07-05 refers to it as `generatedAt`) — freshness.ts consumes it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix shouldRevalidateRoadmap for explicit same-URL revalidate (R-4)</name>
  <files>src/lib/roadmap/loader.test.ts, src/lib/roadmap/loader.ts</files>
  <read_first>
    - src/lib/roadmap/loader.ts (the `shouldRevalidateRoadmap` function, lines ~83-91 — the only block that changes; `roadmapLoader` stays untouched).
    - src/lib/roadmap/loader.test.ts (the existing `rargs()` helper and the `describe("shouldRevalidateRoadmap")` block — extend, do not replace).
    - .planning/phases/07/07-RESEARCH.md § "Pitfall 1" and "Pattern 3" (the verified additive fix).
    - .planning/phases/07/07-PATTERNS.md § "src/lib/roadmap/loader.ts" and "loader.test.ts" (the exact new test cases).
  </read_first>
  <behavior>
    - RED first: add a failing test asserting `shouldRevalidateRoadmap(rargs("/?source=live", "/?source=live"))` returns `true` (explicit same-URL revalidate in Live mode).
    - Add a test asserting `shouldRevalidateRoadmap(rargs("/", "/"))` returns `true` (same-URL revalidate in Snapshot mode).
    - Existing suppression cases must stay green: `rargs("/?source=live", "/?source=live&status=active")` returns `false` (filter navigation), and the source-flip case returns `true`.
  </behavior>
  <action>
    Write the two failing test cases FIRST in loader.test.ts using the existing `rargs()` helper, run them to confirm RED, then apply the additive fix to `shouldRevalidateRoadmap`: keep the existing `sourceMode(currentUrl) !== sourceMode(nextUrl)` early-return-true branch unchanged, then add a second branch that returns `true` when `currentUrl.pathname + currentUrl.search === nextUrl.pathname + nextUrl.search` (identical-URL explicit revalidate), otherwise `false`. Do NOT replace the function with `defaultShouldRevalidate` (that would regress the Phase-5 zero-network filter suppression — RESEARCH anti-pattern). Preserve every existing test in the file unchanged.
  </action>
  <verify>
    <automated>CI=true npx vitest run src/lib/roadmap/loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - loader.test.ts contains the two new same-URL-revalidate cases and they assert `true`.
    - The pre-existing filter-suppression and source-flip cases still pass unchanged.
    - `shouldRevalidateRoadmap` retains BOTH branches (source-mode-flip AND identical-URL); no wholesale replacement.
    - Full command exits 0.
  </acceptance_criteria>
  <done>The R-4 bug is proven fixed by an additive change; explicit revalidate re-runs the loader while filter navigations stay suppressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh button + freshness hint in AppHeader (Live-mode-only)</name>
  <files>src/lib/roadmap/freshness.ts, src/lib/roadmap/freshness.test.ts, src/components/AppHeader.tsx</files>
  <read_first>
    - src/components/AppHeader.tsx (the `live` derivation, the `useRouteLoaderData("root")` read, and the `<div className="ml-auto flex items-center gap-3">` slot — insert the button + hint here, matching the existing toggle button's Tailwind class style).
    - src/lib/roadmap/schema.ts (confirm the snapshot timestamp field name that `formatFreshness` consumes — D-07-05 calls it `generatedAt`).
    - .planning/phases/07/07-PATTERNS.md § "src/components/AppHeader.tsx" (the exact insertion point + Live-mode gate).
    - .planning/phases/07/07-RESEARCH.md § "Pattern 3" (the `useRevalidator` wiring).
  </read_first>
  <behavior>
    - `formatFreshness(generatedAt, now)`: given an ISO timestamp and a reference `Date`, returns a short human string ("updated just now" for <60s, "updated Nm ago", "updated Nh ago", "updated Nd ago"). Deterministic — no `Date.now()` inside; `now` is injected so the test is stable.
    - Invalid/empty timestamp returns a safe fallback string, never throws.
  </behavior>
  <action>
    Create the pure, React-free `src/lib/roadmap/freshness.ts` exporting `formatFreshness(generatedAt: string, now: Date): string` and write `freshness.test.ts` covering the <60s, minutes, hours, days, and invalid-input cases (node-runnable `.ts`, matching this repo's pure-logic test convention — the repo has no DOM test environment, so UI rendering is not unit-tested here). Then modify `AppHeader.tsx`: add `const revalidator = useRevalidator()` and `const refreshing = revalidator.state === "loading"`; inside the existing `ml-auto` slot, render a Live-mode-only (`{live && ...}`) fragment containing a freshness `<span>` fed by `formatFreshness(loaderData.data.generatedAt, new Date())` and a Refresh `<button>` (`aria-label="Refresh from Linear"`) that calls `revalidator.revalidate()` onClick and is `disabled={refreshing}`, showing "Refreshing…" while loading. Do not add a Refresh button in Snapshot mode (keeps snapshot path network-free, D-07-05). Match the existing toggle button's class-array styling; do not introduce a new styling convention.
  </action>
  <verify>
    <automated>CI=true npx vitest run src/lib/roadmap/freshness.test.ts && npx tsc -b --noEmit</automated>
    <human-check>In `pnpm dev` with `?source=live`, the Refresh button appears with a freshness hint; clicking it re-fetches (Network shows a `/api/linear/snapshot` call) and the button shows "Refreshing…"; in Snapshot mode no Refresh button is present.</human-check>
  </verify>
  <acceptance_criteria>
    - `freshness.ts` is React-free and `freshness.test.ts` passes in the node vitest env (0 exit).
    - `npx tsc -b --noEmit` reports no type errors (no `any`).
    - AppHeader renders the Refresh button and freshness hint only when `live` is true; the onClick calls `revalidator.revalidate()`.
    - Snapshot mode produces no Refresh button (verified by reading the render gate).
  </acceptance_criteria>
  <done>Live mode exposes a working Refresh control + freshness hint; snapshot mode is unchanged and network-free.</done>
</task>

</tasks>

<verification>
- `CI=true npx vitest run src/lib/roadmap` is green (loader + freshness).
- `npx tsc -b --noEmit` clean.
- Human-check confirms the Live-mode Refresh visibly re-pulls and swaps the dataset.
</verification>

<success_criteria>
LIVE-01 satisfied: in Live mode the Refresh control reconciles fresh live data into the
rendered view (full dataset replace via the existing loader), the R-4 suppression bug is
fixed additively, and snapshot mode stays network-free with no Refresh affordance.
</success_criteria>

<output>
Create `.planning/phases/07/07-01-SUMMARY.md` when done.
</output>
