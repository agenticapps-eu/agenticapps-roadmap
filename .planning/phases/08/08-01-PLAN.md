---
phase: 08-deploy-gate-document
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wrangler.toml
  - functions/api/backfill/dispatch.ts
  - functions/api/backfill/dispatch.test.ts
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "A reused previewRunId authorizes at most one apply — a second apply with the same previewRunId is rejected 403 without dispatching a GitHub run."
    - "The wrangler.toml declares a single BACKFILL_NONCE KV binding the dispatch Function reads."
    - "The 15-minute recency bound (MAX_PREVIEW_AGE_MS) still rejects stale previewRunIds as defense-in-depth (unchanged)."
  artifacts:
    - path: "wrangler.toml"
      provides: "BACKFILL_NONCE KV namespace binding for Pages"
      contains: "[[kv_namespaces]]"
    - path: "functions/api/backfill/dispatch.ts"
      provides: "consume-once nonce (check-then-set) on the apply write path"
      contains: "BACKFILL_NONCE"
    - path: "functions/api/backfill/dispatch.test.ts"
      provides: "consume-once unit tests (reuse -> 403; distinct ids -> both succeed)"
      contains: "BACKFILL_NONCE"
  key_links:
    - from: "functions/api/backfill/dispatch.ts (mode === 'apply' branch)"
      to: "env.BACKFILL_NONCE"
      via: "get(previewRunId) -> 403 if present; put(previewRunId, TTL 900) before dispatch"
      pattern: "env\\.BACKFILL_NONCE\\.(get|put)"
---

<objective>
Close CR-01's replay gap (D-08-06) before the write path first goes live: add a
Cloudflare KV binding to `wrangler.toml` and a consume-once nonce to the `apply`
branch of `functions/api/backfill/dispatch.ts`, so a `previewRunId` authorizes
**exactly one** apply. This is the only genuinely new code in Phase 8.

Purpose: A recent-but-reused preview can currently authorize multiple applies
within the 15-minute window; the nonce makes a `previewRunId` single-use. Resolves
`07-HUMAN-UAT.md` item #13.
Output: KV binding in `wrangler.toml`, check-then-set nonce in `dispatch.ts`, and
unit tests proving reuse is rejected without a second GitHub dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08/08-CONTEXT.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-PATTERNS.md

<interfaces>
<!-- Current Env interface (functions/api/backfill/dispatch.ts:19-21) — extend, don't replace: -->
interface Env {
  GH_BACKFILL_TOKEN: string;
}
<!-- Existing reject-helper already defined in the file (reuse verbatim for the 403): -->
function textResponse(body: string, status: number): Response  // sets Cache-Control: no-store
<!-- Insertion point: dispatch.ts:215 opens the single try; the mode === "apply" block runs
     lines 216-231. isValidPreviewRun passes at line 228-230; the shared dispatch POST is line
     233-248. The nonce check-then-set goes AFTER line 230's isValidPreviewRun check and BEFORE
     line 233's correlationId/dispatch, inside the same existing try block. -->
<!-- The TODO to replace lives at dispatch.ts:97-100, just below MAX_PREVIEW_AGE_MS (line 95). -->
<!-- KVNamespace type comes from @cloudflare/workers-types (already in tsconfig.functions.json). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the KV binding and implement the consume-once nonce</name>
  <files>wrangler.toml, functions/api/backfill/dispatch.ts</files>
  <read_first>
    - wrangler.toml (current 3-line flat config, no [env.*] blocks)
    - functions/api/backfill/dispatch.ts (Env interface line 19-21; MAX_PREVIEW_AGE_MS + TODO lines 92-100; textResponse line ~179-181; the mode === "apply" branch lines 215-248)
    - functions/api/linear/[[path]].ts (one-field-per-binding Env convention)
    - .planning/phases/08/08-PATTERNS.md (dispatch.ts section: exact insertion point + Surgical-Changes exclusions)
    - .planning/phases/08/08-RESEARCH.md (Pattern 1 check-then-set + Pattern 2 wrangler.toml block)
  </read_first>
  <action>
    Append a single top-level `[[kv_namespaces]]` block to wrangler.toml (no `[env.production]`/`[env.preview]` split, matching the file's current flat shape): `binding = "BACKFILL_NONCE"` and `id = "PLACEHOLDER_FILLED_IN_08-03"` with an inline comment that the real id is produced out-of-band in plan 08-03 via `npx --yes wrangler@4 kv namespace create BACKFILL_NONCE`.

    In dispatch.ts: add `BACKFILL_NONCE: KVNamespace;` as a second field on `interface Env` (flat idiom, matching functions/api/linear/[[path]].ts). Replace the `TODO(phase-8)` comment block (lines 97-100) with `const NONCE_TTL_SECONDS = 900;` and a one-line comment noting 900s matches MAX_PREVIEW_AGE_MS (15 min) — the nonce never needs to outlive the recency bound. Do NOT touch MAX_PREVIEW_AGE_MS itself (stays as defense-in-depth per D-08-06).

    Inside the existing `mode === "apply"` branch, immediately after the `isValidPreviewRun` check at line 230 and before the shared dispatch call at line 233 (inside the same single try block — no new try/catch): compute `const nonceKey = \`previewRunId:${previewRunId}\`;`, `const consumed = await env.BACKFILL_NONCE.get(nonceKey);` — if `consumed !== null` return `textResponse("preview already applied", 403)`; otherwise `await env.BACKFILL_NONCE.put(nonceKey, "1", { expirationTtl: NONCE_TTL_SECONDS });` before falling through to the dispatch. The KV value is the opaque marker `"1"` only — never a token or secret.

    Do NOT modify the rate limiter, the input-validation/allow-list guards, or the dispatch POST body (Surgical Changes — the nonce is additive inside the apply branch only).
  </action>
  <acceptance_criteria>
    - AUTOMATED: `grep -q '\[\[kv_namespaces\]\]' wrangler.toml` and `grep -q 'BACKFILL_NONCE' wrangler.toml` both succeed.
    - AUTOMATED: `grep -q 'BACKFILL_NONCE: KVNamespace' functions/api/backfill/dispatch.ts` and `grep -q 'expirationTtl: NONCE_TTL_SECONDS' functions/api/backfill/dispatch.ts` both succeed.
    - AUTOMATED: no `TODO(phase-8)` remains — `grep -c 'TODO(phase-8)' functions/api/backfill/dispatch.ts` returns 0.
    - AUTOMATED: `MAX_PREVIEW_AGE_MS = 15 * 60 * 1000` still present (recency bound untouched).
    - AUTOMATED: `npx tsc -b --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
  </verify>
  <done>KV binding declared in wrangler.toml (placeholder id); Env has BACKFILL_NONCE; check-then-set nonce inserted at the apply branch with TTL 900; recency bound untouched; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume-once nonce unit tests</name>
  <files>functions/api/backfill/dispatch.test.ts</files>
  <behavior>
    - Test 1: after a first successful apply's `put()`, a second apply reusing the SAME previewRunId returns 403 with body "preview already applied", and the dispatch `fetch` (workflows/backfill.yml/dispatches) is NOT called for the second apply.
    - Test 2: two DIFFERENT previewRunIds each authorize their own apply successfully (both dispatch).
    - Test 3: the 403-consumed response body never matches TOKEN_REGEX (`/ghp_|github_pat_/`) and carries `Cache-Control: no-store`.
  </behavior>
  <read_first>
    - functions/api/backfill/dispatch.test.ts (the `ctx(...)` mock-env helper lines 20-29; the CR-01 recency test inside `describe("apply with invalid preview run ...")` lines ~227-248; the "token never in body" block lines ~449-513; the "Cache-Control: no-store" block lines ~404-443; `stubFetchSequence`/`goodPreviewRun` helpers lines 31-57)
    - .planning/phases/08/08-PATTERNS.md (dispatch.test.ts section: Map-backed fake KVNamespace; use option (a) — keep ctx's 2-arg shape, add the kv mock into the env object — to avoid touching ~30 existing call sites)
    - .planning/phases/08/08-RESEARCH.md (Pattern 1 test snippet)
  </read_first>
  <action>
    Extend the existing `ctx(...)` helper so the env object carries a Map-backed fake `BACKFILL_NONCE` KVNamespace (`get`/`put` as `vi.fn`, the only two methods dispatch.ts calls). Use PATTERNS.md option (a): keep `ctx`'s current 2-arg `(body, env)` shape and build the kv mock inside the default env so the ~30 existing call sites need no change; allow tests that need a pre-consumed key to pass an env whose BACKFILL_NONCE is seeded. Add a new `describe("consume-once nonce (D-08-06)", ...)` block mirroring the CR-01 recency test's structure and the existing "apply dispatch success" block. Add the new 403-consumed status to the existing "token never in any response body" and "Cache-Control: no-store on every response" describe blocks (using the established `bodyOf` + `TOKEN_REGEX` pattern). Assert in Test 1 that the dispatch-step `fetch` mock is not invoked a second time (the reused id short-circuits before dispatch).
  </action>
  <acceptance_criteria>
    - AUTOMATED: `CI=true npx vitest run functions/api/backfill/dispatch.test.ts` is green, including the new nonce describe block.
    - AUTOMATED: the reuse test asserts the dispatch fetch is called exactly once across the two apply attempts (second attempt short-circuits at 403).
    - AUTOMATED: full suite `CI=true npx vitest run` is green (no regression in status/proxy/client code).
  </acceptance_criteria>
  <verify>
    <automated>CI=true npx vitest run functions/api/backfill/dispatch.test.ts</automated>
  </verify>
  <done>Consume-once behavior proven: reused previewRunId -> 403 with no second dispatch; two distinct ids both succeed; 403-consumed leaks no token and is no-store; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/backfill/dispatch | Access-gated caller submits {project, mode, previewRunId}; the apply write path is the highest blast-radius surface (pushes to main + writes Linear). |
| dispatch.ts → Cloudflare KV | The Function reads/writes the durable "consumed" fact for a previewRunId. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege / Tampering | reused/stale `previewRunId` authorizing multiple applies (CR-01) | mitigate | KV consume-once check-then-set on the apply branch (this plan) + 15-min recency bound retained as defense-in-depth (D-08-06). Proven by Task 2's reuse-rejected test. |
| T-08-02 | Information Disclosure | secret value written into the KV namespace | mitigate | KV value is the opaque marker `"1"` only; the PAT/LINEAR_API_KEY are never placed in KV (RESEARCH Security Domain V6). Enforced by code review + Task 2 token-leak assertion. |
| T-08-03 | Denial of Service / consistency | KV lacks compare-and-swap; a same-millisecond double-request could both pass the check | accept | Accepted, documented risk for the small trusted email allow-list (D-08-02) — CR-01's concern is naive/accidental reuse in-window, not a concurrent attacker. Recorded in the ADR (plan 08-02). D1/Durable Object escalation is out of scope. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — the KV binding is a Cloudflare platform primitive, not a registry package (RESEARCH Package Legitimacy Audit: N/A). No slopcheck gate required. |
</threat_model>

<verification>
- `npx tsc -b --noEmit` clean.
- `CI=true npx vitest run functions/api/backfill/dispatch.test.ts` green (new nonce cases).
- `CI=true npx vitest run` full suite green.
- `grep` gates in acceptance_criteria pass (binding present, TODO gone, recency bound intact).
</verification>

<success_criteria>
The dispatch apply path consumes a previewRunId exactly once (reuse → 403 without dispatch),
the KV binding is declared in wrangler.toml, the recency bound is preserved, and the full test
suite plus typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/08/08-01-SUMMARY.md` when done.
</output>
