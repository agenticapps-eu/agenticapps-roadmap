---
phase: 08-deploy-gate-document
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/0001-hosting-and-sync-architecture.md
  - docs/runbook.md
  - README.md
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04]
must_haves:
  truths:
    - "A reader can follow docs/runbook.md to deploy, rotate the token(s), understand snapshot refresh, and run a backfill without reading source."
    - "docs/decisions/0001-hosting-and-sync-architecture.md records the Cloudflare Pages + CI-dispatch hosting/sync decision, its rejected alternatives, and the accepted KV/PAT-scope risks."
    - "README documents where GH_BACKFILL_TOKEN, GH_CROSS_REPO_TOKEN, and BACKFILL_NONCE live and points to the runbook."
  artifacts:
    - path: "docs/decisions/0001-hosting-and-sync-architecture.md"
      provides: "hosting/sync ADR for the v0.1.0 tag (DEPLOY-04)"
      contains: "## Alternatives Rejected"
    - path: "docs/runbook.md"
      provides: "deploy / token-rotation / snapshot-refresh / backfill operational runbook (DEPLOY-03)"
      contains: "rotation"
    - path: "README.md"
      provides: "environment/secrets summary + Deploy pointer to the runbook"
      contains: "GH_BACKFILL_TOKEN"
  key_links:
    - from: "README.md ## Deploy section"
      to: "docs/runbook.md"
      via: "markdown link"
      pattern: "docs/runbook\\.md"
    - from: "docs/runbook.md"
      to: ".planning/phases/07/07-HUMAN-UAT.md"
      via: "reference-by-name (do not duplicate the checklist)"
      pattern: "07-HUMAN-UAT"
---

<objective>
Write the operator documentation that DEPLOY-03/DEPLOY-04 require: a hosting/sync
**ADR** (first entry in a new `docs/decisions/` directory), an operational
**`docs/runbook.md`** (deploy, token rotation, snapshot refresh, backfill), and
surgical **README** additions (secrets summary + a Deploy pointer). No code.

Purpose: The v0.1.0 tag (plan 08-03) is gated on the ADR existing and the runbook
covering the four DEPLOY-03 areas. This plan produces those artifacts so the live
plan only has to bind, verify, and tag.
Output: `docs/decisions/0001-hosting-and-sync-architecture.md`, `docs/runbook.md`,
and extended `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08/08-CONTEXT.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-PATTERNS.md
@docs/architecture.md
@docs/access-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the hosting/sync ADR</name>
  <files>docs/decisions/0001-hosting-and-sync-architecture.md</files>
  <read_first>
    - ../claude-workflow/docs/decisions/0010-backend-language-routing-go.md (cross-repo ADR format: header block + section skeleton)
    - docs/architecture.md ("Why Cloudflare Pages over GitHub Pages" + Decisions table — source prose)
    - .planning/phases/08/08-CONTEXT.md (D-08-01..06 with their inline "Rejected:" alternatives)
    - .planning/phases/07/07-CONTEXT.md (D-07-01 CI dispatch, D-07-02 two-phase, D-07-03 Access-only write auth)
    - .planning/phases/08/08-RESEARCH.md (Pitfall 2 uniform-PAT scope; Pitfall 4 KV eventual-consistency — accepted risks for Consequences)
    - .planning/phases/08/08-PATTERNS.md (ADR header/section skeleton)
  </read_first>
  <action>
    Create the `docs/decisions/` directory and `0001-hosting-and-sync-architecture.md`. Mirror the sibling ADR's four metadata lines exactly: `**Status:** Accepted`, `**Date:** 2026-07-16`, `**Linear:** —` (or the AgenticApps Roadmap Linear project link if known), `**Phase:** Phase 8 of agenticapps-roadmap`. Use the H2 section order: `## Context`, `## Decision`, `## Alternatives Rejected`, `## Consequences` (with `**Positive:**`/`**Negative:**`/`**Follow-ups:**` sub-bullets), `## References`. Title: `# ADR-0001: Hosting and sync architecture`.

    `## Context` + `## Decision` record the hosting choice (Cloudflare Pages + Pages Functions, snapshot-first zero-network default) and the sync architecture (CI-dispatch write path D-07-01, two-phase preview→apply D-07-02, Access-only write authorization D-07-03, single fine-grained PAT D-08-03, KV consume-once nonce D-08-06). `## Alternatives Rejected` draws on the inline Rejected notes in 08-CONTEXT (public `/api/linear/*`, split least-privilege PATs, recency-only nonce) and 07-CONTEXT (Worker-direct-write, diff-from-scheduled-artifact). `## Consequences` MUST document two accepted risks: (1) the KV nonce is best-effort (eventual consistency, no compare-and-swap — RESEARCH Pitfall 4); (2) the single fine-grained PAT grants Contents:Read + Actions:Read/Write uniformly across all 4 agenticapps-eu repos, not scoped to one (RESEARCH Pitfall 2), mitigated by the hardcoded `REPO` constant in dispatch.ts/status.ts. `## References` links docs/architecture.md, docs/runbook.md, and the phase CONTEXT files. This is a compilation/backfill ADR (recording a shipped decision), not a proposal.
  </action>
  <acceptance_criteria>
    - AUTOMATED: `test -f docs/decisions/0001-hosting-and-sync-architecture.md`.
    - AUTOMATED: all five H2 sections present — `grep -c '^## ' docs/decisions/0001-hosting-and-sync-architecture.md` returns 5.
    - AUTOMATED: `grep -q '^\*\*Status:\*\* Accepted' docs/decisions/0001-hosting-and-sync-architecture.md` and `grep -q 'Phase 8' docs/decisions/0001-hosting-and-sync-architecture.md`.
    - AUTOMATED: accepted-risk coverage — `grep -qi 'eventual' docs/decisions/0001-hosting-and-sync-architecture.md` (KV) and `grep -qi 'all 4' docs/decisions/0001-hosting-and-sync-architecture.md` (PAT scope).
    - HUMAN (at tag time, plan 08-03): prose is accurate against the actual shipped decisions.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/decisions/0001-hosting-and-sync-architecture.md && [ "$(grep -c '^## ' docs/decisions/0001-hosting-and-sync-architecture.md)" -ge 5 ]</automated>
  </verify>
  <done>ADR-0001 created with the sibling-repo header + 5-section skeleton; hosting + sync decisions recorded; rejected alternatives and the two accepted risks (KV consistency, uniform PAT scope) documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/runbook.md (deploy / rotation / refresh / backfill)</name>
  <files>docs/runbook.md</files>
  <read_first>
    - docs/access-setup.md (numbered-H2-step + `curl -sS -o /dev/null -w "%{http_code}\n"` verify idiom + never-commit blockquote callout)
    - docs/architecture.md (data-path summary)
    - .planning/phases/08/08-RESEARCH.md (Pattern 3 PAT dual-secret-name; Pitfall 1 merge-to-main prerequisite; Pitfall 2 uniform PAT scope; Pitfall 3 cron-recognition delay; Code Examples curl blocks for /api/backfill/*)
    - .planning/phases/08/08-CONTEXT.md (D-08-01..06)
    - .planning/phases/08/08-PATTERNS.md (runbook section: analog structure)
  </read_first>
  <action>
    Create `docs/runbook.md` following docs/access-setup.md's idiom (numbered H2 sections, each with a dashboard-step sub-list, `> ` blockquote callouts for invariants, and terminal `curl`/`bash` blocks with `# expect:` comments). Cover the four DEPLOY-03 areas as H2 sections:

    (1) **Deploy** — create the Cloudflare Pages project (dashboard: Create application → Connect to Git → production branch `main`), bind `LINEAR_API_KEY` and `GH_BACKFILL_TOKEN` (Pages Function bindings) and the `BACKFILL_NONCE` KV namespace; note the merge-to-main prerequisite (Pitfall 1 — `dispatch.ts` hardcodes `ref: "main"` and cron only fires on the default branch, so nothing works until this branch is merged).

    (2) **Token rotation** — the single fine-grained PAT stored under TWO secret names, one value: `GH_BACKFILL_TOKEN` (Cloudflare Pages) and `GH_CROSS_REPO_TOKEN` (GitHub → Settings → Secrets → Actions). Document the scope explicitly: Contents:Read + Actions:Read/Write uniform across all 4 agenticapps-eu repos (`agenticapps-roadmap`, `claude-workflow`, `cparx`, `fx-signal-agent`) — a fine-grained PAT cannot restrict Actions:write to one repo (Pitfall 2); the app-level hardcoded `REPO` constant limits actual blast radius. Also document `LINEAR_API_KEY` dual-binding rotation (Cloudflare Pages secret + GitHub Actions secret, D-08-04). Include a `> Never-commit-secrets` blockquote (mirror access-setup.md).

    (3) **Snapshot refresh** — `snapshot.yml`'s daily `0 6 * * *` UTC cron + `workflow_dispatch`; note GitHub can take 15–60+ min to recognize a cron after a workflow change and a same-session "cron didn't fire" is not a failure (Pitfall 3).

    (4) **Backfill** — the dispatch → preview (dry-run) → apply flow; reference `.planning/phases/07/07-HUMAN-UAT.md` by name for the live checklist rather than duplicating it.

    Reuse the `curl -sS -o /dev/null -w "%{http_code}\n" https://<domain>/...` idiom for Access-gate verification snippets (expect 302/403 on the app root, `/api/linear/snapshot`, `/api/backfill/dispatch`, `/api/backfill/status`). Do not restate the ADR; link to it.
  </action>
  <acceptance_criteria>
    - AUTOMATED: `test -f docs/runbook.md`.
    - AUTOMATED: covers rotation + all three new bindings — `grep -qi 'rotation' docs/runbook.md`, `grep -q 'GH_CROSS_REPO_TOKEN' docs/runbook.md`, `grep -q 'BACKFILL_NONCE' docs/runbook.md`.
    - AUTOMATED: references the live checklist by name — `grep -q '07-HUMAN-UAT' docs/runbook.md`.
    - AUTOMATED: contains a verification curl block — `grep -q 'http_code' docs/runbook.md`.
    - AUTOMATED: documents the merge-to-main prerequisite — `grep -qi 'main' docs/runbook.md` with a Pitfall-1 note (`grep -qi 'default branch' docs/runbook.md`).
    - HUMAN (at tag time): operator can follow it end-to-end.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/runbook.md && grep -qi 'rotation' docs/runbook.md && grep -q 'BACKFILL_NONCE' docs/runbook.md && grep -q '07-HUMAN-UAT' docs/runbook.md && grep -q 'http_code' docs/runbook.md</automated>
  </verify>
  <done>Runbook covers deploy, token rotation (two-secret PAT + LINEAR_API_KEY dual-binding), snapshot refresh (cron + delay caveat), and backfill (referencing 07-HUMAN-UAT.md), with curl verify snippets and the merge-to-main prerequisite documented.</done>
</task>

<task type="auto">
  <name>Task 3: Extend README with the secrets summary and a Deploy pointer</name>
  <files>README.md</files>
  <read_first>
    - README.md (current `## Environment` `| Var | Where | Purpose |` table lines ~48-57; `## Status` lines ~33-37; `## Local development` block; the README-links-out-to-docs pattern)
    - docs/runbook.md (created in Task 2 — link target)
    - .planning/phases/08/08-PATTERNS.md (README section: extend the existing single environment table, don't start a new one)
    - .planning/phases/08/08-CONTEXT.md (D-08-03/04/06 binding topology)
  </read_first>
  <action>
    Surgically extend README.md (do not rewrite). Add rows to the existing `## Environment` `| Var | Where | Purpose |` table: `GH_BACKFILL_TOKEN` (Cloudflare Pages Function binding — dispatch/status GitHub calls), `GH_CROSS_REPO_TOKEN` (GitHub Actions secret — same PAT value, sibling-repo checkout), and `BACKFILL_NONCE` (Cloudflare KV binding — consume-once apply nonce, D-08-06). Add a concise `## Deploy` H2 section (2-4 lines) that states the app is hosted on Cloudflare Pages behind Access and links to `docs/runbook.md` for the full deploy/rotation/backfill procedure — keep README high-level, defer detail to the runbook (mirror the existing `## Status` → docs/claude-code-prompts.md link pattern). Do not duplicate runbook content. Leave the `## Status` v0.1.0 wording to the tag step in plan 08-03.
  </action>
  <acceptance_criteria>
    - AUTOMATED: `grep -q 'GH_BACKFILL_TOKEN' README.md`, `grep -q 'GH_CROSS_REPO_TOKEN' README.md`, `grep -q 'BACKFILL_NONCE' README.md`.
    - AUTOMATED: `grep -q 'docs/runbook.md' README.md` (Deploy pointer present).
    - AUTOMATED: existing content preserved — `grep -q 'LINEAR_API_KEY' README.md` still succeeds (Environment table not clobbered).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GH_BACKFILL_TOKEN' README.md && grep -q 'BACKFILL_NONCE' README.md && grep -q 'docs/runbook.md' README.md && grep -q 'LINEAR_API_KEY' README.md</automated>
  </verify>
  <done>README Environment table gains the three new binding rows and a `## Deploy` section links to the runbook; existing sections untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed docs | Documentation is committed to a private repo; the risk is a real secret value being pasted into an example rather than a placeholder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | a real PAT/`LINEAR_API_KEY` value pasted into runbook/README examples | mitigate | Use placeholder/pattern text only (`ghp_…`, `<token>`); the rotation section describes where secrets live, never their values (CLAUDE.md never-commit invariant). The existing CI grep gate (`lin_api_`, `ghp_`, `github_pat_`) catches an accidental paste at commit. |
| T-08-05 | Repudiation / drift | ADR/runbook understating the actual PAT scope (implying tighter scope than configured) | mitigate | ADR Consequences + runbook rotation section explicitly state the uniform 4-repo grant (RESEARCH Pitfall 2) so a future reviewer is not misled. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs — docs-only plan. No slopcheck gate required. |
</threat_model>

<verification>
- `test -f docs/decisions/0001-hosting-and-sync-architecture.md` with 5 H2 sections.
- `test -f docs/runbook.md` covering rotation + BACKFILL_NONCE + 07-HUMAN-UAT reference + curl verify.
- README grep gates pass; existing Environment table preserved.
- No real secret literals in any created doc (`grep -rE 'ghp_[A-Za-z0-9]|github_pat_|lin_api_' docs/ README.md` finds no real token — placeholders only).
</verification>

<success_criteria>
The hosting/sync ADR exists (unblocking the DEPLOY-04 tag), the runbook covers all four
DEPLOY-03 areas with the two-secret PAT pattern and merge-to-main prerequisite, and the README
summarizes the new bindings and points to the runbook — all without leaking a secret value.
</success_criteria>

<output>
Create `.planning/phases/08/08-02-SUMMARY.md` when done.
</output>
