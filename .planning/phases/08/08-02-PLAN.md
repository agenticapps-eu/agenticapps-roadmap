---
phase: 08-deploy-gate-document
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/0001-hosting-and-sync-architecture.md
  - docs/runbook.md
  - docs/access-setup.md
  - docs/architecture.md
  - README.md
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04]
must_haves:
  truths:
    - "A reader can follow docs/runbook.md to deploy, rotate the token(s), understand snapshot refresh, and run a backfill without reading source."
    - "docs/decisions/0001-hosting-and-sync-architecture.md records the Cloudflare Pages + CI-dispatch hosting/sync decision, its rejected alternatives, and the accepted KV/PAT-scope risks."
    - "The ADR AND the runbook both state — in their FIRST authored version — that live write-capable secrets (LINEAR_API_KEY, the PAT) bind to the Production Pages environment ONLY, never to the ungated preview env. Documented as a security consequence of the already-locked D-08-02 (preview URLs ungated) + D-08-04 + the RESEARCH fact that a single top-level [[kv_namespaces]] binding applies to both Production and Preview — NOT a new numbered decision."
    - "docs/access-setup.md is reconciled with D-08-01 (a single whole-domain Access app protects the root AND all of /api/*) — it no longer mandates a separate /api/* application. docs/architecture.md no longer describes the scheduled snapshot as an open/optional choice."
    - "README documents where GH_BACKFILL_TOKEN, GH_CROSS_REPO_TOKEN, and BACKFILL_NONCE live and points to the runbook."
  artifacts:
    - path: "docs/decisions/0001-hosting-and-sync-architecture.md"
      provides: "hosting/sync ADR for the v0.1.0 tag (DEPLOY-04), incl. the Production-only-secrets boundary"
      contains: "## Alternatives Rejected"
    - path: "docs/runbook.md"
      provides: "deploy / token-rotation / snapshot-refresh / backfill operational runbook (DEPLOY-03), KV via wrangler.toml only, Production-only secrets"
      contains: "rotation"
    - path: "docs/access-setup.md"
      provides: "Access setup reconciled with D-08-01 (single whole-domain app, no separate /api/* app)"
      contains: "whole domain"
    - path: "README.md"
      provides: "environment/secrets summary + Deploy pointer to the runbook"
      contains: "GH_BACKFILL_TOKEN"
  key_links:
    - from: "README.md ## Deploy section"
      to: "docs/runbook.md"
      via: "markdown link"
      pattern: "docs/runbook\\.md"
    - from: "docs/runbook.md"
      to: ".planning/phases/07/07-HUMAN-UAT.md"
      via: "reference-by-name (do not duplicate the checklist)"
      pattern: "07-HUMAN-UAT"
---

<objective>
Write the operator documentation that DEPLOY-03/DEPLOY-04 require: a hosting/sync
ADR (first entry in a new `docs/decisions/` directory), an operational
`docs/runbook.md` (deploy, token rotation, snapshot refresh, backfill), a
reconciliation of the two existing docs that currently contradict the locked
Phase-8 decisions (`docs/access-setup.md`, `docs/architecture.md`), and surgical
README additions (secrets summary + a Deploy pointer). No code.

Purpose: The v0.1.0 tag (plan 08-03) is gated on the ADR existing and the runbook
covering the four DEPLOY-03 areas. This plan produces those artifacts — including
the Production-only-secrets boundary in their FIRST version so nothing ships stale
and no post-hoc ADR patch is needed — so the live plan only has to bind, verify,
and tag. It also removes two live contradictions: `docs/access-setup.md` still
mandates a separate `/api/*` Access application (contradicts D-08-01's single
whole-domain app), and `docs/architecture.md` still calls the scheduled snapshot
an open/optional choice (it is now the shipped CI cron).
Output: `docs/decisions/0001-hosting-and-sync-architecture.md`, `docs/runbook.md`,
reconciled `docs/access-setup.md` + `docs/architecture.md`, and extended `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08/08-CONTEXT.md
@.planning/phases/08/08-RESEARCH.md
@.planning/phases/08/08-PATTERNS.md
@docs/architecture.md
@docs/access-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the hosting/sync ADR (incl. the Production-only-secrets boundary)</name>
  <files>docs/decisions/0001-hosting-and-sync-architecture.md</files>
  <read_first>
    - ../claude-workflow/docs/decisions/0010-backend-language-routing-go.md (cross-repo ADR format: header block + section skeleton)
    - docs/architecture.md ("Why Cloudflare Pages over GitHub Pages" + Decisions table — source prose)
    - .planning/phases/08/08-CONTEXT.md (D-08-01..06 with their inline "Rejected:" alternatives; D-08-02 preview URLs left UNGATED; D-08-04 LINEAR_API_KEY dual-binding)
    - .planning/phases/07/07-CONTEXT.md (D-07-01 CI dispatch, D-07-02 two-phase, D-07-03 Access-only write auth)
    - .planning/phases/08/08-RESEARCH.md (Pitfall 2 uniform-PAT scope; Pitfall 4 KV eventual-consistency; Assumption A1 single [[kv_namespaces]] binding applies to both Production + Preview)
    - .planning/phases/08/08-PATTERNS.md (ADR header/section skeleton)
  </read_first>
  <action>
    Create the `docs/decisions/` directory and `0001-hosting-and-sync-architecture.md`. Mirror the sibling ADR's four metadata lines exactly: `**Status:** Accepted`, `**Date:** 2026-07-16`, `**Linear:** —` (or the AgenticApps Roadmap Linear project link if known), `**Phase:** Phase 8 of agenticapps-roadmap`. Use the H2 section order: `## Context`, `## Decision`, `## Alternatives Rejected`, `## Consequences` (with `**Positive:**`/`**Negative:**`/`**Follow-ups:**` sub-bullets), `## References`. Title: `# ADR-0001: Hosting and sync architecture`.

    `## Context` + `## Decision` record the hosting choice (Cloudflare Pages + Pages Functions, snapshot-first zero-network default) and the sync architecture (CI-dispatch write path D-07-01, two-phase preview→apply D-07-02, Access-only write authorization D-07-03, single fine-grained PAT D-08-03, KV consume-once nonce D-08-06). `## Alternatives Rejected` draws on the inline Rejected notes in 08-CONTEXT (public `/api/linear/*`, split least-privilege PATs, recency-only nonce) and 07-CONTEXT (Worker-direct-write, diff-from-scheduled-artifact).

    `## Consequences` MUST document THREE accepted-risk / boundary facts:
    (1) the KV nonce is best-effort — Workers KV is eventually consistent with no compare-and-swap, so replay suppression is best-effort sequential (not "exactly once") and a duplicate remains possible within the consistency window (RESEARCH Pitfall 4); the 15-min recency bound co-mitigates.
    (2) the single fine-grained PAT grants Contents:Read + Actions:Read/Write uniformly across all 4 agenticapps-eu repos, not scoped to one (RESEARCH Pitfall 2), mitigated by the hardcoded `REPO` constant in dispatch.ts/status.ts.
    (3) the Production/Preview secret boundary: live write-capable secrets (LINEAR_API_KEY, the PAT) bind to the PRODUCTION Pages environment ONLY. Because D-08-02 leaves preview `*.pages.dev` URLs ungated by Access, and because a single top-level `[[kv_namespaces]]` binding applies to BOTH Production and Preview (RESEARCH Assumption A1), binding those secrets to Preview would place write-capable credentials behind an unauthenticated URL. State this as a security CONSEQUENCE of the already-locked D-08-02 + D-08-04 (NOT as a new numbered decision). Note that the app renders fully from `public/roadmap.json` with zero network, so a preview build needs no live secrets to succeed.

    `## References` links docs/architecture.md, docs/runbook.md, docs/access-setup.md, and the phase CONTEXT files. This is a compilation/backfill ADR (recording a shipped decision), not a proposal.
  </action>
  <acceptance_criteria>
    - AUTOMATED: `test -f docs/decisions/0001-hosting-and-sync-architecture.md`.
    - AUTOMATED (per-title, not a count): each required H2 present — grep individually for `^## Context`, `^## Decision`, `^## Alternatives Rejected`, `^## Consequences`, and `^## References`; all five must succeed.
    - AUTOMATED: `grep -q '^\*\*Status:\*\* Accepted'` and `grep -q 'Phase 8'` both succeed.
    - AUTOMATED: accepted-risk + boundary coverage — `grep -qi 'eventual'` (KV consistency), `grep -qi 'all 4'` (uniform PAT scope), `grep -qi 'production'` AND `grep -qi 'preview'` (Production-only-secrets boundary present in v1).
    - HUMAN (at tag time, plan 08-03): prose is accurate against the actual shipped decisions.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/decisions/0001-hosting-and-sync-architecture.md && for h in Context Decision "Alternatives Rejected" Consequences References; do grep -q "^## $h" docs/decisions/0001-hosting-and-sync-architecture.md || exit 1; done</automated>
  </verify>
  <done>ADR-0001 created with the sibling-repo header + the 5 required H2 titles (grepped individually); hosting + sync decisions recorded; rejected alternatives and the three accepted-risk/boundary facts (KV consistency, uniform PAT scope, Production-only secrets) documented in v1.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/runbook.md + reconcile docs/access-setup.md and docs/architecture.md</name>
  <files>docs/runbook.md, docs/access-setup.md, docs/architecture.md</files>
  <read_first>
    - docs/access-setup.md (its section 2 currently mandates a SEPARATE /api/* Access application/path target as required — contradicts D-08-01; mirror its numbered-H2-step + http_code verify idiom + never-commit blockquote)
    - docs/architecture.md (line ~25 "an optional Pages cron" and line ~62 "cron in CI vs Pages cron trigger" open decision — the schedule is now SETTLED as the CI snapshot.yml cron)
    - .planning/phases/08/08-RESEARCH.md (Pattern 3 PAT dual-secret-name; Pitfall 1 merge-to-main; Pitfall 2 uniform PAT scope; Pitfall 3 cron-recognition delay; Assumption A1 single [[kv_namespaces]] binding; wrangler-is-source-of-truth — dashboard KV fields become non-editable with a Wrangler file)
    - .planning/phases/08/08-CONTEXT.md (D-08-01 whole-domain gate, D-08-02 preview ungated, D-08-03 single PAT, D-08-04 LINEAR_API_KEY dual-binding)
    - .planning/phases/08/08-PATTERNS.md (runbook section: analog structure)
  </read_first>
  <action>
    (A) Create `docs/runbook.md` following docs/access-setup.md's idiom (numbered H2 sections, dashboard-step sub-lists, blockquote callouts, terminal curl/bash blocks with `# expect:` comments). Cover the four DEPLOY-03 areas as H2 sections, each grep-detectable by its area keyword:

    (1) Deploy — create the Cloudflare Pages project (Create application → Connect to Git → production branch `main`, build command + output dir `dist`). `wrangler.toml` is the SOLE source of truth for the `BACKFILL_NONCE` KV binding — do NOT attach the KV binding in the dashboard; when a Pages Wrangler file is present it is authoritative and matching dashboard fields become non-editable. The dashboard is used ONLY for encrypted secrets + Access. Bind `LINEAR_API_KEY` and `GH_BACKFILL_TOKEN` as Production-environment-only Pages Function secrets — NOT on Preview: because D-08-02 leaves preview `*.pages.dev` ungated and the single top-level `[[kv_namespaces]]` binding applies to both environments (Assumption A1), binding live secrets to Preview would expose write-capable credentials at an unauthenticated URL; the app renders from `public/roadmap.json` with zero network so a preview build needs no live secrets. Note the merge-to-main prerequisite (Pitfall 1 — `dispatch.ts` hardcodes `ref: "main"` and cron only fires on the default branch) and the post-secret-change redeploy step.

    (2) Token rotation — the single fine-grained PAT stored under TWO secret names, one value: `GH_BACKFILL_TOKEN` (Cloudflare Pages, Production) and `GH_CROSS_REPO_TOKEN` (GitHub → Settings → Secrets → Actions). Scope: Contents:Read + Actions:Read/Write uniform across all 4 agenticapps-eu repos (`agenticapps-roadmap`, `claude-workflow`, `cparx`, `fx-signal-agent`) — a fine-grained PAT cannot restrict Actions:write to one repo (Pitfall 2); the app-level hardcoded `REPO` constant limits actual blast radius. Rotation ORDER: create + (if org requires) approve the replacement PAT, update BOTH secret stores, run a dry-run/status smoke, and only THEN revoke the old token. Also document `LINEAR_API_KEY` dual-binding rotation (Cloudflare Pages Production secret + GitHub Actions secret, D-08-04). Include a Never-commit-secrets blockquote.

    (3) Snapshot refresh — `snapshot.yml`'s daily `0 6 * * *` UTC cron + `workflow_dispatch`; GitHub can take 15–60+ min to recognize a cron after a workflow change and a same-session "cron didn't fire" is not a failure (Pitfall 3); a run makes NO commit when the snapshot is unchanged (verifying a real cron commit requires a staged Linear delta).

    (4) Backfill — the dispatch → preview (dry-run) → apply flow with concise UI steps, expected outcomes, and retry/failure recovery (after an upstream dispatch failure a previewRunId is already consumed → re-preview required); reference `.planning/phases/07/07-HUMAN-UAT.md` by name for the release checklist rather than duplicating it. Reuse the `curl -sS -o /dev/null -w "%{http_code}\n" https://<domain>/...` idiom (expect 302/403 on the app root and every `/api/*` route). Do not restate the ADR; link to it.

    (B) Reconcile `docs/access-setup.md` with D-08-01. Its section 2 currently says the policy must cover two targets and that omitting a separate `/api/*` application leaves the proxy open (mandating a second app). Rewrite section 2 so it describes a SINGLE self-hosted Access application over the WHOLE Pages domain with an empty path — which protects the root AND all paths including `/api/backfill/*` and `/api/linear/*` (D-08-01). Remove the second-application/separate-`/api/*` requirement and the "Both targets are required" line; keep the email allow-list steps and the unauthenticated-blocked verification. Note the preview `*.pages.dev` gating residual-hardening option is deferred per D-08-02.

    (C) Reconcile `docs/architecture.md`. Update the "optional Pages cron" language and the open "cron in CI vs Pages cron trigger" decision line to the settled choice: the scheduled snapshot refresh is the CI `snapshot.yml` cron (`0 6 * * *` UTC) on `main` — no longer open/optional. Keep it surgical (a couple of lines). Leave the "GitHub Pages optional public mirror" wording as-is (a separate deferred idea).
  </action>
  <acceptance_criteria>
    - AUTOMATED: `test -f docs/runbook.md`.
    - AUTOMATED (one grep per DEPLOY-03 area): `grep -qi 'deploy'`, `grep -qi 'rotation'`, `grep -qi 'snapshot'`, `grep -qi 'backfill'` on docs/runbook.md all succeed.
    - AUTOMATED: new bindings + Production-only + KV-source-of-truth — `grep -q 'GH_CROSS_REPO_TOKEN'`, `grep -q 'BACKFILL_NONCE'`, `grep -qi 'production'`, `grep -qi 'wrangler.toml'` on docs/runbook.md.
    - AUTOMATED: `grep -q '07-HUMAN-UAT' docs/runbook.md`; `grep -q 'http_code' docs/runbook.md`; `grep -qi 'default branch' docs/runbook.md`.
    - AUTOMATED (access-setup reconciled): `grep -qi 'whole domain' docs/access-setup.md` succeeds; a grep for the phrase "Both targets are required" in docs/access-setup.md must return nothing (the mandatory-second-app line is removed).
    - AUTOMATED (architecture reconciled): a grep for "optional Pages cron" in docs/architecture.md must return nothing (schedule no longer described as optional).
    - HUMAN (at tag time): operator can follow the runbook end-to-end.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/runbook.md && grep -qi 'rotation' docs/runbook.md && grep -q 'BACKFILL_NONCE' docs/runbook.md && grep -q '07-HUMAN-UAT' docs/runbook.md && grep -qi 'whole domain' docs/access-setup.md</automated>
  </verify>
  <done>Runbook covers deploy, token rotation, snapshot refresh, and backfill with the two-secret PAT pattern, Production-only secrets, wrangler.toml-as-KV-source-of-truth, and the merge-to-main prerequisite; access-setup.md now describes a single whole-domain Access app (no separate /api/* app); architecture.md's schedule is no longer "optional".</done>
</task>

<task type="auto">
  <name>Task 3: Extend README with the secrets summary and a Deploy pointer</name>
  <files>README.md</files>
  <read_first>
    - README.md (current `## Environment` `| Var | Where | Purpose |` table lines ~48-57; `## Status` lines ~33-37; the README-links-out-to-docs pattern)
    - docs/runbook.md (created in Task 2 — link target)
    - .planning/phases/08/08-PATTERNS.md (README section: extend the existing single environment table, don't start a new one)
    - .planning/phases/08/08-CONTEXT.md (D-08-03/04/06 binding topology)
  </read_first>
  <action>
    Surgically extend README.md (do not rewrite). Add rows to the existing `## Environment` `| Var | Where | Purpose |` table: `GH_BACKFILL_TOKEN` (Cloudflare Pages Function binding, Production — dispatch/status GitHub calls), `GH_CROSS_REPO_TOKEN` (GitHub Actions secret — same PAT value, sibling-repo checkout), and `BACKFILL_NONCE` (Cloudflare KV binding — consume-once apply nonce, D-08-06). Add a concise `## Deploy` H2 section (2-4 lines) stating the app is hosted on Cloudflare Pages behind Access and linking to `docs/runbook.md` for the full deploy/rotation/backfill procedure — keep README high-level, defer detail to the runbook. Do not duplicate runbook content. Leave the `## Status` v0.1.0 wording to the tag step in plan 08-03 (which owns the README status update).
  </action>
  <acceptance_criteria>
    - AUTOMATED: `grep -q 'GH_BACKFILL_TOKEN' README.md`, `grep -q 'GH_CROSS_REPO_TOKEN' README.md`, `grep -q 'BACKFILL_NONCE' README.md`.
    - AUTOMATED: `grep -q 'docs/runbook.md' README.md` (Deploy pointer present).
    - AUTOMATED: existing content preserved — `grep -q 'LINEAR_API_KEY' README.md` still succeeds (Environment table not clobbered).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GH_BACKFILL_TOKEN' README.md && grep -q 'BACKFILL_NONCE' README.md && grep -q 'docs/runbook.md' README.md && grep -q 'LINEAR_API_KEY' README.md</automated>
  </verify>
  <done>README Environment table gains the three new binding rows and a `## Deploy` section links to the runbook; existing sections untouched; the `## Status` v0.1.0 update is left to plan 08-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed docs | Documentation is committed to a private repo; the risk is a real secret value being pasted into an example rather than a placeholder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | a real PAT/`LINEAR_API_KEY` value pasted into runbook/README examples | mitigate | Use placeholder/pattern text only (`ghp_…`, `<token>`); the rotation section describes where secrets live, never their values (CLAUDE.md never-commit invariant). The existing CI grep gate (`lin_api_`, `ghp_`, `github_pat_`) catches an accidental paste at commit. |
| T-08-05 | Repudiation / drift | ADR/runbook understating the actual PAT scope, or docs contradicting each other (access-setup mandating a second /api/* app vs D-08-01; architecture calling the schedule optional) | mitigate | ADR Consequences + runbook rotation section explicitly state the uniform 4-repo grant (RESEARCH Pitfall 2); Task 2 reconciles access-setup.md to the single whole-domain app and architecture.md to the settled CI cron, so the repo does not ship two contradictory runbooks. |
| T-08-10 | Information Disclosure | docs implying live secrets are safe to bind to the ungated preview environment | mitigate | ADR + runbook state the Production-only-secrets boundary in v1 (consequence of D-08-02 + D-08-04 + Assumption A1) — the ungated `*.pages.dev` preview env never carries LINEAR_API_KEY or the PAT. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs — docs-only plan. No slopcheck gate required. |
</threat_model>

<verification>
- `test -f docs/decisions/0001-hosting-and-sync-architecture.md` with the 5 required H2 titles grepped individually.
- `test -f docs/runbook.md` covering all four DEPLOY-03 areas (deploy/rotation/snapshot/backfill) + BACKFILL_NONCE + Production-only + wrangler.toml-source-of-truth + 07-HUMAN-UAT reference + curl verify.
- docs/access-setup.md reconciled to a single whole-domain Access app (no "Both targets are required"); docs/architecture.md schedule no longer "optional".
- README grep gates pass; existing Environment table preserved.
- No real secret literals in any created/edited doc (`grep -rE 'ghp_[A-Za-z0-9]|github_pat_|lin_api_' docs/ README.md` finds no real token — placeholders only).
</verification>

<success_criteria>
The hosting/sync ADR exists (unblocking the DEPLOY-04 tag) with the Production-only-secrets
boundary in v1, the runbook covers all four DEPLOY-03 areas with the two-secret PAT pattern,
KV-via-wrangler-only, and the merge-to-main prerequisite, the two contradictory docs
(access-setup.md, architecture.md) are reconciled to the locked decisions, and the README
summarizes the new bindings and points to the runbook — all without leaking a secret value.
</success_criteria>

<output>
Create `.planning/phases/08/08-02-SUMMARY.md` when done.
</output>
