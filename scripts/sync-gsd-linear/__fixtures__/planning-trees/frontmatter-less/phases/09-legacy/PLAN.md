# Phase 09 — PLAN

- [ ] Migrate the legacy config loader
- [ ] Remove the old flag parser
- [ ] Update the README

No YAML frontmatter and a generic, non-descriptive H1 (pre-migration-era
plan, mirrors `claude-workflow`'s real pre-GSD-migration plans). Issue-title
extraction must fall back to the directory slug (`09-legacy`) rather than
using this heading verbatim.
