# Roadmap (stub)

> This ROADMAP.md is an intentionally partial stub — it enumerates only
> `01-gsd-bug-fixes`, NOT `01-go-routing` (which shipped earlier and is
> tracked only via git history). Mirrors the real `claude-workflow` repo's
> ROADMAP.md, which only covers phases 25+.
>
> The walker MUST NOT treat this file as the source of phase existence —
> phase existence always comes from `phases/*` directory listing. This file
> is only an optional completion-status enrichment layer.

- [x] Phase 01 (gsd-bug-fixes): Fix off-by-one in pagination cursor
